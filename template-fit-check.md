# 母版适合性检测（把本文件内容发给 AI）

在旧工程目录下启动 Claude Code 后，把下面的内容粘贴给它：

---

请检测当前目录这个嵌入式工程是否适合作为"母版模板"。母版用途：电赛工程生成器以母版为骨架复制出完整工程，"打开就能编译、直接开写"。因此母版必须：平台清晰、配置完整、内容通用干净、不含具体赛题逻辑。

## 检测项（逐项执行，给出证据文件路径）

0. **目录定位**：先确认当前目录是一个工程，还是包含多个工程或无关文件。不是单个工程请直接说明并停止。
1. **平台识别**：查找 `.uvprojx`（STM32/Keil）和 `.cproject`（MSPM0/CCS）。必须恰好存在一种：
   - 两种都有 → 不适合（平台含糊）
   - 都没有 → 不适合（无法识别平台）
2. **IDE 必需配置文件**：STM32 需 `.uvprojx`；MSPM0 需 `.cproject` + `.project`。缺失 → 不适合。
3. **目录结构总览**：列出顶层目录与文件。`.git`、`Debug`、`Release` 允许存在（工具会自动忽略），但请说明里面是什么。
4. **源码通用性**：读 `main.c` 和主要源文件——是否写死了某个具体赛题/具体功能（具体外设初始化、状态机、业务逻辑）？母版应是能编译的通用骨架，赛题相关内容应删掉。
5. **残留物扫描**：构建产物（.o/.axf/.hex/.map）、备份文件（*.bak/*~）、临时文件、无用杂物。
6. **个人痕迹**：工程配置文件里是否有本机绝对路径、用户名等个人痕迹。
7. **编译宏/设备**：设备型号、include path、编译宏是否正常、是否通用。

## 输出格式

一份结论报告：

- **结论**：适合 / 不适合 / 清理后可适合
- **逐项检查结果**（0–7，每项一句话 + 证据文件路径）
- **清理清单**：按两级列出，每项给出具体路径和原因
  - 必须清理才能用（如：平台配置文件缺失、赛题逻辑写死在 main.c、个人绝对路径）
  - 建议清理（如：残留产物、备份文件）
