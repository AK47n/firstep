__pycache__/
*.pyc
.pytest_cache/
.venv/
.mypy_cache/
*.egg-info/

# Claude Code 并行 worktree（内嵌 git 仓库，绝不提交）
.claude/worktrees/

# 真机验收产物（Keil 编译日志 / 生成工程 / 会话日志）
.scratch/real-run/

# 推荐缓存（工单 check-recommend-cache/01 决策 3：done 载荷跨运行落盘；
# 已被上行整目录忽略覆盖，显式一行防将来整目录规则收窄时漏网）
.scratch/real-run/cache/

# 本地服务运行日志（uvicorn stdout 重定向）
webapp.log

# fix-errors 运行产物：真机修复的备份目录（输出目录外，可回滚；不入库）
library/fix-backups/
