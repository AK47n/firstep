__pycache__/
*.pyc
.pytest_cache/
.venv/
.mypy_cache/
*.egg-info/

# Claude Code 并行 worktree（内嵌 git 仓库，绝不提交）
.claude/worktrees/

# 真机验收产物（Keil 编译日志 / 生成工程 / 会话日志）
.scratch/real-run/
.scratch/module-functionalize/out_*_mspm0/
.scratch/module-polish/matrix/
.scratch/module-multi-instance/matrix/

# 本地服务运行日志（uvicorn stdout 重定向）
webapp.log

# DSH 桌面端 / 开发服务器产物（运行日志 + 安装包，非本仓库源码）
dsh-web-*.log
updates/

# fix-errors 运行产物：真机修复的备份目录（输出目录外，可回滚；不入库）
library/fix-backups/

# b1-adc-servo 双平台编译矩阵产物（生成工程 + 真机编译日志，可重建；不入库）
.scratch/b1-adc-servo/out_matrix/
# PDF 疑似重复回收目录（不真删、移进回收目录；git 忽略 + 可手动恢复）
sources/.trash-pdf/

# 资料库素材（含大量 100MB 级文件，GitHub 仓库不收；走资料库增量包分发，不进仓库）
sources/materials/

# key-multi-instance 编译矩阵产物（矩阵摘要 + build.log / key_instances.h，可重建；不入库）
.scratch/key-multi-instance/matrix/


# 资料库更新探针产物（浏览器冒烟 profile / 截图，不入库）
.scratch/materials-update/chrome-profile/
.scratch/materials-update/*.png
.scratch/wiki-modules-batch12/matrix/

# 代码页冒烟脚本自建夹具（每次运行重生成，不入库；2026-09-09 第六轮 CDP 实跑）
.scratch/code-page-vscode-overhaul/sample-proj/
.scratch/code-editor-vscode-polish/sample-proj/
.scratch/code-editor-perf-structural/sample-proj/

# 第十轮补：其余代码页冒烟脚本的同类夹具（同样每次运行重生成）
.scratch/code-editor-refine/sample-proj*/
.scratch/code-ide-ai/sample-proj/
.scratch/code-ide-flow/sample-proj/
.scratch/code-viewer-editor/sample-proj/

# CDP 批跑器的逐次运行记录（run-<时间戳>.json，每次批跑都新增；不入库）
.scratch/cdp-smoke-runs/

# 真机验收（playwright，工单 module-intro-detail/05）：依赖与浏览器二进制不入库
# ——`npm install && npx playwright install chromium` 一行即可重建；
# 浏览器本体默认装在 %LOCALAPPDATA%\ms-playwright（本来就在仓库外）。
node_modules/
tests/browser/**/test-results/
tests/browser/**/*.png

# 磁力计双件批次的编译矩阵产物（生成工程，可重建；2026-09-12）
.scratch/magnetometer-modules/matrix/
.scratch/magnetometer-modules/matrix-logs/

# .scratch 下 C 探针的本机编译产物（.c 入库当证据，exe 每次可重建；2026-09-13 收尾）
# 构建命令写在各自 .c 的文件头（如 probe/heading_check.c 的 gcc -O2 ... -lm）
.scratch/**/*.exe

# 跨包字节一致性验证的打包产物（真实仓库 1 GB 级 zip 分卷，脚本可重建；2026-09-13）
.scratch/full-download/_verify08/

# 素材库回收目录（不真删、可手动恢复；与 -xr!.trash-pdf 的既有口径一致）
sources/.trash-dedup/
sources/.trash-pdf/

# 「没展开的环境变量」被当成相对路径落进仓库的产物（2026-09-13 清理）：某脚本把
# `%SystemDrive%\ProgramData\...\Windows\Caches` 原样写进了仓库根，又被 `git add`
# 带进提交（`2c22f061`），三个 Windows 字体缓存 db 因此一直在库里。已 `git rm` 清掉。
# 根因（写这份文件的脚本）没找到，先在这里挡住这个形状：目录名就是没展开的变量，一律不入库。
%SystemDrive%/
%USERPROFILE%/
%LOCALAPPDATA%/
%TEMP%/


# 说明（2026-09-13）：上面有 4 行注释（b1-adc-servo / PDF 回收 / 资料库素材 / key-multi-instance）
# 的原文在一次写盘事故里被替换字符（U+FFFD）毁掉，git 历史里也从未有过完好版本，
# 故是**按上下文重写**；另 3 行（worktree / 真机验收 / fix-errors）逐字还原自历史。
# **忽略规则一行未改**（改前后规则清单逐行比对相同）。
