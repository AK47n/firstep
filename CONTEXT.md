# CONTEXT.md — 电赛工程生成器

单上下文领域词表（架构评审 / 领域建模的共享语言）。实现决策见 `docs/adr/`。

## 领域词表

| 术语 | 含义 | 主要实现 |
|---|---|---|
| 赛题 | 粘贴或上传（PDF / .docx / .txt / .md）的竞赛题目原文 | extraction.py → str，贯穿所有 LLM prompt；生成入口装配唯一出处 = generator.resolve_topic_context（TopicContext） |
| 平台 | `stm32`（STM32F103C8T6 / Keil5）、`mspm0`（地猛星 MSPM0G3507 / CCS） | 词表在 platforms.py；识别知识（工程配置文件后缀表）= platforms.PLATFORM_CONFIG_FILE_SUFFIXES 单源；蒸馏侧平台行为经 distill_adapters 适配器（master 只消费）；生成侧行为在 patchers.py / webapp.py；mspm0 母版 syscfg = 默认外设布局（模块代码与 syscfg 实例名绑定，改引脚不改实例名）；**协议串口默认实例分配（module-functionalize/05-09）：UART0=IMU601、UART1=DIGIT_UART（digit_uart/coord_detect 共享）、UART2=UWB_UART、UART3=ZIGBEE_UART（zigbee_uart 收/zigbee_uart_key 发共享）**；**ADC12_0 = adc 模块（MEM0=PA24/A0_3、MEM1=PA25/A0_2，轮询；b1-adc-servo/01）、SERVO_PWM = servo 模块（TIMG8 C0=PA7，周期按 INST_CLK_FREQ 运行时算；b1-adc-servo/02）**；**2026-08-14 起母版 syscfg 已地猛星化（引脚全落在地猛星 2×20 排针，工单 mspm0-master-dimx/01）**；工具链外部头豁免（stm32 = DFP 提供、mspm0 = SysConfig 生成）经 patchers.external_headers 分派，C 标准库头归门禁（generator._LIBC_HEADERS，平台无关） |
| 模块 | 可复用 .c/.h 单元 + 机器可读 manifest；库目录即数据库；与功能库相对——模块承载外设/赛题功能，功能库是母版自带底层库；**模块 = 纯驱动切片（init + 服务函数），不自带状态机/调度/流程逻辑（ADR 0009）——逻辑归生成骨架，跨题复用逻辑归参考文件库；能力方向必声明（简介判据③）、无题绑定（判据④）**；模块推荐（AI 选模块）的模型类与收敛工作流归 selection.py——llm 层运行时依赖 selection 而非反向（report.py 先例） | manifest.py（模型）/ library.py（库操作）；模块推荐域在 selection.py；推荐域判决（build_module_selection：模型输出 → ModuleSelection 解释链——需求派生 / 词表约束 / DeepSeek 怪癖）在 selection.py，llm 只做机械提取 |
| manifest | 模块目录下 manifest.json：slug、简介、依赖、平台条目（文件 / 验证状态 / 硬件绑定 / 备注 / 硬件身份字段 kit + source_url） | manifest.py |
| 简介 | manifest.description，判据四要素：① 与代码一致（AI 校验）；② 硬件身份可确认——套件型号（kit）与购买链接（source_url），平台条目字段、新录入必填、URL 格式校验、由人补填；③ 能力方向——声明可用于哪类赛题功能（如"灰度循迹""PID 闭环控制""K230 视觉帧解析"），允许多值点明主能力，补录必填；④ 无题绑定——禁题号/年份/具体题名/专用逻辑（"XX 题专用"类别已消灭，ADR 0009；机械拦截词表单源 = library.py 的 BANNED_TOPIC_WORDS / CAPABILITY_WORDS，结构测试与补录流程共用，改词表只改那一处） | library.py / llm.py（AI 校验）；结构测试机械拦截（黑名单词表单源 = library.py） |
| 能力方向 | 模块的普适性声明：可用于哪类赛题功能（如"灰度循迹""PID 闭环控制""K230 视觉帧解析"），简介判据③、补录必填，允许多值点明主能力；普适 ≠ 万金油——是方向明确的复用切片，推荐链路（功能需求层 → 模块匹配）据此对齐 | 简介判据③（library.py / llm.py） |
| 依赖 | manifest 声明的模块依赖，生成前递归展开（依赖先于使用者）；上层依赖下层；不得声明对功能库的依赖（母版必有，声明反而使解析器找不到而报错） | selection.py |
| 可选配套 | 模块的可裁剪组件：如 filter 之于 uwb_uart——代码层现状为必需依赖（uwb_uart.c 写死 include 与滤波调用），可选化（条件编译 + 生成器按选中定义宏）已立项未实现；普适 filter / 普适巡线逻辑为将来方向 | （未实现） |
| 巡线 | 灰度循迹类能力（21F / 2024H / 2026H 巡线题），模块库内两层：**驱动层**（模块承载、普适）——灰度读取（stm32=pid 内 gray_track / mspm0=huidu）+ 循迹核心（stm32=pid.c / mspm0=motor 内嵌 adjust_motor_pwm 加权质心，原 huidu.c 并入）+ 加权质心（xunji_centroid）；**决策层**（路口检测 / 模式状态机 / 声光时序 = 题逻辑）——进生成骨架，素材来自参考文件库（2024H 原生决策源码已归档 car-1-1），不落模块（ADR 0009）；决策层原实现在各模块内，由工单 module-universalization 02/03 逐模块剥离 | manifest 简介 / library/modules |
| 母版 | 每平台一个的基础工程；现阶段 = 空的最小系统板工程（平台基础设施齐全 + 模板 main.c，能直接编译烧录；stm32 母版自带全套逐飞库 ml_* 功能库；**mspm0 母版 = TI 官方 empty 示例（CCS Theia 20.5 导出，TMS470_TICLANG 4.0）整理入库**：main.c 模板 = SYSCFG_DL_init() + while(1)（ADR 0002 形态），mspm0.syscfg 已地猛星化（2026-08-14，工单 mspm0-master-dimx/01；原为 TI 官方板 LP_MSPM0G3507 布局），.cproject/.project 原样保留语义）；元数据在母版目录外的平级 json；**stm32 母版另带工程根 pin_config.h（电机引脚宏集中，值 = 21F 原值；motor/pid 模块只引用宏不写死引脚，板级引脚配置只改此文件（ADR 0010），对偶 mspm0 SysConfig 生成的 DC_MOTOR_* 宏）与工程根 led_instances.h（LED 通道宏 + 通道表，多实例渲染产物——母版自带默认 = 单实例三通道 PC13/14/15 取 pin_config.h 宏，多实例时生成器按计划覆写，见「多实例」行）**；**母版库浏览 UI（工单 master-library-ui/01-05）：列表每平台带 platform_label（PLATFORM_DISPLAY_NAMES 平台展示名单源）+ key_files（MASTER_KEY_FILES 白名单目录单源——stm32 = 模板 main.c / pin_config.h / led_instances.h / user/Project.uvprojx 四条、mspm0 = 模板 main.c / mspm0.syscfg / .cproject 三条，{path,label,size_bytes,exists} 列表一次算好不带内容）；GET /api/masters/{platform}/files/{path:path} 内容端点（白名单墙——清单外/穿越路径、平台不存在、白名单内文件缺失三态均 400 中文，UTF-8 errors=replace 读全文，零新错误类）；浏览 UI = 表格增强（平台展示名徽章 + 详情/删除双钮）+ 删除确认弹窗（.ref-files-overlay 遮罩，不可恢复警告，失败弹窗保留可重试）+ 详情弹窗（元数据 + 清单行点击加载 memo 三态，400 缓存网络可重试，重复打开零重复请求）**；**母版体检（工单 master-library-ui-2/01）**：/api/masters 每条带 health{ok,missing_key_files,config_file_ok,artifact_dirs} 与 stats{total_size_bytes,file_count,big_files(>256KB Top 10)}（一次算好不按条回查），表格健康徽章列（✓ 健康 / ⚠ 有缺失或残留 + 悬停明细）、详情元数据段统计行（formatSize 单源）；存在性/白名单/构建产物残留判定收敛 _disk_master_dir / _master_key_catalog / _top_level_artifact_dirs（与 analyze_structure 同口径，文案单源）；**文件树（工单 02）**：master_tree_files（统一噪音跳过全清单）+ read_master_tree_file（路径安全拒绝面与 entry_store.is_unsafe_path 对齐——`..` 任意层级 / 空段 / 首字符 `/` / 反斜杠 / 冒号（NTFS ADS），另有 resolve 后必须落平台目录内的兜底；NUL 二进制拒绝；超 TREE_FILE_MAX_PREVIEW_BYTES=1MB 拒绝；utf-8 errors=replace + 换行归一化），端点 GET /tree 与 /tree/{path:path} 与白名单 /files 两条路线并存；前端 buildMasterTree / masterTreeNodeHTML（原生 details/summary 零 JS 收起，目录在前码点序）+ masterTreeFileURL（与 masterFileURL 共享 _masterFileURL）；**预览增强（工单 03）**：fx/highlight.js languageOf/highlightXml/highlightText（C 分发复用 fx/code.js cHighlight 单源；先切 token 后逐段转义无注入面；超 HIGHLIGHT_MAX_BYTES=128KB（UTF-8 字节）回退纯文本），内容箱统一 masterContentHTML（复制按钮 + 高亮 + 加载三态，关键文件与树文件共用 memo key=platform/path，复制 = clipboard API → execCommand 回退 → toast 中文失败）；**免提炼导入（工单 04）**：import_master_direct（复用 import_master 全编排——结构校验/临时目录/原子替换/备份回滚/autocommit，sources=[源目录名]）经 POST /api/masters/import（{platform,project_dir}，与 scan 同风险面）+ 母版库卡「直接导入替换」（选文件夹整夹暂存复用 /api/masters/stage → 确认弹窗 → toast + 刷新；失败弹窗重开可重试）；**confirm 仓库级统一（工单 05）**：共享工厂 ui/confirm.js confirmModal（Promise，cancel=Esc/×/点遮罩 false，确认=true 或 extra 内 [data-confirm-value] 的 value；级联清理旧弹窗）+ fx/overlay.js overlayConfirmHTML 纯件，8 处原生 confirm 全迁（母版提炼确认/覆盖生成/修复回滚/修订回滚/模块删除/平台文件移除/参考删除/赛题删除），原生 alert 8 处归 toast，守卫 tests/js/confirm-guard.test.mjs 扫 static/js 全树（剥注释）零裸调用** | master.py（蒸馏编排）/ master_store.py（母版库 CRUD 与元数据） |
| 功能库 | 母版自带的底层库（stm32 母版 = 逐飞 ml_*：I2C/UART/PWM/GPIO/OLED 等，headfile.h 聚合），先于一切模块存在、生成时随母版进工程；不属于模块库；模块平台条目 files 空 = 实现内嵌母版（随母版进工程，不复制不注册） | masters/stm32/ml_libs |
| 提炼 | 导入多旧工程 → 对比 → AI 判定 → 报告（保留 / 整合 / 剔除 + 残留清单）→ 确认 → 入库；判定范围 = 公共 + 冲突 + 独有全部逐个判定，唯一判据：读内容判断是否通用、是否基础建设必需，不看重复次数 / 出现范围；确认是一条事务（confirm_distillation） | master.py + llm.py |
| 多实例 | 一次配置里选同一个「简单模块」多次，每个实例带显示名 / 变体 / 可选引脚（led 首例，beep/key/motor 留扩展口）：manifest 可选 `multi_instance` 块（max 上限 / variant 变体名，旧 manifest 缺 = 单实例）；实例清单 `{slug: [{name, variant, pin}]}` 随选择 / 生成请求走（旧请求不带 = 单默认实例）；**推荐链路 AI 猜实例（工单 06）**：模型按题面数量在命中模块条目输出 instances（{name, variant}，pin 归自动），ManifestSummary 摘要行带「多实例」标注作能力证据，build_module_selection 按同源能力清单校验（非多实例模块带 instances = 幻觉拒收；同名实例跨需求条目不齐 = 并集合并宁多勿少——题面拆分两条需求各提一部分灯，拒收会让 2021F+stm32 整次推荐卡死，多出的实例用户可在实例卡删除）、聚合进 ModuleSelection.instances、收敛键纳入实例清单、done 载荷带 instances 回填前端实例卡；`expand_instances` 展开为 (宏名, 默认脚) 计划（命名 LED_RED/YELLOW/GREEN / 重复内置色 _2 / 非内置色 LED_1..n；默认脚 stm32 红黄绿 PC13/14/15、mspm0 首实例 PA15，其余 board 顺序首个可用 io，同模块内去重）；渲染层按 slug 注册 hook（instance_render，照 patcher_registry 先例）产出 led_instances.h——stm32 工程根（与 pin_config.h 同级，多实例全通道具体 GPIO_x/Pin_y 对）/ mspm0 modules/led/code/（库内默认单通道 PA15；多实例时 mspm0.syscfg 通道 0 复用 LED_BEEP（计划脚 ≠ 现值改 $assign）、其余新 GPIO 实例 LED_<实例号>、关联 pin $name LED<实例号>——SysConfig pin 名全局唯一判例）；**默认兜底（工单 instance-default-fallback/01）：AI 没猜实例（题面无数量）且命中多实例模块 → done 载荷按平台默认清单自动填（stm32 红黄绿 3 实例 / mspm0 单实例，与「不配置 = 单默认实例」生成等价零回归，default_instance_plan 单源），实例卡直接可见可改；AI 猜了用 AI 结果**；单实例路径零写侧变化（pin_config.h / syscfg 逐字节不写）；骨架 / 冒烟接口块注入同一份通道宏清单（LLM 逐个 led_init(<通道宏>)，静态自检不误占位） | manifest.py（multi_instance / ManifestSummary 标注）/ selection.py（build_module_selection 猜实例解析 / expand_instances）/ instance_render.py（渲染注册表 + led hook）/ skeleton.build_skeleton_interfaces（注入）；module-multi-instance/01/02/03/06 |
| 上传暂存 | 「选择文件夹」上传（webkitdirectory 整夹上传）的落盘点：路径穿越拒绝（entry_store.is_unsafe_path 单源，空段 a//b 也拒绝）、目录名清洗（白名单 + 空回退 "upload"）、噪音跳过（.git 任意深度 + 构建产物目录，与扫描侧 iter_project_files 同一套）、单次上限 512MB、空清单报错——staged/ 目录在母版库同级、扫描后即用不自动清理；与 master.py 蒸馏预览的 mkdtemp 暂存（函数内自生自灭）区分命名 | stage.py（staged_root 推导 / stage_project_files）/ webapp.py（路由只收参数转调） |
| 条目库原语 | 模块库 / 赛题库 / 参考文件库共用的"目录即数据库"骨架：事务落盘、目录迭代、JSON 元数据读写与校验（read_json）、删除（delete_entry）、目录名 = 键的校验（validate_store_key）、必填字符串字段（require_str）、路径安全（is_unsafe_path）；不持业务形状，错误类型与文案归各库（StoreError 家族从不直达 web 层）；键文法 SLUG_PATTERN 单源（模块 slug 与母版平台名共用）；库的 StoreError 翻译骨架（3 分支映射 / require_str 包装 / key 校验包装）不参数化共享——各库语义差异真实（master 不存在特判 / manifest 合并分支 / topic 上下文文案），参数化劣于清晰重复；新库以 reference_library 为最新模板（错误类 + 元文件名 + 键文法 + 3 分支映射 + CRUD 形状） | entry_store.py |
| 库根 | 四库（模块库 modules / 母版库 masters / 赛题库 topics / 参考文件库 references）平级共居的根目录；随软件仓库走（仓库内 library/，git 版本化，ADR 0008）——库生命周期与软件一致、发布时随软件分发；config.json 只配 module_library_dir 与 masters_dir 两个键，topics / references 按约定跟随同级；**写库动作自动 git 提交**（工单 01 + 深化）：库 CRUD 落盘成功后自动 add + commit（变更限库根子树、不碰 src/），开关 config.json 的 autocommit_enabled 默认开，库根在 git 工作树外静默跳过；四库全部写函数全覆盖（结构测试强制分类注册表：commit / delegated / read 三类，新增公开函数漏挂即红）；批次级动作（confirm_topics / write_archive_entries）一次提交一批、不逐条提交 | config.py（布局推导 / 开关）/ autocommit.py（自动提交）/ master_store.py / library.py / topic_library.py / reference_library.py / archive.py |
| 参考文件库 | 与赛题库分开的独立素材区（存储与浏览入口都分开）：参考文件 = 不编译进生成工程的整包配套资料（套件例程 + 参考说明书）与提炼残渣，锚定赛题或套件；生成时 LLM 两级注入读取（先关联清单、需要时取全文）作学习素材——与模块相对（模块进工程，参考文件只被读）；区别于"可选配套"（那是模块的可裁剪组件，这是独立条目）；套件锚定的 kit 词表单源 = manifest.collect_kits（保序去重）；**套件锚定只跟选中模块走（修订 revise-deepen 前置排查：推荐阶段不再全库扫描套件词表——历史行为把全库套件参考注入任意赛题的推荐/骨架，如 2024H 里出现 UWB ALX 套件参考，UI 自动勾选误导且带偏 LLM；模块摘要行已带套件名，AI 无需靠全库套件参考发现模块）——推荐无选中集 = 只题面锚定 + 手动；骨架按 slugs 收选中模块的 kit（"选了这套件给配套例程"语义）**；**手动指定 = 追加准入（锚定命中 ∪ 手动选并集去重）＋ 全文直读强制，锚定两级照旧**（工单 01：reference_ids 经 manual_reference_admission 严格校验——幻觉 id / 重复大声失败，no-topic 形手动选是唯一准入）；**条目平台属性 + 生成平台过滤**（工单 01）：reference.json 带 platform（stm32 / mspm0 / any，缺省 any 向后兼容——旧条目无字段照旧注入），装配点 associated_references 按生成平台统一过滤 topic 与 kit 锚定命中（不匹配跳过、any 全进、platform 空串 = 不过滤），手动选不过平台过滤（用户显式意图，UI 标注平台 chip 让用户自判） | reference_library.py / webapp.py；两级注入装配在 generator.py（TopicContext）+ selection.py（清单段）+ reference_library.py（全文回读 read_fulltext，store 自持：路径安全 + 二进制跳过 + 标签单源）；手动准入在 selection.py（ManualReferenceError 已登记 errors.py）；平台过滤在 selection.py（_platform_matches） |
| 归档 | 提炼确认时的"归档为该题参考文件"动作：字节复制入库、锚定该题、内容自持（源工程删除不丢）；AI 判定不配归档的文件拒绝；归档批次与母版入库同事务；归档步骤在 archive.py（master 不 import 参考库族，防 import 链，函数级延迟导入） | archive.py / master.py / reference_library.py / report.py（ArchiveDecision）/ llm.py |
| PDF 资料库 | 素材根 sources/materials 下全量 PDF 的直通入口（tab-pdf；生成链不消费，纯浏览/取用素材）：批次 = 第一级目录、批次内子目录 = 二级目录（列表「目录」列）；列表字段 rel_path / name / batch / size_bytes / mtime（UNIX 秒）；页数按需 PyMuPDF 单文件（GET /api/pdfs/{rel_path}/pages，0 字节/损坏 → 400 中文，路由须注册在文件路由之前）；**数据健康语义：0 字节 = 损坏（行内 ⚠ 徽章 + 统计条红段，只警示不可删）、同名（大小写不敏感）+同大小+>0 字节 = 疑似重复（纯客户端判定，不读内容 hash）——重复组内可回收删除（用户裁决 r2：不真删，移入回收目录 sources/.trash-pdf/&lt;日期&gt;/&lt;rel_path 镜像&gt;，git 忽略可手动恢复；仅重复组成员可删，损坏/健康不可删；行内单删 + 详情组级「保留一份删其余」；确认弹窗含参考库镜像提示——materials 部分文件被 resolve_entry_file 引用，删除后条目文件 400 不自动解除）** | pdf_library.py（list_pdfs / page_count / trash_pdf）/ config.py（pdf_trash_dir）/ webapp.py（/api/pdfs 系列路由）；前端 index.html tab-pdf（pdfFilterEntries / pdfDupGroups / pdfHealth / pdfTrashConfirmHTML） |
| 入库路径 | 旧工程素材进库的两条路，不互斥——**模块化**（补录进模块库，生成时被推荐选中、编译进工程 = 模块复用）与**归档**（进参考文件库，LLM 只读学习 = 素材复用）；同一旧工程可双路径并存（历史遗留：car 1.1 只归档未补录 → mspm0 巡线题专用层缺口的根因）；**补录移植源判据：原生平台真机代码优先**（car xunji 真机编译过，先于 21F/26H stm32 → driverlib 跨平台移植，后者仅作对照） | add_module（library.py）/ write_archive_entries（reference_library.py） |
| 赛题库 | 与参考文件库分开的独立素材区：历年真题长 PDF 导入拆成的条目（年份 + 编号 + 题面），支持"几几年几题"编号解析为题面、作生成入口之一（贴题面或选历史题）；赛题条目锚定该题附带的完整程序（如 2026C 钥匙/锁两套）；历史赛题入口不再并入"题专用模块"（工单 module-universalization/07 已移除发现机制——普适化后无题专用模块，推荐链路 AI 按题面能力推荐承担）；**视觉图注（工单 topic-vision-notes/01-03 + vision-deepseek-native/01 + topic-vision-render/01-02）：拆条按视觉 key 配置对 PDF 嵌入图做视觉图注（[示意图N：…] 段，照 /api/extract 同款；视觉 key 留空 + DeepSeek 官方端点 = 复用主 key，零额外配置）；取题面时存量条目题面引用图（图N）但无图注且原 PDF 在库 → 自动补图注写回（enrich_topic_image_notes，幂等），**三级生成——渲染视觉优先（矢量图无栅格图对象，PyMuPDF 渲染页成位图 → DeepSeek 视觉模型描述 → [图N 标注：…] 段，2021F 图1 实测完整还原尺寸标注与红实线走向；无图页/正文页描述按长度与否定词过滤），视觉失败 → 文字标注兜底（矢量图标注文字在文本层，pdf_figure_annotations 按 visitor 坐标复合矩阵还原页面坐标 → 行聚类 → 图N 标题归属 → [图N 标注] 布局段，零额度、标注与线段位置关系保留），文字层也为空 → 内嵌栅格图视觉兜底**；任何失败降级原样——未配置（自定义端点无 key）与现状逐字节一致**；**浏览 UI 提升（工单 topic-library-ui/01-05）：列表端点每条带 health（original_pdf_missing / programs_missing / original_pdf_size——服务端文件系统实况判定，一次算好不按条回查；原 PDF 缺失 = 有声明但文件不在条目目录，程序悬空 = 引用路径非目录，与确认入库同判据）；PUT /api/topics/{key} 编辑端点（题面 / programs / hint_module_groups 三字段全量替换，校验全在落盘前，落地后自动 git 提交；year+number=目录名身份不可改、original_pdf/problem_md 属文件引用不可改；body 多余键忽略）；前端纯函数组（topicFilterEntries/topicSortEntries/topicStats/topicGroupVocabulary/topicDanglingGroups/topicHasNotes/topicHealthText/topicChipRowHTML/topicCardHTML/topicDetailHTML/topicEditHTML——extract 括号配平 + deps 注入单测）；功能组词表 = /api/modules 的 exclusive_group.id 客户端派生（hint 悬空判定，词表空降级不判定）；详情弹窗（元数据 + 题面全文 pre-wrap + 页图懒加载三态，memo 缓存——业务 400 缓存、网络/≥500 不缓存可重试）；编辑弹窗（题面多行 / programs 逐行 / 功能组勾选——词表外当前值兜底标注悬空，保存三态，成功后全量刷新）** | topic_library.py（拆条 LLM 协议与解析在 llm.py，确定性分块在 topic_library）/ webapp.py |
| 判定模型 | 提炼报告的判定条目与容器（FileDecision / DistillationReport）+ AI 判定素材（JudgmentFile / FileVersion）：形状 / 序列化 / 不变量（merge 必须带整合产物全文与说明；main_c_preview 与 uvprojx_preview 由平台重推导；版本分组不重不漏）唯一所有者；报告平台必须与工程平台一致（平台交叉校验）；扫描/对比模型（ProjectStructure / ProjectComparison）归模型层（report.py），master↔archive 依赖环消除；master 对 archive 保留函数级延迟导入（链约束非环，工单 C3） | report.py |
| 模板 main.c | 母版自带的最小系统板空 main（时钟初始化 + while(1) 空循环 + TODO 区），确定性模板、非 AI 生成；生成时被按赛题的"骨架"覆盖 | master.py |
| 整合 | 同路径多份内容不同时的动作：读多份 → 分析 → 产出通用版本（选一份是特例）；产物全文 + 说明进报告，用户审查后可改回选某份或剔除 | llm.py / master.py |
| 工程配置文件 | .uvprojx（stm32）：确定性渲染器现写（固定落位 user/Project.uvprojx，C8T6 设备块，文件树引用全部保留 .c/.s，IncludePath = 保留 .h 目录），移出 AI 判定（判例 09 治本）；.cproject/.project（mspm0）：确定性保留首份原样；条目不可改动作、进报告 keep 带规则原因；报告带 .uvprojx 全文预览 | keil.py（格式）/ master.py（编排）；渲染与预览经蒸馏适配器（守卫翻译归 MasterError） |
| 启动文件 | startup_stm32f10x_*.s 候选跨工程去重：至多保留一份（优先 _md，无则路径排序取第一份），落选规则剔除（Reset_Handler 重复定义风险）；密度守卫：保留份非 _md 大声失败（目标板 C8T6 中密度） | keil.py（格式）/ categories.py（去重生命周期）；谓词（is_startup_candidate / is_md_startup）经蒸馏适配器按平台取，mspm0 显式 False |
| 残留 | 构建产物（.o/.axf/.hex/.map/.lst/.crf/.dep/.lnp/.out/.elf/.htm）、备份（.bak 精确后缀，以及路径含 .bak 段的变体如 .bak2 / .bak_consolidate）、临时文件、IDE 用户选项（.uvoptx / .uvguix，编译时自动重建）：机器识别、确定性剔除，但进报告 exclude 清单并带规则化原因；构建输出目录（Debug/Release/Listings/Objects，后者任意层级）整目录忽略 | categories.py（规则）/ treewalk.py（目录忽略） |
| 项目树遍历 | "绕开噪音遍历工程目录"的唯一出处：iter_project_files（rglob + 统一跳过规则，绝对路径、排序确定性）+ skip_project_noise（顶层 .git / Debug / Release / Listings / Objects + Keil 输出目录任意层级）——母版扫描 / 旧工程扫描 / 生成摘要 / 语料构建六处消费，不再各走各的树（旧矛盾：Listings/ 下的 .uvprojx keil 找得到、master 忽略）；不持业务形状，类别判定归 categories.RuleCategory | treewalk.py |
| 二进制 | 非源码素材（文档 / 图片 / 模型 / 压缩包 / .exe 等）：文件头含 NUL 字节即判定，读全文会污染 LLM 判定素材，确定性剔除，但进报告 exclude 清单并带规则化原因 | categories.py |
| 文件类别 | 残留 / 旧 main.c / 基础设施 / 二进制四类的统一生命周期：识别（reason_of）→ 扫描分类 → 对比并集 → 报告汇编 → 越界拦截（AI 判定即报错）→ 处置校验；新增类别 = RULE_CATEGORIES 加一条 + 结构/对比字段声明 | categories.py（RuleCategory / RULE_CATEGORIES / classify，唯一出处） |
| 骨架 | AI 生成的 main.c（初始化序列 + TODO 预留区）+ 静态自检（幻觉调用改注释占位）；`main_mode` 由 webapp 透传（skeleton 缺省） | skeleton.generate_skeleton |
| 自检冒烟 | main.c 的只自检模式（工单 skeleton-smoke-refs/01）：初始化每个所选模块 → 读一次/动一次 → 打印结果；OLED 为主 / debug_uart 串口为辅；两者都没选 = 400 中文；与骨架同款 `sanitize_skeleton` 兜底；不写赛题逻辑 | skeleton.generate_smoke_main / llm.generate_smoke_main / webapp `/api/skeleton` `main_mode: smoke` |
| C 词法层 | C 源码文本的机械切分唯一出处：围栏剥离 / 行号检测、注释剥离（keep_preprocessor 轴：# 行透传与否）、引号 include 提取、顶层 #define 扫描、语句级切分原语（iter_c_regions 区域迭代 / match_bracket 括号配对 / next_significant 空白注释跳读，骨架替换走查与死循环检测的消费基座）；接口 = 字符串进 / 字符串出，不碰盘上文件；不做调用形态识别（那是骨架自检的语义判断） | clex.py |
| 校验语料 | 生成前六道门禁共吃的内存语料：模块文件（文本 / 类别 / 所在目录）+ 母版头 + 母版搜索目录 + main.c，一次读盘；门禁退化为吃语料的纯谓词（可内存直构测试），不各自读盘；门禁装配唯一出处 = generator.GENERATION_GATES 表 + run_generation_gates（照 categories.RULE_CATEGORIES 先例）：顺序 / 输入依赖 / 全貌收进表，新增门禁 = 表加一条 + 谓词；产物树侧可重建（build_output_tree_corpus，真机验收脚本与门禁同源跑同一套谓词） | generator.py（ModuleCorpus / ModuleFile / build_module_corpus / build_output_tree_corpus / GENERATION_GATES） |
| 模块摘要 | 模块库摘要对象（喂 LLM 的可用模块清单）：slug / description / kits（collect_kits 单源）/ 依赖 / multi_instance（多实例能力，缺省 None；工单 06 起随摘要走——推荐猜实例数的能力证据，解析校验与清单标注同源）；行渲染唯一实现 = to_line()（字符串只在 prompt 边界渲染一次，无反向解析方，有 multi_instance 才显示「多实例」段）；known_slugs 取 slug 字段 | manifest.py（ManifestSummary / build_manifest_summaries 批量投影） |
| 修改器 | 平台工程文件适配器：Keil 改 .uvprojx、CCS 改 .cproject；各自是格式读 + 写的唯一所有者；XML 解析 / 写回 / 头部回注与 include 查找器（find_project_file，孪生查找器收敛）/ 条目解析核心（resolve_include_entries：绝对保留 / 相对基准 / 去重保序 / resolve）共享 projectfile.py 底座，宏策略归平台模块（ccs 展开 ${PROJECT_LOC} / ${PROJECT_ROOT} 并跳过 SDK 宏与 ${ 残留，keil 无宏）。ccs.py 双格式认知：同时认 CCS classic 与 Theia 20.5（TMS470_TICLANG 4.0）——差异在 cdtBuildSystem 的 storageModule 位置（classic 在 settings 内、Theia 独立）、include/define 选项 superClass 命名空间（ti.ccs.misc.* / com.ti.ccstudio.buildDefinitions.TMS470_TICLANG_4.0.compilerID.*）与位置（classic 直接子元素、Theia 在编译器 tool 内），单实现路径通吃；include 值展开 ${PROJECT_LOC} / ${PROJECT_ROOT}，SDK 环境宏（${COM_TI_MSPM0_SDK_*}）跳过不猜。include 读侧接缝：patchers.include_search_dirs 按平台分派——stm32 走 keil 版 .uvprojx IncludePath、mspm0 走 ccs 版 .cproject buildIncludePath | keil.py / ccs.py / patchers.py / projectfile.py |
| 平台警告 | missing / unverified / hardware_bound，生成前暴露 | selection.py |
| 功能需求层 | 推荐输出的第一层：题面驱动的能力/外设级需求清单（声光提示 → LED/蜂鸣器、识别数字 → 视觉），粒度贴题面关键词；每条必须挂题面对应句（逐句对照），禁止题外联想——"送药小车所以需要视觉"是脑补，题面要求识别数字才需要视觉 | selection.py |
| 逐句对照 | 收敛循环的机械防漏机制：题面按句编号，每句对应功能需求或"无功能"；找不到对应句的功能 = 脑补，删 | selection.py |
| 实现覆盖检查 | 功能需求逐条查库内模块能否实现：命中 → 库内模块（可勾选进工程）；无命中 → 库外建议 | selection.py |
| 库外建议 | 无库内实现的功能的外设推荐：类别名（视觉模块）+ 常识举例（K230 / OpenMV），仅展示、不进工程、不参与生成；name 只允许硬件词表条目（型号或类别），词表外型号降级为类别或拒收，examples 自由发挥 | selection.py |
| 硬件词表 | 电赛常见硬件名两条目型（类别 / 具体型号），进选模块 prompt 作科普素材、作库外建议 name 的校验源，可手补 | selection.py |
| 收敛循环 | 推荐分析以题面为依据反复自检修订（删脑补 / 补遗漏 / 重查覆盖），连续两轮功能需求层一致即收敛（上限 4 轮）；拿不准暂停向用户补问（澄清阶段先行：补问在收敛前完成，回答后不重跑已跑轮次）；澄清问答历史贯穿收敛循环（select_modules 每轮带 clarifications 独立段，题面编号不动）；select_modules / clarify 走 _retry_parse 整次重试兜底（瞬时空内容/畸形输出自动重问，最多 3 轮）；**赛题答疑 Q&A（工单 qa-material/01）**：赛事组对题面的澄清问答随请求带 qa_text，题面后独立段注入（权威材料，原文直引、不并入题面、不干扰收敛判定），缓存含 qa_sha256 指纹（Q&A 变化缓存失效重推，旧缓存无字段兼容）；**推荐缓存库指纹（工单 recommend-cache-fingerprint/01）**：library_sha256 = 装配点 ManifestSummary 摘要行（to_line）排序 hash——库变（模块增删/简介/能力/多实例标注）缓存失效走真实推荐，旧缓存无字段保守失效（宁可重推）；收敛解可缓存进赛题条目（v2，库内命中每次现算，库会变） | selection.py / webapp.py / llm.py |
| 错误映射 | 路由异常的唯一出口：error_to_http 表把核心异常转 HTTP 状态与中文 message（业务 400 / LLM 502 / 文件系统 400）；**未登记异常 = 真 bug → 500 大声失败**，不吞成业务 400 | errors.py（error_entry 单表，webapp 只取值 / 包装；结构测试反射包内全部异常类防漏登） |
| 进度事件 | 后端经 SSE 推送的实时进展（提炼 = 阶段 / 批次 / 补问轮，推荐 = 收敛轮次 round / converged；编译修复 = parse_done / fix_start / apply_result；LLM telemetry = llm_telemetry 脱敏观测快照，展示调用数 / provider split / 最新 operation / retry-error 状态 / bytes / duration / usage；最后一个事件携带完整结果）；模型单次调用期间通常不产生业务进度事件，存活证明 = 客户端每秒跳动的计时器；接入 collector 的 SSE 流可在单次调用完成记录后旁路发 llm_telemetry，不改变 done/error/question 终态；已完成工作流另入内存 ring buffer（LLMRecentWorkflowStore，maxlen 20，不落盘），设置页经只读 GET /api/llm-workflows/recent 展示摘要 + 每次调用明细（token 标注服务商上报、不估算费用） | llm.py / selection.py（进度发射）/ llm_telemetry.py（collector → telemetry 事件适配）/ llm_recent_workflows.py（最近工作流 ring buffer + 只读端点）/ events.py（事件词表唯一出处，含终态）/ sse.py（线格式与流化运行器） |
| 板定义 | 一块开发板的数据：引脚（丝印名 / 板图坐标 / 类型 / 能力集）+ 固定资源标注；后端 boards/*.json 单源（生成门禁与前端渲染同吃），/api/boards 提供；stm32 = 最小系统板（蓝药丸），mspm0 = 地猛星；能力集口径 stm32 = ml_libs 支持表（v1 受限）、mspm0 = 芯片复用能力（任意）（ADR 0010） | boards.py（已实现，工单 pin-board-config/01） |
| 引脚角色 | 模块 manifest pins 字段声明的引脚需求条目（id / 类型 / 标签 / 默认引脚 / 必选）；标签 = 模块_用途（如 TB6612_STBY）；配置菜单项 = 已选模块的未绑角色——配置词汇用模块引脚角色而非芯片 AF 复用（ADR 0010） | manifest.py pins（已实现，工单 pin-board-config/01） |
| 角色类型 | 引脚角色的能力类别词表：gpio_out / gpio_in / uart_tx / uart_rx / pwm / enc / adc / i2c_scl / i2c_sda / spi_* / exti；能力 token 可带实例（如 uart_tx:UART_1、pwm:TIM2_CH1） | boards.py（已实现，工单 pin-board-config/01） |
| 引脚绑定 | 角色→板引脚 的分配：随生成请求走（v1 不持久化），未绑 = 用声明默认值（默认布线，生成即编译不破）；生成器按绑定覆写 pin_config.h（stm32）/ 只改 .syscfg 的 $assign 行（mspm0，实例名与宏名不动）；同引脚多角色允许（共享，如 xunji/huidu 共传感器，门禁不拦、前端叠加标注）（ADR 0010）；**自动配置（工单 pin-auto-assign/01）**：`/api/bindings/auto` + `auto_assign_bindings` 确定性求解——逐角色先试原值（合法保留）、非法换脚（跳过已占用，倾向不制造新共享），只动真冲突角色（resolve_bindings 校验面：能力 / UART 成对 / mspm0 槽位互斥 / 类型级实例），合法共享保留并标注（shared 组 + I2C 总线说明），无解中文报错 | generator 写侧 / pin_bindings.py（校验 + auto_assign）/ pinwriter.py（已实现，工单 pin-board-config/02） |
| 类型级能力校验 | 角色绑定校验的两种口径之一：只查引脚能力集含该角色**类型**（任意实例 token 即合法），实例由**绑定引脚**推导并喂渲染器写宏（代码吃宏，换实例零库改动）——stm32 pwm / enc / uart 三类已类型级（ADR 0011/0012），mspm0 分级开放（同族迁移类型级、跨族迁移随 pin-family 渲染，ADR 0012）；**adc 双平台类型级（b1-adc-servo/01：换引脚 = 换通道，stm32 宏值 ADC_Channel_N 随引脚、mspm0 改 adcPinN.$assign + adcMem<N>chansel 通道行）**；对照 strict-all 实例校验（默认引脚的**全部**实例都必须命中）——未类型级化的类型保持（spi/adc 等无消费方驱动）。分级 = 平台×类型×族 | pin_bindings.py resolve_bindings + index.html pinCanHost（工单 pin-unlock-stm32/01、pin-full-unlock/01/02/03/04、b1-adc-servo/01） |
| 定时器实例冲突 | pwm 绑定实例与骨架调度/滴答定时器同 TIM 外设（26H 骨架 TIM3 10ms 调度、2026C 骨架 TIM2 1ms 滴答）→ 运行期 ARR/PSC 互覆，编译绿运行坏；门禁扫 main_c 的 tim_interrupt_ms_init 实例与绑定 pwm 实例比对，冲突 400（只查用户绑定，默认组合冲突不拦——现状性质；识别不到不拦，漏报优于误报）（ADR 0011） | generator.py 门禁 _check_timer_instance_conflicts（工单 pin-unlock-stm32/01） |
| 共享端口宏 | 多个角色的 macros 共享同一 _GPIO/_PORT 尾形宏（I2C_GPIO 为 MPU6050 SCL/SDA 共用、LED_PORT 为三灯共用）——改一个角色会改到同族其它角色的共享宏；绑定层面：两条改动绑定写同一共享宏且**值不同** = 坏代码 → 渲染层 400；未改同族角色的隐式漂移仍为提示语义（前端卡片已做）（ADR 0011） | pinwriter.py 渲染层校验（工单 pin-unlock-stm32/02） |
| EXTI 线冲突与共线组 | stm32 EXTI 线号 = 脚号 mod 16：两绑定 enc/exti 角色**异口同线**（PA2/PB2）互斥 → 400（同脚共享仍提示语义）；线 5-9 共 EXTI9_5_IRQHandler、10-15 共 EXTI15_10_IRQHandler——motor 模块 7 条件 handler 按 _LINE 宏预处理器选择、共线组内查 PR 位分派 A/B 编码器（ADR 0012） | generator.py 门禁 _check_exti_line_conflicts + motor_stm32.c 条件 handler + ml_exti.c 枚举 48 项（工单 pin-full-unlock/01） |
| UART 实例仲裁 | UART 角色绑定 = 类型级：TX/RX 对同实例（两脚 token 实例集交集非空，空 = 400 成对绑定）；实例冲突门禁——绑定推导实例 × 未绑定角色默认实例 → 400，绑定×绑定同实例放行（共享提示语义，换位合法），默认×默认不查（UWB/DIGIT/COORD 共 UART_1 现状合法，与 TIM 门禁同"只查用户绑定"口径）；fputc 跟随 DEBUG_UART（printf 流 = 调试串口，默认 USART2/PA2）；母版静态 isr.c + pin_config.h 渲染 USARTx_IRQ_CALLS 聚合宏（按绑定实例分组各模块 rx_handler 调用，__weak 空兜底——纯静态 isr.c 被默认共享实例的重复定义炸掉，故聚合入渲染面）（ADR 0012） | pin_bindings.py + generator 门禁 _check_uart_instance_conflicts + ml_uart.c（uart_pin_init_ex / fputc 宏化）+ isr.c（工单 pin-full-unlock/02） |
| mspm0 实例迁移分级 | **同族迁移**（UART0↔UART1、I2C0↔I2C1、TIMGx↔TIMGy、GPIO 组换端口）：改写器改 peripheral/port 字段，实例名不动 → SysConfig 生成宏名不动 → 模块代码零改动（step_motor 四脚同端口锁借此解）；**跨族迁移**（TIMG↔TIMA）：API 函数族变 → 模块 #if 双分支 + 生成器渲染族标志头 pin_family.h；step_motor 跨族物理不可能（32 位需求，TIMA 16 位）；PWMAB 两通道同实例门禁；mspm0 同脚撞车由 SysConfig Resource conflict 自然拦，不另设门（ADR 0012） | syscfg_model.py 文件模型（syscfg 改写器，见「syscfg 文件模型」）+ motor.c 双分支 + generator 渲染 pin_family.h（工单 pin-full-unlock/03/04） |
| syscfg 文件模型 | mspm0.syscfg 的「文件格式知识」单源（架构评审 ② 落地）：独占文法（实例声明 addInstance / 模块声明 addModule / `$assign` 赋值）**一次解析**为结构化模型（lines + instances + assigns）；**prune**（按选中模块裁剪未选实例）与 **rewrite**（按绑定改写 `$assign` 引脚值 + uart/i2c/pwm 的 peripheral 行）是对同一解析的两个操作、`to_text` 唯一 serialize 出口——生成写侧走单一 pipeline（parse → prune → rewrite），先后由构造保证、不再靠注释/调用顺序；槽位身份原语 syscfg_path_matches（binding → syscfg 实例/路径）校验侧与写侧共用；MSPM0_SYSCFG_FILENAME 文件名常量单源在此；SyscfgModelError = 母版/数据漂移防御（白名单，pinwriter 翻译回 PinBindingError 保持旧错误契约） | syscfg_model.py（parse_syscfg / SyscfgModel / syscfg_path_matches / MSPM0_SYSCFG_FILENAME）+ pinwriter.apply_pin_bindings 单一 pipeline 挂钩 + syscfg_prune.prune_syscfg 薄委托（工单 syscfg-file-model/01-04） |
| Python 副产物 | 模块 manifest 的模块级声明（python_artifact：template → output，缺省不落键逐字节兼容）：选中模块除主控 C 代码外，生成器额外渲染并写出 `.py` 到工程根（K230 侧 CanMV main.py，SD 卡根开机自启动）；**多模板（工单 k230-multi-template/01-04）**：python_artifact 支持两种形状——旧 `{template, output}`（单模板 id=default，序列化逐字节不变）与新 `{templates: [{id,name,description,template,output}], default}`（default 必须在列表）；生成请求可选 python_templates（{slug: id}，域层校验非法 slug/id 400，缺省 = default），渲染按选中模板读盘；ManifestSummary 行标注「副产物模板可选」；k230 首批两模板 = blob 色块追踪（default）/ rect 矩形识别（find_rects → corners 外接框 → B 帧，主控零改动）；模板级依赖替换（digit → digit_uart）留待 AI 数字识别第二批。未选任何带声明模块 = 产物与现在逐字节一致。**帧契约单源**（K230 发送 ↔ 主控解析同吃）：COORD_FRAME_FIELDS / COORD_FRAME_FORMAT（B,<cx>,<cy>,<confidence>,<x1>,<y1>,<x2>,<y2> 由字段序派生）/ NO_DETECT_FRAME（N）/ UART_BAUDRATE（115200）只定义在 k230_render.py——模板占位符渲染（{{coord_frame_format}} 等，replace 不解释花括号）与主控 coord_detect parse_coord_line 两侧同源，防漂移测试从双平台 C 源机械提取字段序比对锁定；**k230 模块 = 首个纯副产物模块**（双平台 files 空 = 主控侧无自有 C 文件、依赖 coord_detect = 生成级契约依赖而非 include 依赖——零 C 文件模块豁免死依赖检查、且不重复声明串口 pins） | manifest.py（PythonArtifactSpec / PythonArtifactTemplate）/ k230_render.py / generator.py `_write_python_artifacts` + `resolve_python_template_choices`（工单 k230-vision-copilot/01-03、k230-multi-template/01-04） |
| 修订 | 生成后阶段（工单 revise-deepen/01-05）：补入新赛题答疑 Q&A → AI 逐条影响分析（影响哪些功能需求 → 建议模块增/删/不变 + 理由）→ 确定性 diff（新旧模块集集合差，纯函数，AI 不算差异）→ 用户确认 → 覆盖式重生成进同一输出目录（整树备份到输出目录外可回滚，复用既有生成全链路）；模块集不变 → 不重生成、main.c 原样保留（手工编辑不丢）；两段式 API（先分析返回待确认结果，用户确认后再执行） | impact.py（影响分析 + diff）/ revision.py（执行 + 备份回滚）/ webapp.py `/api/revise/analyze` + `/api/revise/apply` + `/api/revise/rollback`；决策 ADR 0014 |
| 深化 | 生成后阶段：AI 按功能需求清单逐条填充 main.c 的 TODO 预留区（输出与需求清单对应、不做题外发挥、保留手工编辑）→ 写盘前备份可回滚 → 强制编译验证闭环（工具链探测复用 → 编译 → 失败自动修复一轮 → 重编译 → 绿 = 已验证；无工具链 = 大声降级，结果保留、状态 = 未验证；修一轮仍红 = failed）；不设自动循环，用户可重复触发；**主入口已统一为逐步深化（任务推进 + 每步步骤报告，见下）——本行一次性调用保留为快速兜底**（不逐步执行、无步骤报告） | deepen.py（run_deepen）/ llm.deepen_main_c / webapp.py `/api/revise/deepen`；决策 ADR 0014 |
| 任务推进 | 生成后阶段（工单 task-progress/01-03，UI 入口文案「逐步深化（任务推进）」）：AI 把题面 + 功能需求 + 评分点 + 模块接口 + 当前 main.c 拆成**有序任务清单**（每任务 = 一段可独立实现并编译验证的 main.c 增量）→ 学生逐卡点「做这一步」→ 一次 LLM 调用产出实现后的 main.c 全文 → 整树备份 → 写盘 → 编译验证闭环（**与深化共用 verify_compile_tail 尾段 + main_diff 公共函数，工单 02 抽取**）→ 每步编译验证后 **步骤报告**（工单 stepwise-deepen/01-02：LLM `report_task_step` 产出 {what_changed, user_action}——「做了什么 + 接下来你要做什么（含接线/上板指引）」，随轮次落盘）；**任务洞察**（工单 task-insight/01-02：拆解时 AI 给每任务标 `resources`（互斥资源：引脚/外设/中断真实名，落盘清单）→ 顶部**资源总览**（`resourcesOverviewHTML`：资源 × 任务聚合表，同一资源 ≥2 任务标黄「⚠ 多任务使用，上板前确认」——联调冲突暗雷；任务卡资源徽标 `taskResourcesHTML`）；步骤报告升级 **上板自检清单**（`StepReport.checklist` 3-6 条原子勾选项「应观察到什么；若不正常检查哪里」→ 随轮次落盘 `TaskIteration.checklist`；结果面板 + **任务卡常驻**渲染——`taskChecklistHTML`/`checklistStateKey`，卡常驻 = 刷新回显路径（结果面板是执行时一次性快照）；勾选态 localStorage 备忘 `firstep.checklist.v1.<taskId>/<seq>`，不落盘不动状态机））；**编译错误跳转**（工单 error-jump-task/01-02：compile 摘要带 `parsed_errors`——与 compile_runner 同型同源，walk verify_compile_tail 的路径全带；任务结果面板 failed 时逐条列出「文件:行号 消息」（`taskErrorsHTML`），main.c 行点击跳转 main.c 预览卡选中高亮 = fx/code.js `maincJumpToLine`（迁自修复中心单源化，返回错误码、toast 由调用方做——修复中心与任务面板共用，非 main.c 错误不可点））→ 状态徽章实时更新 + **顶部进度总览**（分段条：已验证/进行中/待上板/失败/待做/已跳过，`tasksOverviewHTML`；卡上「下一步要做」粘性摘要 `taskNextActionHTML`，直到上板确认）→ **每卡常驻「烧录到板子」按钮**（工单 flash-step-button/01：已实现过的步骤——执行过至少一轮或已终态，显示条件 = taskCanFeedback 同判据；pending/skipped/doing 不显示；卡内独立状态/结果容器 uid = task.id，与执行结果面板烧录行并存）；**做完一步自动引导下一步**（工单 step-next-guide/01：执行/反馈轮 done 后，当前卡内显示「下一步 → tN：标题」，下一张待执行卡——清单顺序上当前卡之后第一个 pending/failed——平滑滚动到视口中央并描边高亮 2.4s；纯前端瞬态不落盘，无剩余待执行不提示；「做这一步」仍为单卡执行、不自动连做）；**逐任务手动点，不做 AI 自动连做**；清单落盘工程根 `.contest_tasks.json`（仿 `.contest_context.json` 纯新增隐藏文件，version 向后兼容读）；**任务状态机（工单 03，ALLOWED_STATUS_TRANSITIONS 单源）**：pending → doing → verified / unverified / failed（doing / 终态由 run_task 回填，不经人工转移表），人工改标 = pending→skipped(跳过)、skipped→pending(恢复)、verified/unverified/failed→pending(重做)、unverified/failed→verified(上板人工确认)；**verify=manual 的任务编译绿初始仍为 unverified**（编译绿只证语法链接，验收 = 上板观察，学生改标 verified）；`force` 重新拆解 = 旧清单备档 `.contest_tasks.json.bak` 再覆盖；修订执行（模块集变化重生成，rmtree 清整树）后清单作废 = 重生成前记录存在性标记（tasks_invalidated），前端提示重新拆解；**想法套件（全局商量 / 清单微编辑 / 调序 / 草稿箱）见「想法套件」行** | task_progress.py（Task / TaskPlan / build_task_plan / run_task_planning / run_task / update_task_status / find_task / check_plan_replaceable / _report_task_step）+ llm.plan_tasks / llm.execute_task / llm.report_task_step（StepReport）+ deepen.verify_compile_tail（run_deepen / run_task 共用）+ revision.tasks_invalidated + events.py EVENT_TASK_REPORTING + webapp.py `/api/tasks/plan` / `/api/tasks/execute` / `/api/tasks/status` / `/api/tasks/plan-read` + fx/task.js（tasksOverviewHTML / taskStepReportHTML / taskNextActionHTML / taskResourcesHTML / resourcesOverviewHTML / taskChecklistHTML / checklistStateKey / taskErrorsHTML）+ ui/generate-tasks.js；**效果 diff 展示 = fx/diff.js（mainDiffHTML / diffStatsLineHTML，主题化行级视图——gutter 符号列 + 新增绿/删除红/上下文灰，走 CSS 变量明暗自适应；深化与任务结果面板共用，工单 diff-restyle/01 从 ui/generate-revise.js 迁入）；**⚙️ 参数速调卡（独立于任务清单，工单 param-tune/01-02）见「参数速调」行**；**🚀 交付卡（打开工程 / 交付检查 / 一键打包，工单 delivery-suite/01-02）见「交付」行**；**僵尸 doing 恢复**（工单 stuck-doing-recover/01：`ALLOWED_STATUS_TRANSITIONS` 的 doing → pending——执行中断（服务重启 / 断电 / 进程被杀）后磁盘残留 doing 的僵尸卡有唯一人工出口（doing → 其余状态仍不可，无证据的跳过 / 验证 / 失败不做）；webapp 模块级 `_running_task_execs: set[str]` 进程内执行注册表——tasks_execute 的 run() 闭包 add / finally discard，tasks_status 见占用记录即 400「任务正在执行中，无法恢复」（防真实执行中被人工恢复后并发双写；进程死 = 注册表空 = 恢复安全）；前端 `taskCardActions(status, opts.recoverable)` doing → ["recover"] + `btn-task-recover`「已中断？恢复此步」（仅 doing && !tasks.busy 显示 = 无活跃执行才给恢复入口，真实执行中的拦截由后端注册表兜底）） |
| 任务卡 | 任务清单里一个任务的展示与操作单元：标题 / 描述 / 关联评分点（分值标注）/ 前置依赖 / 验收方式（compile 编译绿即验证 / manual 需上板人工确认）/ 状态徽章；卡上「做这一步」+ 可留空补充框（向 AI 补一句说明）+ 跳过 / 重做 / 人工改标 / 回滚 | fx/task.js（taskCardHTML / taskStatusLabel）+ ui/generate-tasks.js + task_progress.py |
| 上板反馈 | 任务执行的迭代闭环（工单 task-feedback/01-03）：任务卡「上板反馈」按钮（显隐 fx `taskCanFeedback`：非 doing 且已有迭代记录或已终态）→ 用户描述上板实测现象（如「左轮不转 / 不沿线向右偏」）→ `run_task(feedback=...)` 反馈轮——**终态任务先自动重开**（verified/unverified/failed → pending 均已在转移表；主任务自动重开时机与失败语义：`previous_status` 必须在重开前捕获，否则失败恢复错位为 pending）；`execute_task` 协议加 feedback 参数（prompt 约束「按反馈修复，只改相关部分，不重写无关代码」，feedback 段在 note 后，空=整段不出现逐字节兼容）；**迭代记录 TaskIteration**（seq/kind=execute|feedback/feedback 原文/status/backup_id/compile_summary/at）随任务落盘，轮次历史区每轮 = 时间 + 编译结果徽章 + 状态 +「回到这轮之前」（撤销语义 = 恢复该轮执行前快照 + 状态回该轮前终态，端点 POST /api/tasks/rollback-iteration，复用 revision.restore_revision 同一备份族）；**不做强制上板门禁**（用户拍板：compile 类不设门禁，全部任务都有反馈通道）；结果面板「上板反馈」原文回溯（fx `taskLatestFeedbackNote` 单源） | task_progress.py（TaskIteration / _parse_iterations / rollback_task_iteration / run_task feedback 分支）+ llm.execute_task + webapp.py /api/tasks/execute（feedback 校验）+ /api/tasks/rollback-iteration + fx/task.js（taskCanFeedback / taskIterationLabel / taskIterationsHTML / taskLatestFeedbackNote）+ ui/generate-tasks.js |
| 任务商量 | 每卡「和 AI 商量」的多轮对话区（工单 task-chat/02）：用户对某个实现任务发表想法/纠正 → AI 任务顾问回应（**先判可行性 → 再说对任务实现的影响 → 给修正建议**；「做这一步」才动手改代码）——LLM 新协议 `discuss_task(task, problem_text, qa_text, requirements, module_interfaces, main_c, history)` → `TaskDiscussion{reply}`（无 review 结构——讨论是沟通不是选型，回应结构由系统提示词引导）；一轮一请求（同步端点 POST /api/tasks/discuss：message + history（旧→新全量，前端积累）校验 400、服务端读 .contest_context.json 装配（题面/Q&A/需求/接口/main.c 现读）、TaskError→400/LLMError→502、telemetry EVENT_TASK_DISCUSS + recent_llm_workflows 收尾）；对话历史**会话级**（前端 Map 不落盘，刷新丢）；观点不落结构化字段 | llm.py（TaskDiscussion / TASK_DISCUSS_SYSTEM_PROMPT / _task_discuss_user_prompt（历史段复用 _discuss_history_segment 字符帽，需求行 _requirement_lines））+ webapp.py /api/tasks/discuss + events.py EVENT_TASK_DISCUSS + fx/task.js（taskDialogButtonHTML / taskDialogAreaHTML）+ ui/generate-tasks.js（taskDialogs Map） |
| 对话结论 | 采纳进执行的任务沟通结论（工单 task-chat/01）：用户对某条 AI 回复点「采纳这条结论」→ 回复全文写入任务级 `Task.dialog_note`（落盘 `.contest_tasks.json`，刷新不丢；text 空串 = 取消采纳，端点 POST /api/tasks/dialog-adopt → {"task","plan"}）；执行注入 = `_task_execute_user_prompt` 读 task dict 的 dialog_note → 独立段【用户沟通结论（采纳自对话，按此修正实现）】，位置 = note 段后、feedback 段前（对话语义晚于预填说明、早于烧录反馈），空 = 无段逐字节兼容（**execute_task 协议签名零变更**——dialog_note 随 task.to_dict() 走）；卡上「已采纳对话结论」徽标 + 摘要（截 30 字）+ 取消采纳按钮；与 note/feedback 三通道并存、语义两两互补 | task_progress.py（Task.dialog_note / set_task_dialog_note / _with_task_status dialog_note 保留）+ llm.py（_task_execute_user_prompt dialog_note 段）+ webapp.py /api/tasks/dialog-adopt + fx/task.js（taskDialogAdoptHTML） |
| 任务序号 | 任务卡的展示序号（工单 task-chat/03）：「第 N 步（建议顺序）」——N = 清单内序号（index+1 纯展示层换算，id/depends_on 零改动）；AI 拆解输出顺序 = 建议执行顺序（方便做的顺）；**不强制**（depends_on 只作展示与排序用途，后端无强制闸，网格顶部声明「建议按序号从上往下做——不强制，可跳着做」）；手动调序 = 任务卡上 ↑/↓ 按钮（工单 idea-suite/03-04：`move_task` 相邻换位、id 不变、依赖引用稳定）；不做拖拽（用户拍板） | fx/task.js（taskOrderLabel / taskCardHTML 头部序号 / tasksGridHTML 声明行） |
| 灵活修正 | 任务推进区顶部的全局「新想法 / 问题」入口（工单 idea-fix/01-02，不绑定任务卡）：用户随时输入想法/问题 → AI 分析并分类 `IdeaAnalysis{kind: new_task|direct_fix|discussion, reply, new_task, fix_summary, affected_task_ids}`——new_task（新功能需求 → `insert_task_from_idea` 生成任务卡追加清单 t{n+1}，depends_on 序号转 id、校验失败不动盘面）；direct_fix（改现有代码 → `run_direct_fix`：备份 → `apply_idea_fix` → 写盘 → 编译验证 → 步骤报告，返回 {status, backup_id, compile, main_diff, message, step_report}，**不造任务轮次**，回滚复用 /api/revise/rollback，事件复用 compile_start/fix_start/verify_result/task_reporting 词表）；discussion（先讨论——漏斗态：把 AI 建议文本重新分析，结果与目标一致时自动落地）；**needs_redo 联动**：修正落地后受影响任务 `Task.needs_redo=true` 落盘（旧清单缺省 false），任务卡「⚠ 建议重做」徽章 +「重做此步」（重置 pending + 清标记，可再走「做这一步」）；执行过 needs_redo 任务自动清标；端点 = POST /api/tasks/idea/analyze（SSE idea_analyzing→idea_result→done）/ insert（同步，**校验通过后才 .bak 备档**——backup 是 move 语义，先备后验会吞现有清单，评审整改）/ fix（SSE）/ mark-redo（同步，needs_redo 可选布尔）；前端 = fx/task.js（ideaResultHTML / taskNeedsRedoBadge）+ ui/generate-tasks.js（ideaState / tasksIdeaAnalyze / tasksIdeaInsert / tasksIdeaFix / tasksIdeaConvert / tasksRedoTask / resetIdeaArea）+ index.html（想法输入区 + .task-redo-badge）；范围外：全局结论注入生成/骨架/深化、拖拽调序、草稿批量进度持久化、自动连做（全局商量 / 清单微编辑 / 草稿箱已由「想法套件」行承接，工单 idea-suite/01-06） | llm.py（IdeaAnalysis / IDEA_KINDS / TASK_IDEA_SYSTEM_PROMPT / IDEAFIX_SYSTEM_PROMPT / _idea_user_prompt / _idea_fix_user_prompt / _idea_plan_summary / analyze_idea / apply_idea_fix + RoutingLLM passthrough）+ task_progress.py（Task.needs_redo / insert_task_from_idea / set_tasks_needs_redo / run_direct_fix / _report_task_step 复用）+ events.py（EVENT_IDEA_ANALYZING / EVENT_IDEA_RESULT）+ webapp.py 4 路由 + fx/task.js + ui/generate-tasks.js |
| 想法套件 | 任务推进区的三个延伸能力（工单 idea-suite/01-06，均不绑任务卡）——**① 全局商量**：任务卡「和 AI 商量」以外的工程级多轮对话区（LLM 新协议 `discuss_global_idea` → `TaskDiscussion{reply}`，工程总顾问视角：题面/需求/清单现状/接口/main.c/全局结论一致性，不改代码只讨论；一轮一请求同步端点 POST /api/tasks/idea/chat/send，历史落盘 `.contest_idea_chat.json`（IdeaChat{version,generated_at,messages,note}，读侧条目级容错、坏 JSON → 400 中文），末条强制 user=本轮、LLM 成功才 append 两条落盘（失败 502 历史不动）；每条 AI 回复挂「转成任务 / 转成修正 / 采纳为全局结论」——采纳写入 `note`（POST /api/tasks/idea/chat/adopt，空串=清除），`_read_global_note` 在 /api/tasks/execute 与 /api/tasks/idea/fix 装配处注入 `global_note` → 执行 prompt 的【工程级全局结论】段（位置 = feedback 段后、赛题段前），后续任何一步执行都受其约束；**② 清单微编辑 + 调序**：任务卡 ✏️ 编辑（标题/描述/依赖（逗号序号）/验收方式/评分点，`update_task_fields` 纯函数、verify 词表外拒收、depends_on 按数组位置解析 `plan.tasks[i-1].id`）与 ↑/↓ 调序（`move_task` 相邻换位、id 不变、依赖引用稳定；首 up/末 down 禁用），端点 POST /api/tasks/idea/edit|move（先 find_task 校验再 backup 备档——`.bak` 是 move 语义，先备后验会吞清单）；**③ 想法草稿箱**：想法输入区「存入草稿」→ 落盘 `.contest_ideas.json`（IdeaDraft{id=uuid4().hex[:12],text,at}，同文本去重），逐条「分析这条」（复用 idea-fix 分析链路）或「全部逐条分析」（串行 + 状态行「分析中 第 N/总」，失败即停可重试），删除/清理；草稿区与想法分析共享 tasks.busy 全局闸 | idea_chat.py + drafts.py + llm.py（TASK_GLOBAL_DISCUSS_SYSTEM_PROMPT / _global_idea_user_prompt / _idea_plan_summary / discuss_global_idea / global_note 透传）+ task_progress.py（update_task_fields / move_task）+ webapp.py（/api/tasks/idea/chat/read|send|adopt、/api/tasks/idea/edit|move、/api/tasks/idea/drafts/read|add|delete）+ events.py EVENT_IDEA_CHAT + fx/task.js（globalChatHTML / globalNoteBadgeHTML / taskEditFormHTML / taskMoveButtonsHTML / ideaDraftListHTML）+ ui/generate-tasks.js |
| 交付 | 任务推进区「🚀 交付」卡（工单 delivery-suite/01-02，收尾三动作）——**① 打开工程**：POST /api/delivery/open-ide → `open_project_dir`（平台自适应：stm32 优先拉起 Keil——`uv4_path` 配置覆盖 > `C:/Keil*/UV4/UV4.exe` 探测 > 系统关联 `os.startfile`，全失败 explorer 兜底；mspm0 = explorer 打开文件夹 + CCS 手动导入提示；进程启动失败降级为 message 不抛异常，目录缺失 / 平台未知 = DeliveryError 400）；**② 交付检查**：POST /api/delivery/check → `delivery_check`（任务清单完成度：verified+skipped = 完成，其余状态 = 未完成并逐条列出（失败一眼可见）；未拆解清单 = ok False + 中文提示不 400；main.c 缺失 message 追加）；**③ 一键打包**：POST /api/delivery/package → `package_project`（工程目录 zip 到**父目录**，排除 `.contest_*` 内部状态与 `*.tmp` / `*.bak`，命名 `{目录名}-交付-{YYYYMMDD-HHMMSS}.zip` + 同秒序号不覆盖）；前端 = fx/delivery.js（deliveryCheckHTML / deliveryPackageHTML，状态徽章复用 fx/task.js 单源）+ ui/delivery.js（busy 闸共享 tasksSetBusy / tasksIsBusy，目录取 reviseGetDir，跨簇两事件清结果） | delivery.py + errors.py（DeliveryError → 400）+ webapp.py 三同步端点 + fx/delivery.js + ui/delivery.js |
| 参数速调 | 任务推进区「⚙️ 参数速调」卡（工单 param-tune/01-02，与任务清单独立）：AI 扫描 main.c 可调数值参数（阈值/速度/PID 系数/延时/占空比等 → LLM 新协议 `scan_params(main_c, module_interfaces)` → `ParamList`）→ 参数表落盘 `.contest_params.json`（ParamItem{name,label,old_value,anchor,unit,range_hint}，锚 = 声明行原文逐字节、old_value 在锚内恰 1 次且为完整字面量——子串式旧值（"80" 在 800 内）拒收防「确定性改值」静默错值，带后缀值（800UL/1.2f）必须写全）；改值 = `apply_param_change` 纯函数锚首现替换（**零 LLM 改值**）→ 备份 → 写盘 → `verify_compile_tail`（subject="参数修改"）→ 结果 {status, backup_id, compile, main_diff, message}；**不造任务轮次、不动任务状态**（改值 ≠ 重做任务）；事件 EVENT_PARAM_SCANNING / EVENT_PARAM_RESULT / EVENT_PARAM_APPLYING；端点 = POST /api/tasks/params/scan（SSE：param_scanning → param_result → done{params}；**识别结果空表不落盘**——无文件 = 未识别过）/ apply（SSE：param_applying → compile_start → fix_start → verify_result → done；value 非空 ≤64 字符在路由体 400、name 不在表 → 400「请先识别」；**apply 成功回写参数表**——目标项 old_value/anchor 同步磁盘状态（_refresh_param_after_apply；编译失败修复轮改写了该值则保持旧表 → 前端标失效 = 诚实））/ read（同步，逐参数重验锚带 valid——失效行前端禁用 + 「锚已失效」标记；`scanned` 标志区分「未识别」与「已识别无参数」两态）；前端 = fx/params.js（paramListHTML 参数表**网格卡片流**（工单 param-grid/01：响应式 `.param-grid` 卡片——宽屏 3 列 / 窄屏 2 列 / 手机 1 列自适应，12 参数从 12 行变约 3 行；卡头 = 参数名等宽小字（title 悬停全文）+ 含义截 24 字（title 全文），卡体 = 输入框 + 应用按钮，提示段 = 范围 / 单位灰字）+ paramResultHTML 结果面板——复用 verifyStatusMarkup / mainDiffHTML / flashPanelHTML(dir,"params") 烧录行）+ ui/params.js（与任务执行/回滚/烧录共享 tasks.busy 全局闸互斥——两个流程都写 main.c；参数表目录加载自动回填、应用后回写重读刷 new old_value/valid）；范围外：AI 调参建议（不主动给推荐值，range_hint 仅供参照）、自动批量调参、参数表与任务清单联动 | params.py（ParamItem / ParamList / build_params / load_params_file / validate_param_value / apply_param_change / _refresh_param_after_apply / run_param_scan / run_param_apply）+ llm.py（TASK_PARAM_SYSTEM_PROMPT / _param_scan_user_prompt / scan_params / RoutingLLM passthrough）+ webapp.py（/api/tasks/params/scan|apply|read）+ events.py（EVENT_PARAM_*）+ fx/params.js + ui/params.js |
| 步骤报告 | 每步执行后的 AI 行程简报（工单 stepwise-deepen/01-02）：编译验证完成后 LLM 新协议 `report_task_step(task, verify_result, diff_text, module_interfaces)` → `StepReport{what_changed, user_action}`（JSON：做了什么——改了哪些逻辑/编译结果；接下来你要做什么——接线/烧录/观察等用户侧动作，指引落到模块接口清单真实引脚/接口名）；入参含任务采纳的对话结论（dialog_note）；**失败降级不阻断**（report 调用任何异常 → 两字段空串，任务照常落盘终态；前端中文兜底文案）；`TaskIteration` 加 `what_changed` / `user_action`（缺省空串向后兼容，旧清单读取兼容）；事件 `EVENT_TASK_REPORTING`（SSE 序列 …verify_result → task_reporting → done）；调用时机 = run_task 内 verify_compile_tail 之后、manual 覆盖之后、轮次构造之前（报告描述的是最终状态） | llm.py（StepReport / TASK_REPORT_SYSTEM_PROMPT / _task_report_user_prompt / DeepSeek report_task_step）+ task_progress.py（TaskIteration 两字段 / _report_task_step）+ events.py EVENT_TASK_REPORTING + fx/task.js（taskStepReportHTML / taskNextActionHTML / taskIterationsHTML 摘要）+ ui/generate-tasks.js（task_reporting 状态文案 + 结果面板报告块） |
| 烧录 | 「生成 / 任务执行后的烧录到板子」一键烧录（工单 flash-deploy/01-02）：后端定位固件产物（stm32 → `user/Objects/*.hex`（母版已开 `<CreateHexFile>1`）｜mspm0 → `Debug/*.out`，rglob 取 mtime 最新、跳过备份类目录）+ 工具探测（config 覆盖 openocd_path / stflash_path / dslite_path > `which` / `C:/ti/ccs*` 扫 DSLite——**MSPM0/XDS110 走 CCS 自带 DSLite.exe 零安装**；STM32 走 OpenOCD 优先、st-flash 兜底）> 缺失 → FlashError 400 中文指引（安装说明 + 参考命令）；命令构建三 builder（openocd `-f interface/stlink.cfg -f target/stm32f1x.cfg -c "program <hex> verify reset exit"`｜st-flash `write <hex> 0x08000000`｜DSLite `flash --config=<ccxml> <out> -u`，ccxml `targetConfigs/*.ccxml` 自动定位）；执行 180s 超时、输出尾 40 行、中文结果 `{ok, tool, command, command_text, firmware, output, message, timed_out, duration, exit_code}`——烧录失败（探针未接 / 未供电 / DFU 状态）正常返回 ok=False 非异常；平台从产物树反推（context_manifest._infer_platform）；无 AI 配置也可烧（与 /api/compile 同规）；settings GET 带 `flash_auto_tools`（前端「已自动找到」提示，spec 故事 8）；前端三处入口（生成结果面板 + 任务执行结果面板 + **每张任务卡——已实现过的步骤常驻按钮，工单 flash-step-button/01：卡内独立状态/结果容器（uid = task.id），显示条件与上板反馈同判据（taskCanFeedback），pending/skipped/doing 不显示**）+ busy 防重（按钮 disabled / tasks.busy 全局闸）+ 「烧录中…（XDS110/ST-Link，请确认连接，勿拔线）」+ 结果行（✓ / ✗ + 可折叠输出明细 + 复制命令）+ 工具缺失指引卡（安装说明 + 设置页跳转 + 命令复制）；**任务结果面板插入位置 beforeend（网格容器内）——修复回滚按钮原死域委托**（afterend 是网格兄弟节点，冒泡不过网格）；范围外：编译后自动烧录 / 串口 ISP / UART BSL / DSLite identifyProbe 硬件探测（v2 候选） | flash.py（find_firmware / find_ccxml / resolve_flash_tool / build_flash_command / run_flash / flash_project / _join_command）+ config.py 三键 + webapp.py `POST /api/flash` + `_flash_auto_tools` + errors.py FlashError + fx/flash.js（flashBusyText / flashResultHTML / flashGuideHTML / flashOutputHTML / flashCommandHTML / flashPanelHTML）+ ui/flash.js（flashRunShared）+ ui/generate-core.js + ui/generate-tasks.js + ui/settings.js |
| 题型 | 参考库条目的决策题型标记（工单 topic-framework/01）：词表单源 `reference_library.TOPIC_TYPES`（首批 `line_follow` 巡线/循迹决策、`generic` 通用决策结构）+ `validate_topic_type`（词表外值 = 元数据损坏 / 录入非法大声失败，与 platform / anchor_kind 同款）——空 = 未标记（向后兼容）；`ReferenceEntry.topic_type` 落盘 reference.json，经录入 / 编辑表单「题型」下拉写入（词表端点 `GET /api/references/topic-types` 单源带出） | reference_library.py（TOPIC_TYPES / ReferenceEntry.topic_type / add_reference / update_reference）+ webapp.py `/api/references/*` + fx/reference.js（referenceTopicTypeChip）+ ui/reference.js |
| 题型框架 | 题型条目的确定性决策框架段（工单 topic-framework/02-05）：条目目录 `framework/main.c`（**控制文件**——与 reference.json 同阶，不入 files 清单、read_fulltext 不读（避免与注入段重复），entry_stats / list_entry_files 磁盘实况照旧统计；缺失 / 不可读 = 降级 None 不抛错）；语义 = 骨架生成时经 `build_topic_framework` 原样注入骨架 prompt 的「必须保留结构」（状态机枚举 + 10ms 调度 + TODO 填充位，0 真实接口调用 = 平台中立），强指令「枚举 / 调度循环 / 框架函数名保持原样，只在 TODO 位填实现」（强约束）与参考全文（弱约束学习素材）互补——框架代码段走确定性注入、学习说明走 LLM 段（决策点 3）；装配 = webapp `_topic_framework_info`（保序取首个平台匹配 + topic_type 非空条目，平台过滤与 _platform_matches 同判据），/api/skeleton 返回体带 `topic_framework: {injected, topic_type?, source?}`（smoke 冒烟不注入），llm.generate_main_skeleton 加 topic_framework 参数（deepen / task 执行不在本期，范围外） | reference_library.py（REFERENCE_FRAMEWORK_FILENAME / build_topic_framework）+ llm._skeleton_user_prompt + skeleton.generate_skeleton / run_skeleton + webapp.py /api/skeleton + fx/generate.js（frameworkNoteHTML）+ 三条例目 framework/main.c（21F / 26H / car-1-1，均已标 line_follow） |
| 买件指引 | 库外建议的选型知识（工单 buy-guide/01-02）：`wordlist.json` 每个词表行（类别 / 型号）挂 `solutions`（3-5 条方案 = SolutionOption：name / interface / price（文字区间如「￥2-5/套」不写精确价）/ note / suitable / recommended（词表级推荐，每类 ≤2）——**知识确定性**：方案全部来自词表，LLM 只从 solutions.name 里选 `selected`（AI 建议，词表外 = 置空不阻断）不编造方案文本；解析校验单源 wordlist.py `_parse_solutions`（solutions 非数组 / 缺 name / recommended 非 bool = WordlistError 大声失败；旧词表无该键 = 空向后兼容）；命中判定单源 selection.py `_solution_group`（类别名或型号名 → 所属行，_parse_suggestions / _solutions_for / _resolve_selected 三者共用一次线性扫描）；提示词段预算 `WORDLIST_PROMPT_BYTES=4200`（默认词表完整 wire 3170 全量送达——selected 判决依据，曾 1100 截到 37% 导致尾部类别 selected 恒空的回归已钉死：test_wordlist_segment_covers_default_wordlist_and_budget；截断标注 `WORDLIST_TRUNCATION_NOTICE` 进预算预扣）；词表段实发进请求预算反推（REFERENCE_FULLTEXT_BYTES 67000→64000，budget.py 推导同步） | wordlist.py（SolutionOption / HardwareWordGroup.solutions / _parse_solutions / format_wordlist_prompt 紧凑版只列方案名+推荐标记）+ selection.py（OutOfLibrarySuggestion.solutions / selected / _solution_group）+ llm.py（_wordlist_prompt_segment / SELECT_SYSTEM_PROMPT selected 指引）+ fx/recommend.js（suggestionChipHTML 可展开「⤵ N 方案」→ 选型参考面板：方案行 + 推荐 / AI 建议徽标（可共存）+ ui/generate-recommend.js 点击委托（.sugg-wrap active class toggle，建议 chip 无 data-remove 不冲突）+ .chip.out.sugg-chip CSS 特异性 |
| 方案商量 | 买件决策的「用户与 AI 商量」环节（工单 buy-discuss/01-05）：选型参考面板内「和 AI 商量」→ 多轮讨论（LLM 新协议 `discuss_buy_options`：题面/需求句/平台/词表方案/历史（旧→新）→ {reply, review?}；review = 用户最新一轮提出**词表外自定方案**时的 AI 可行性校核 {verdict: feasible|risky|infeasible, reason, suggestion}——**AI 是顾问非裁判**（用户原话「用户的方法只是他的猜想，AI 校核一下这个是不是真的可行并且跟用户进一步沟通」；verdict 不阻断确定，UI 明确标注 AI 意见）；讨论轮数上限 8（`DISCUSS_MAX_ROUNDS`，前端硬闸）；一轮一请求（同步端点 POST /api/buy/discuss，词表行匹配注入 solutions、词表外空方案不编造，BuyError→400/LLMError→502，telemetry `EVENT_BUY_DISCUSS` + recent_llm_workflows 收尾）；wordlist.py / selection.py 的 verdict 词表单源 `BUY_VERDICTS`（llm 从 selection import 防环反向） | llm.py（BuyReview / BuyDiscussion / DISCUSS_SYSTEM_PROMPT / _solutions_list_text / _discuss_history_segment（字符帽 CLARIFICATION_HISTORY_CAP，与澄清历史段同口径）/ _discuss_user_prompt）+ selection.py（BuyError）+ webapp.py /api/buy/discuss + events.py EVENT_BUY_DISCUSS + fx/recommend.js（discussionAreaHTML）+ ui/generate-recommend.js（discussions state + 重渲染委托） |
| 已定方案 | 商量结论的承载与记忆（工单 buy-discuss/02-05）：`SuggestionDecision`（frozen：source=wordlist|custom / name / note / verdict）+ 形状契约单源 `selection.parse_decision`（非 dict/缺 name = None 不阻断；source 词表外→custom、verdict 词表外→空——展示增强零判定）；结论三处生效：① 前端 state（`discussions` Map 键 = 建议名 suggestionKey）+ chip「✓ 已定」/「✓ 已定·自定」徽标（已定渲染行首，推荐/AI 建议可共存）；② localStorage 记住（键 `firstep.buy-decisions.v1`，draft-memory 先例——load/save 注入 storage 纯函数化；刷新/重推经 hydrateDecisions 恢复，本轮已定不覆盖）；③ **进生成上下文**：applyDecision 写回 `s.decision`（载荷）→ 生成请求 requirements 原样上行 → `.contest_context.json` → 深化/任务拆解/任务执行的 `_requirement_lines` 注记（wordlist「（已定：<方案名>）」/ custom「（已定·自定：<名>；<note 全文>；AI 审核：<verdict>）」——note 承载自定想法全文，截断名不丢信息）；wordlist 方案「就用这个」/ 自定「就用我提的」（输入框被发送清空时回退最后一条用户消息；verdict 只在用该消息时借用，新输入未经校核不带审核）；不做已购清单页/服务端存储/结论反向改模块集 | selection.py（SuggestionDecision / parse_decision / BUY_DECISION_SOURCES）+ llm.py（_decision_note / parse_decision 消费）+ fx/recommend.js（decisionBadgeHTML / reviewBadgeHTML / decisionPayload / matchBuyDecision）+ ui/generate-recommend.js（hydrateDecisions / applyDecision / pickCustom） |
| 上下文清单 | 生成尾部写工程根 `.contest_context.json`（纯新增隐藏文件，README 先例，不触碰任何既有生成文件、逐字节契约不破）：完整记录生成输入（题面 / 平台 / slugs / 绑定 / 多实例 / 副产物模板 / Q&A / 功能需求清单 / 参考条目 / main.c 快照 / 时间 / 工具版本）；读侧缺字段 / 旧版本向后兼容；无清单的历史目录自动反推（平台 = 工程配置文件后缀识别、模块 = modules/<slug>/、绑定 = 写侧逆运算（stm32 pin_config.h 宏 / mspm0 syscfg $assign）、main.c 现读），题面 / 需求清单反推不了 → missing 标记提示用户补 | context_manifest.py（写 / 读 / 反推 / 形状校验）/ generator.generate 尾部 / webapp.py `/api/revise/context`；决策 ADR 0014 |

## 架构要点

- 薄壳（webapp 路由 / LLM 网络调用 / 文本抽取）包裹纯逻辑核心。
- 判定素材模型归模型层（依赖倒置）：JudgmentFile / FileVersion 在 report.py（master 构造、llm 消费），llm 层依赖模型层而非反向；master 不再从 llm 导入模型类型（仅 LLM 协议参数类型）。版本分组不变量（版本工程名组不重不漏）在素材模型上唯一声明与校验。
- llm.py 拆层——域判决随域走（照 report.py / selection.py 先例）："模型输出 → ModuleSelection"整条解释链归 selection.build_module_selection（需求派生 / 词表约束 / DeepSeek 怪癖，错误类型 SelectionError 由传输侧翻译回 LLMError），ValidationResult 归 library.py，llm 缩为 协议 + 提示词 + 机械解析 + 预算 薄传输层。
- 文件类别生命周期单源化：四大类别（残留 / 旧 main.c / 基础设施 / 二进制）+ 工程配置文件（工单 09）各是一条 `RuleCategory` 描述（识别规则 + 确定性处置 + 报错文案），流水线遍历 `RULE_CATEGORIES`，不再每处复制平行分支；启动文件候选是表内钩子（决策 2，跨工程去重）。类别表与 `classify` 收进 categories.py，master 只消费（结构测试防回退：恒等引用 + 模块内无规则函数）。
- 蒸馏侧平台适配接缝 = distill_adapters（摘要读 / 渲染（含密度守卫翻译）/ 启动候选谓词 per platform；mspm0 显式无操作；母版库入库结构校验留在 master_store，存储域边界）。识别知识（工程配置文件后缀表）单源 = platforms.PLATFORM_CONFIG_FILE_SUFFIXES。
- 错误映射单源化：路由不写 catch 元组（漏类型是裸 500 的 bug 根源），`_map_errors` 包装兜底统一走 error_to_http 表；表住 errors.py，结构测试反射枚举包内全部异常类断言已登记（白名单只放从不直达 web 层的类）——漏登从此是测试红，不是线上 500。
- 库素材拼装标签格式单源 = library.file_label：模块源码 / 参考素材 / 参考全文共用（prompt 可见契约，改格式只改这一处——曾三处各抄一份，漏一处模型就逐功能看到不同格式）。
- 生成流程的接缝是 `generator.generate_project`（选模块 → 定位母版 → 生成 → 摘要；内部落盘步骤 `generate`）；母版库布局（masters_dir/<platform>）归母版库模块（`master_store.master_project_dir`）。赛题入口装配 = `generator.resolve_topic_context` 唯一出处（永远返回 TopicContext，key 空串 = 未识别到历史赛题），路由只消费不装配。
- 生成侧读缝成真——include 搜索目录按平台分派（写侧 patcher registry 对偶），generator 不再 import 平台模块。外部头豁免同缝：工具链头在 keil/ccs 声明、经 patchers.external_headers 分派（跨平台工具链头拒绝放行），C 标准库头（_LIBC_HEADERS）留门禁，generator 不持有平台工具链知识。
- 模块源读路径单源：模块文件的读盘与头部判定归 skeleton.py 原语（read_module_sources + is_header_path），errors="replace" 编码策略单源——骨架（build_skeleton_interfaces）与生成语料（build_module_corpus）同读法，非 UTF-8 头文件骨架阶段不再崩（曾各抄一份读盘、两种容错）。
- 不变量：任何校验失败都在落盘前发生，绝不产出残缺工程。
- 模块形态：模块 = 纯驱动切片（ADR 0009）——状态机/调度/流程逻辑归生成骨架，跨题复用逻辑归参考文件库；机械验证 = 结构测试黑名单（题号/年份/题名，词表单源 = library.py，能力词白名单防误伤）+ AI 语义校验（判据③能力方向 / ④无题绑定，llm.py 校验提示词双端同源）。
- 前端纯函数单源 = static/js/fx/*.js：index.html 内联脚本已 `<script type="module">` 顶部静态 import（0 构建 / 0 捆绑，浏览器原生 ESM + webapp 静态挂载 /js）；被测试纯函数从 index.html 迁出后不再允许回迁（fx-guard.test.mjs 枚举全部已搬名称兜底「双源回退」）；window 同名桥 = 兼容层（探针脚本 / devtools 按全局名取用）；**DOM 胶水单源 = static/js/ui/*.js（阶段 2 完成：工单 01-25——各 tab 簇 + generate 八簇 + 评审收尾 21-25（断环 / 钉表修正 / host 渲染归簇 / smell 清理，f26e1fa）；index.html 模块区仅剩 fx/app/ui imports + 页签分发器 + 跨簇接缝注册 + 启动 IIFE + init* 调用，无 inline 渲染/监听器定义；ui 模块 import 环由 tests/js/ui-cycle.test.mjs 静态守卫）**：ui 模块间允许单向静态 import；宿主内联脆服务（readinessState 等）经接缝 setter 注册（host 闭包，工单 19 迁出后改静态 import——setStepsDeps 先例已闭环）；跨簇 mutable 状态「主写簇拥有、读方 import、写经 setter」；ui 模块不挂 window 桥（探针用 DOM 实况或动态 import）；app.js 禁止 import 任何 ui 模块（环）。
