{
  "slug": "ads1115",
  "description": "ADS1115 四通道 16bit 外扩 ADC 驱动（mspm0 纯驱动，ADR 0009）：软 I2C 位操作（不占硬件 I2C 外设），MUX 通道选择（AIN0-3 单端）、PGA 增益（±6.144V~±0.256V 六档）、数据率（8~860SPS）可配——ads1115_init 写默认配置（AIN0/±4.096V/连续 128SPS）、ads1115_read(ch) 出 16bit 有符号原始值、ads1115_read_voltage(ch) 出换算电压、set_gain/set_data_rate/set_address 配置服务；适用于多路模拟量采集、称重/电压/电流监测、分离供电多传感器等赛题功能（与库内 adc 板载 12bit 分工：adc = 板载单通道，本件 = I2C 外挂 4 通道 16bit）。",
  "dependencies": [
    "delay"
  ],
  "platforms": {
    "mspm0": {
      "files": [
        "code/ads1115.c",
        "code/ads1115.h"
      ],
      "verified": true,
      "hardware_bound": false,
      "notes": "母版 syscfg GPIO 实例 ADS1115（SCL 宏 ADS1115_SCL_PIN / SDA 宏 ADS1115_SDA_PIN / IOMUX 宏 ADS1115_SCL_IOMUX / ADS1115_SDA_IOMUX——SysConfig 命名 <实例>_<引脚名>_IOMUX，aht10 编译矩阵实测确认）。软 I2C：SDA 方向运行时切换（ADS1115_SDA_OUT/IN 宏，不用硬件 I2C 外设；节点需板上/模块自带上拉）。默认 SCL=PA16 / SDA=PA17——与 DC_MOTOR 编码器 AA/AB、RC522 MOSI/MISO 默认重叠（外扩多通道 ADC 与双电机闭环车/读卡门禁同选概率最低），同选时经引脚绑定消解；地猛星 2×20 排针 31 个 IO 全被默认布局占用（IR_BEAM/PA8 先例）。**与库内 adc 模块（板载 12bit）分工**：adc = 板载 ADC12_0 单通道轮询（MEM0 默认 PA24，us016 共读同槽；改绑走 type 级通道迁移），ads1115 = I2C 外挂 4 通道 16bit（MUX 任选 AIN0-3，补码符号位，FSR 六档增益）；多路模拟量/多传感器分离供电用 ads1115、单路板载用 adc，两件同选互不冲突（不同总线/引脚）。\nwiki 手册来源：sources/materials/lckfb-地猛星移植手册/sensor--ads1115-multichannel-a-to-d-sensor.md（原页 https://wiki.lckfb.com/zh-hans/dmx/module/sensor/ads1115-multichannel-a-to-d-sensor.html；网盘完整工程 https://pan.baidu.com/s/1DSzFuyTioy2EsKwDGmNNXA?pwd=5vej）。改造要点：去掉 main.c 演示与 printf；函数名规范化（WriteADS1115→ads1115_write_register（按 add 寄存器通用保留）/ReadADS1115→ads1115_read(ch)（16bit 原始值）+ads1115_read_voltage（换算电压）/ADS1115_Init→ads1115_init（写默认配置 0xC283 同页面））；IIC 原语静态化（ads1115_iic_start/stop/send_ack/wait_ack/send_byte/read_byte 照 aht10）；**页面负数换算缺陷修正**：页面 `if(num>32768) ret=(65535-num)*0.000125`（未含 32768，且 65535-num 近似）——本实现按 int16_t 补码正确换算 `raw/32768*FSR`（页面分辨率式 0.000125=4.096/2^15 正确）；**页面返回 -1 失败语义**：ReadADS1115 返回 float -1 与原始值冲突，本实现 int16_t 失败返回 0（notes 说明，切换配置失败/重试 20 次超时，连续模式极少见）；页面 do-while 读重试（≤20 次 ×1ms 走 delay_ms）保留（从机忙时不响应）；页面比较器阈值/ALERT-RDY 引脚未用不实现（页面明确「用不到」）；配置寄存器位域（OS/MUX/PGA/MODE/DR/比较器）按页面寄存器说明实现，set_address 按页面器件地址表（ADDR=GND→0x48、VDD→0x49、SDA→0x4A、SCL→0x4B，默认 0x48<<1）。未上板（软 I2C 时序/MUX 切换真机验证留后续）。\nwiki-modules-batch5/01 编译矩阵（2026-09-06）：单选生成 → SysConfig CLI → gmake 0 error、0 warning（warning 核查脚本串行复跑确认）。",
      "kit": "ADS1115 超小型 16 位模数转换器 ADC 4 通道（I2C）",
      "source_url": "https://wiki.lckfb.com/zh-hans/dmx/module/sensor/ads1115-multichannel-a-to-d-sensor.html",
      "pins": [
        {
          "id": "ADS1115_SCL",
          "type": "gpio_out",
          "default": "PA16",
          "required": true
        },
        {
          "id": "ADS1115_SDA",
          "type": "gpio_out",
          "default": "PA17",
          "required": true
        }
      ]
    },
    "stm32": {
      "files": [
        "code/ads1115_stm32.c",
        "code/ads1115_stm32.h"
      ],
      "verified": true,
      "hardware_bound": false,
      "notes": "软 I2C 总线件（SDA 方向运行时切换，不占硬件 I2C 外设/TIMER——不用母版 ml_i2c，spec 决策①）。页面 = **F1 标准库**（stm32f10x.h + RCC_APB2PeriphClockCmd + Mode_Out_PP/IN_FLOATING + WriteBit/ReadInputDataBit——非 F4 嫌疑页）。**页面缺陷清单（已取证 + 修正 + notes + 守卫）**：① **负值换算错误（主缺陷）**：页面 `if(num>32768) ret=(65535-num)*0.000125`（未含 32768、且 65535-num 近似）→ 按 mspm0 版 int16_t 补码正确换算 `raw/32768*FSR`（页面分辨率式 0.000125=4.096/2^15 正确）；② **失败返 -1.0 与合法 -1.0V 混用** → mspm0 API 出参+状态码化（int16_t 失败返回 0——切换配置失败/重试 20 次超时，连续模式极少见）；③ 注释 3/4 返回码未实现 → 按实际实现 0/1/2（高/低字节无应答页面忽略，mspm0 同款）；④ printf 残留（页面 error 1/2）剔除；⑤ delay_1ms(1) 库内无 → delay_ms(1)。换算实现：引脚 gpio_init(OUT_OD) + **SCL init 补 gpio_init(ADS1115_SCL_GPIO, ADS1115_SCL_PIN, OUT_OD)+置高**（批次 3 回修口径——F1 复位浮空输入，ODR 写入无效）；SDA 方向切换 gpio_init(SDA, OUT_OD/IU)（页面原式 Out_PP/IN_FLOATING 按批次 2 决策①统一 OD/IU——均需总线外上拉）；原语族 ads1115_iic_*（页面 delay_us(2)/6/4/4 混合半周期统一为 2us（mspm0 版同）≈100kHz 级，ADS1115 ≤400kHz 裕量足）；API 全对齐 mspm0：init（引脚配置 + 写默认配置 0xC283）+ write_register（0/1/2）+ write_config + read（int16_t，MUX=0x04|ch 切通道，读重试 ≤20×1ms，失败返 0）+ read_voltage（raw/32768×FSR，PGA 六档表）+ set_gain/set_data_rate（参数不合法返 1）+ set_address（7 位地址左移 1）。**默认脚 = PA6（SCL）/PA7（SDA）**——页面默认 SCL=PB8/SDA=PB9 不采用（= 母版 OLED_GPIO 段）；与 motor MOTOR_A_DIR/DIR2（电机方向）默认重叠：外扩多通道 ADC 与「带电机方向的小车运动控制」不同框、同选概率最低；**与批次 2 六件共挂同一软 I2C 总线**（地址 0x48 与 0x38/0x23/0x40/0x44/0x50/0x1A 全异——多路模拟采集+环境站合法共挂，白名单登记）；与既有 I2C_GPIO（PA11/12）/OLED_GPIO（PB8/9）零重叠；同选经引脚绑定消解。与 mspm0 条目分工：mspm0 侧 syscfg 实例 + DL_GPIO（默认 PA16/PA17），本条目 ml_gpio + pin_config.h 宏段（默认 PA6/PA7），API 同名同语义、零交叉改动。\n来源：sources/materials/lckfb-地阔星移植手册/sensor--ads1115-multichannel-a-to-d-sensor.md（原页 https://wiki.lckfb.com/zh-hans/dkx-stm32f103c8t6/module/sensor/ads1115-multichannel-a-to-d-sensor.html；网盘完整工程 https://pan.baidu.com/s/13wIYOV9eqCdG8jVKsKpfUA?pwd=11ad；采购 ADS1115 超小型 16 位模数转换器 ADC 4 通道 https://item.taobao.com/item.htm?spm=a1z10.3-c-s.w4002-24706531953.13.41746a4bt0fSOS&id=522572645353）。**未上板**（软 I2C 时序/MUX 切换真机验证留后续）。\nwiki-stm32-batch3/02 编译矩阵（2026-09）：stm32 单选生成 → UV4（C:/Keil5 V5.06u7）0 error、0 module warning（exit 0，PASS）。",
      "kit": "ADS1115 超小型 16 位模数转换器 ADC 4 通道（I2C；地址 0x48、±6.144V 六档增益、8~860SPS）",
      "source_url": "https://wiki.lckfb.com/zh-hans/dkx-stm32f103c8t6/module/sensor/ads1115-multichannel-a-to-d-sensor.html",
      "pins": [
        {
          "id": "ADS1115_SCL",
          "type": "i2c_scl",
          "default": "PA6",
          "required": true,
          "macros": [
            "ADS1115_SCL_GPIO",
            "ADS1115_SCL_PIN"
          ]
        },
        {
          "id": "ADS1115_SDA",
          "type": "i2c_sda",
          "default": "PA7",
          "required": true,
          "macros": [
            "ADS1115_SDA_GPIO",
            "ADS1115_SDA_PIN"
          ]
        }
      ]
    }
  }
}
