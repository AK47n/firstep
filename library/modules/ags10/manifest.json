{
  "slug": "ags10",
  "description": "AGS10 有害气体传感器驱动（mspm0 纯驱动切片，ADR 0009）：软 I2C 位操作读取（不占硬件 I2C 外设，2 GPIO + SDA 方向运行时切换），ags10_init（空实现占位——器件无初始化序列）+ ags10_read 出 TVOC 浓度（ppb，24bit，带 CRC8 校验）；适用于有害气体（TVOC）检测、空气污染监测、空气质量报警、净化/通风联动等赛题功能。",
  "dependencies": [
    "delay"
  ],
  "platforms": {
    "mspm0": {
      "files": [
        "code/ags10.c",
        "code/ags10.h"
      ],
      "verified": true,
      "hardware_bound": false,
      "notes": "母版 syscfg GPIO 实例 AGS10（SCL 宏 AGS10_SCL_PORT/PIN/IOMUX、SDA 宏 AGS10_SDA_PORT/PIN/IOMUX——SCL/SDA 跨 GPIOB/GPIOA 两端口，生成器按引脚名分派各口宏（max7219 先例），SysConfig 命名 <实例>_<引脚名>_IOMUX；SCL/SDA 均 OUTPUT 初值 CLEARED）。软 I2C：SDA 方向运行时切换（AGS10_SDA_OUT/IN 宏，照 AHT10 先例；节点需板上/模块自带上拉）；位操作半周期 5us ≈ 100kHz 级（页面原值——**页面规格表注明 I2C 接口速率 ≤15kHz 与页面代码时序不一致**，按页面代码实现，真机通信异常时调慢 SCL 半周期延时即可（唯一时序宏点））；延时走 delay 模块，不占 TIMER、不注册 GPIO 中断。API = ags10_init（**空实现占位**——AGS10 无独立初始化序列（页面演示上电直接读；模块预热 ≥120s 期间读数起步属器件特性，等待归调用方），mlx90614/at24c02 先例）+ ags10_read(&voc_ppb)（写寄存器 0x00（地址 0x34）→ 读地址 0x35 应答重试（≤50×1ms）→ 5 字节回包 = 状态 + TVOC 24bit（data[1..3]）+ CRC（data[4]）→ CRC8 校验（页面 Calc_CRC8 原式：初值 0xFF/多项式 0x31——自包含保留）→ TVOC = (data[1]<<16)|(data[2]<<8)|data[3]（ppb；0-99999 量程、25℃/50%RH 典型精度 25% 读数、采样周期 ≥2s——页面规格）；返回 0=成功（出参带回）、1=通信失败、2=发送失败、3=等待超时、4=CRC 校验失败（页面失败码语义）。**上游缺陷修正**（ir_remote 反码校验/和 nrf24l01 上游缺陷先例）：① 页面读地址重试 `while((WaitAck()==1) && (timeout >= 50))` 条件写反（应为 `timeout < 50`——原式循环一次即退、超时分支 `return 3` 永不触发），按函数注释「等读地址应答 ≤50×1ms、超时返回 3」语义修正；② 页面返回值 = TVOC 值/错误码 1-4 混用（TVOC=1 ppb 与错误码语义冲突）→ 出参+状态（mlx90614 先例——页面 `ags10_read()` 直接返回 TVOC 值作为主返回，本实现收敛）。默认 SCL=PB18 / SDA=PA14——与 DC_MOTOR BIN1（双电机）、MAX7219 CS（大数字显示）、DCC_100_PWM2（step_motor 步进脉冲——风机排烟联动）、WS2812 IN（灯带指示）、RC522 SCK（读卡门禁）重叠：气体传感与运动控制/读卡/灯带低频同框、同选概率最低（避让原则同 sgp30：不叠温湿度（AHT10 PB6/PB7、DHT11 PB7、DS18B20 PA7、SHT30 PA28/PA31）、光照（PA12/PA13）、OLED 显示（PB2/PB3）、语音（PB19/PB20）、按键（PA2、PA22-27）、报警（PA15）、无线（PA8/PA9/PA23/PA24）、批次 5 八脚），同选时经引脚绑定消解；与同批默认（PB20/PB24/PA18/PB9）不撞。\nwiki 手册来源：sources/materials/lckfb-地猛星移植手册/sensor--ags10-harmful-gas-sensor.md（原页 https://wiki.lckfb.com/zh-hans/dmx/module/sensor/ags10-harmful-gas-sensor.html；模块代码网盘 https://pan.baidu.com/s/1PMb5bFHRhmIujXx97FZePg?pwd=2z5o 提取码 2z5o；采购 https://item.taobao.com/item.htm?spm=a21n57.1.0.0.148e523cqcVeUq&id=709434114260&ns=1&abbucket=8#detail）。改造要点：去掉 main.c 演示与 printf；函数名规范化（ags10_init/read）；IIC 原语族静态化（ags10_iic_start/stop/send_ack/send_nack/wait_ack/send_byte/read_byte——页面 AGS10_IIC_* 收敛）；Calc_CRC8 静态化为 ags10_crc8（初值 0xFF/多项式 0x31 原式保留）；重试条件修正（见上）；返回值与错误码混用收敛为出参+状态；SYSCFG_DL_init 重复调用删除（模板 main 已调）。与库内同类分工（**气体**）：mq2/mq135/mq5（ADC 模拟量 MQ 系——相对值非 ppm 精标、需预热标定）为模拟量气体件；SGP30（软 I2C，TVOC 0-60000ppb + CO2e 400-60000ppm、预热 ~15s、采样周期 ≥1s）为 TVOC+CO2e 双向数字量件；本件 = 单 TVOC 数字量（0-99999ppb、预热 ≥120s、采样周期 ≥2s、约 25% 精度）——要 CO2e 用 SGP30、只要 TVOC 用本件（价更省），与温湿度件（aht10/dht11/sht30）同为环境站常配（默认脚刻意错开）。\nwiki-modules-batch7/04 编译矩阵（2026-09-08）：单选生成 → SysConfig CLI → gmake 0 error、0 warning；未上板（软 I2C 时序/CRC 校验/15kHz 规格真机验证留后续）。",
      "kit": "AGS10 有害气体传感器模块（5Pin I2C，地址 0x1A；3.0-6.0V 供电、典型 75mW；TVOC 0-99999ppb、预热 ≥120s、采样周期 ≥2s；标准测试气体乙醇）",
      "source_url": "https://wiki.lckfb.com/zh-hans/dmx/module/sensor/ags10-harmful-gas-sensor.html",
      "pins": [
        {
          "id": "AGS10_SCL",
          "type": "gpio_out",
          "default": "PB18",
          "required": true
        },
        {
          "id": "AGS10_SDA",
          "type": "gpio_out",
          "default": "PA14",
          "required": true
        }
      ]
    },
    "stm32": {
      "files": [
        "code/ags10_stm32.c",
        "code/ags10_stm32.h"
      ],
      "verified": true,
      "hardware_bound": false,
      "notes": "软 I2C 总线件（SDA 方向运行时切换，不占硬件 I2C 外设/TIMER——不用母版 ml_i2c，spec 决策①）。页面 = **F1 标准库**（stm32f10x.h + RCC_APB2PeriphClockCmd；OD+IPU+SetBits 拉高——非 F4 嫌疑页）；**全批缺陷最重页**。**页面缺陷清单（已取证 + 修正 + notes + 守卫，9 条）**：① **读地址重试条件写反（致命）**：页面 L280 `while((WaitAck()==1) && (timeout >= 50))`——首轮后 timeout=1、条件恒假 → 循环一次即退、L283 `if(timeout >= 50) return 3` 永不触发（与函数注释「3：等待超时」矛盾）→ 修正 `timeout < AGS10_RETRY_MAX`（ir_remote/nrf24l01 上游缺陷先例，mspm0 版已同）；② **返回码与 TVOC 值混用**（ags10_read 主返回 = TVOC 值或错误码 1-4——TVOC=1ppb 与「通信失败」不可分）→ 出参 + 状态码（mlx90614 先例）；③ **delay_1us/delay_1ms 库内不存在**（ml_delay.h 仅 delay_us/ms/s——页面三处用法改写 delay_us(5)/delay_ms(1)）；④ **规格 ≤15kHz vs 代码 5us 半周期 ≈100kHz 矛盾**（差 ~7 倍——按页面代码实现，真机通信异常时调慢 SCL 半周期（唯一时序宏点——mspm0 侧同判））；⑤ 注释掉的调试 printf（Check failed——不落）；⑥ char ack 死变量收敛；⑦ Send_Nack/Send_Ack 冗余二次写（末值正确——页面原式保留）；⑧ main 未等预热 ≥120s + 1s 采样 <2s 规格（演示——不落，预热归调用方）；⑨ 状态字 data[0] 未使用（页面未说明状态位——读入不检查）。换算实现：引脚 gpio_init(OUT_OD)（页面原式）+ init 两脚置高（页面 SetBits——「SCL上电必须保持高电平」要求）；SDA 方向切换 gpio_init(SDA, OUT_OD/IU)（页面原式 OD+IPU——总线需 1kΩ～10kΩ 上拉）；原语族 ags10_iic_*（5us 半周期 ≈100kHz 级；Send_Nack/Send_Ack 拆两函数按页面原式）；ags10_crc8 静态（0x31 多项式/0xFF 初值——页面原式）；API = ags10_init（空占位——无独立初始化序列，预热 ≥120s 归调用方；引脚配置由 init 完成（页面 ags10_gpio_init 合并））+ ags10_read（写 0x34+0x00 → 读 0x35 重试 timeout<50×1ms → 5B = 状态 + TVOC 24bit + CRC → CRC 校验 → TVOC = (d1<<16)|(d2<<8)|d3 ppb (0-99999 量程)；0=成功/1=通信失败/2=发送失败/3=等待超时/4=校验失败——页面失败码语义；出参判空用 0）。**默认脚 = PA6（SCL）/PA7（SDA）**——页面默认 SDA=PB8/SCL=PB9 不采用（= 母版 OLED 段）；与 motor MOTOR_A_DIR/DIR2（电机方向）默认重叠：有害气体传感与「带电机方向的小车运动控制」不同框、同选概率最低（刻意不叠 mspm0 侧 ags10 默认同框组合——stm32 侧共总线设计）；**六件软 I2C 件默认共挂此总线**（地址 0x1A 与 0x38/0x23/0x40/0x44/0x50 全异——空气质量站（气体+温湿度+光照+记录）合法共挂，白名单登记）；与既有 I2C_GPIO（PA11/12）/OLED_GPIO（PB8/9）零重叠；同选经引脚绑定消解。与 mspm0 条目分工：mspm0 侧 syscfg 实例（跨口 PB18/PA14）+ DL_GPIO，本条目 ml_gpio + pin_config.h 宏段（默认 PA6/PA7），API 同名同语义、零交叉改动。\n来源：sources/materials/lckfb-地阔星移植手册/sensor--ags10-harmful-gas-sensor.md（原页 https://wiki.lckfb.com/zh-hans/dkx-stm32f103c8t6/module/sensor/ags10-harmful-gas-sensor.html；成功案例 https://pan.baidu.com/s/1oBG1eHMyNNrstObdw1kDUg?pwd=vhxz（本页无资料下载段）；采购 AGS10有害气体传感器 https://item.taobao.com/item.htm?spm=a21n57.1.0.0.148e523cqcVeUq&id=709434114260&ns=1&abbucket=8#detail）。**未上板**（软 I2C 5us 半周期时序/CRC8 校验/50×1ms 重试窗口真机验证留后续——真机通信异常时按规格 ≤15kHz 调慢 SCL 半周期，唯一时序宏点）。\nwiki-stm32-batch2/06 编译矩阵（2026-09）：stm32 单选生成 → UV4（C:/Keil5 V5.06u7）0 error、0 module warning（exit 0，PASS）。",
      "kit": "AGS10 有害气体传感器模块（5Pin I2C，地址 0x1A；3.0-6.0V 供电、典型 75mW；TVOC 0-99999ppb、预热 ≥120s、采样周期 ≥2s；标准测试气体乙醇）",
      "source_url": "https://wiki.lckfb.com/zh-hans/dkx-stm32f103c8t6/module/sensor/ags10-harmful-gas-sensor.html",
      "pins": [
        {
          "id": "AGS10_SCL",
          "type": "i2c_scl",
          "default": "PA6",
          "required": true,
          "macros": [
            "AGS10_SCL_GPIO",
            "AGS10_SCL_PIN"
          ]
        },
        {
          "id": "AGS10_SDA",
          "type": "i2c_sda",
          "default": "PA7",
          "required": true,
          "macros": [
            "AGS10_SDA_GPIO",
            "AGS10_SDA_PIN"
          ]
        }
      ]
    }
  }
}
