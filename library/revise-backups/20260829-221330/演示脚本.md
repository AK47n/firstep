# 演示脚本

> 本脚本由生成器按评分点 / 功能需求 / 模块清单确定性渲染（不含 AI 生成内容），
> 请按实物接线与现场情况调整演示顺序与细节。

## 演示前准备

- 编译烧录：按 README.md「快速上手：编译 + 烧录」操作
- 硬件接线：按 README.md「引脚接线表」完成
- 模块自检：按 README.md「验证顺序清单」逐项验证

## 评分点演示

### 1 完成第（1）项（基础 · 20 分）

- 操作：演示「完成第（1）项」，对应句子 24
- 预期现象：「完成第（1）项」按题面要求呈现
- 对应功能需求：小车自动行驶到B点停车（句子 24）；停车时有声光提示（句子 24）
- 对应模块：motor（TB6612 双路直流电机驱动（双平台统一 API）：motor_set_duty 调速 + motor_set_direction 方向 + motor_encoder_read 编码器读数（读后清零）。）、pid（PID 闭环控制 + 灰度循迹驱动（双平台）：通用 PID 控制器（位置式/增量式，pid_init/pid_cal/pidout_limit）+ 左右轮目标速度输出（含轮速校准，motor_target_set）+ 灰度 8 路读取与边缘中点巡线偏差计算（line_error_calc）+ PD 巡线速度输出（line_pid_track）；适用于巡线/循迹/速度闭环类赛题功能，路口/停车等决策逻辑归生成骨架。）、xunji、led_beep（声光组合模块（双平台）：LED + 蜂鸣器同时开关 + led_beep_alarm 声光报警；只控制一方请选 led 或 beep。）

### 2 完成第（2）项（基础 · 20 分）

- 操作：演示「完成第（2）项」，对应句子 28、29
- 预期现象：「完成第（2）项」按题面要求呈现
- 对应功能需求：沿半弧线行驶（句子 28）；经过每个点声光提示一次（句子 29）
- 对应模块：xunji、imu_uart（UART 串口陀螺仪（新 IMU 器件）MSPM0 驱动：115200 9 字节帧 + CRC16(Modbus) 校验 + 帧间隙噪声过滤，中断解析出 yaw 角度与角速度（raw×0.1），供车头朝向闭环使用。）、led_beep（声光组合模块（双平台）：LED + 蜂鸣器同时开关 + led_beep_alarm 声光报警；只控制一方请选 led 或 beep。）

### 3 完成第（3）项（发挥 · 30 分）

- 操作：演示「完成第（3）项」，对应句子 33、34
- 预期现象：「完成第（3）项」按题面要求呈现
- 对应功能需求：小车自动行驶到C点（句子 33）；沿半弧线行驶到B点（句子 33）；沿半弧线行驶到A点停车（句子 34）
- 对应模块：motor（TB6612 双路直流电机驱动（双平台统一 API）：motor_set_duty 调速 + motor_set_direction 方向 + motor_encoder_read 编码器读数（读后清零）。）、xunji

### 4 完成第（4）项（发挥 · 30 分）

- 操作：演示「完成第（4）项」，对应句子 39
- 预期现象：「完成第（4）项」按题面要求呈现
- 对应功能需求：按要求3路径自动行驶4圈停车（句子 39）
- 对应模块：pid（PID 闭环控制 + 灰度循迹驱动（双平台）：通用 PID 控制器（位置式/增量式，pid_init/pid_cal/pidout_limit）+ 左右轮目标速度输出（含轮速校准，motor_target_set）+ 灰度 8 路读取与边缘中点巡线偏差计算（line_error_calc）+ PD 巡线速度输出（line_pid_track）；适用于巡线/循迹/速度闭环类赛题功能，路口/停车等决策逻辑归生成骨架。）、xunji

### 5 设计报告（未分区 · 20 分）

- 操作：演示「设计报告」，对应句子 40
- 预期现象：「设计报告」按题面要求呈现
- 对应功能需求：设计报告（句子 40）
- 对应模块：未命中库内模块

## 补充功能需求演示

### 完成一圈用时不大于30秒（句子 31）

- 对应模块：未命中库内模块
- 操作：演示「完成一圈用时不大于30秒」
- 预期现象：「完成一圈用时不大于30秒」相关现象正常呈现

### 小车行驶时只能前进，不得后退（句子 47）

- 对应模块：motor（TB6612 双路直流电机驱动（双平台统一 API）：motor_set_duty 调速 + motor_set_direction 方向 + motor_encoder_read 编码器读数（读后清零）。）
- 操作：演示「小车行驶时只能前进，不得后退」
- 预期现象：「小车行驶时只能前进，不得后退」相关现象正常呈现

### 采用TI MSPM0系列MCU控制（句子 48）

- 对应模块：config（集中外设配置头文件：硬件引脚映射（UART/GPIO/LED/蜂鸣器/DIP 拨码）与通用显示、滤波参数统一集中定义，供各模块与主程序统一调用；适用于 UWB 定位、OLED 显示、LED/蜂鸣器指示等多外设联调场景。）
- 操作：演示「采用TI MSPM0系列MCU控制」
- 预期现象：「采用TI MSPM0系列MCU控制」相关现象正常呈现

### 小车上不得安装摄像头（句子 52）

- 对应模块：未命中库内模块
- 操作：演示「小车上不得安装摄像头」
- 预期现象：「小车上不得安装摄像头」相关现象正常呈现

### 水平铺设场地，无其他标记（句子 54）

- 对应模块：未命中库内模块
- 操作：演示「水平铺设场地，无其他标记」
- 预期现象：「水平铺设场地，无其他标记」相关现象正常呈现

### 小车不得借助周围环境物品导航（句子 59）

- 对应模块：未命中库内模块
- 操作：演示「小车不得借助周围环境物品导航」
- 预期现象：「小车不得借助周围环境物品导航」相关现象正常呈现

### 行驶过程中不得人为干涉、遥控（句子 62）

- 对应模块：未命中库内模块
- 操作：演示「行驶过程中不得人为干涉、遥控」
- 预期现象：「行驶过程中不得人为干涉、遥控」相关现象正常呈现

### 起始点摆放方向自定（句子 65）

- 对应模块：未命中库内模块
- 操作：演示「起始点摆放方向自定」
- 预期现象：「起始点摆放方向自定」相关现象正常呈现

### 经过A、B、C、D点必须有声光提示（句子 67）

- 对应模块：led_beep（声光组合模块（双平台）：LED + 蜂鸣器同时开关 + led_beep_alarm 声光报警；只控制一方请选 led 或 beep。）
- 操作：演示「经过A、B、C、D点必须有声光提示」
- 预期现象：「经过A、B、C、D点必须有声光提示」相关现象正常呈现

### 小车地面投影覆盖圆弧顶点（句子 69）

- 对应模块：未命中库内模块
- 操作：演示「小车地面投影覆盖圆弧顶点」
- 预期现象：「小车地面投影覆盖圆弧顶点」相关现象正常呈现

### 在圆弧上行驶时投影在弧线上（句子 71）

- 对应模块：xunji
- 操作：演示「在圆弧上行驶时投影在弧线上」
- 预期现象：「在圆弧上行驶时投影在弧线上」相关现象正常呈现

### 完成时间超过规定一倍不得分（句子 72）

- 对应模块：未命中库内模块
- 操作：演示「完成时间超过规定一倍不得分」
- 预期现象：「完成时间超过规定一倍不得分」相关现象正常呈现

### 小车采用车载电池供电（句子 73）

- 对应模块：未命中库内模块
- 操作：演示「小车采用车载电池供电」
- 预期现象：「小车采用车载电池供电」相关现象正常呈现

### 进入测试环节中途不得更换电池（句子 74）

- 对应模块：未命中库内模块
- 操作：演示「进入测试环节中途不得更换电池」
- 预期现象：「进入测试环节中途不得更换电池」相关现象正常呈现

## 演示注意事项

> 请按实物调整：本脚本由生成器自动产出，实际演示请以现场硬件与评委要求为准。
