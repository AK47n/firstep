# 演示脚本

> 本脚本由生成器按评分点 / 功能需求 / 模块清单确定性渲染（不含 AI 生成内容），
> 请按实物接线与现场情况调整演示顺序与细节。

## 演示前准备

- 编译烧录：按 README.md「快速上手：编译 + 烧录」操作
- 硬件接线：按 README.md「引脚接线表」完成
- 模块自检：按 README.md「验证顺序清单」逐项验证

## 评分点演示

### 1 A点到B点自动停车并声光提示，不超过15秒（基础 · 20 分）

- 操作：演示「A点到B点自动停车并声光提示，不超过15秒」，对应句子 24、25、26、27
- 预期现象：「A点到B点自动停车并声光提示，不超过15秒」按题面要求呈现
- 对应功能需求：声光提示（LED指示+蜂鸣器）（句子 24）
- 对应模块：led_beep（声光组合模块（双平台）：LED + 蜂鸣器同时开关 + led_beep_alarm 声光报警；只控制一方请选 led 或 beep。）

### 2 A→B→C→D→A一圈，每经过点声光提示，不超过30秒（基础 · 20 分）

- 操作：演示「A→B→C→D→A一圈，每经过点声光提示，不超过30秒」，对应句子 28、29、30、31、32
- 预期现象：「A→B→C→D→A一圈，每经过点声光提示，不超过30秒」按题面要求呈现
- 对应功能需求：沿线自动巡线行驶（弧线路径）（句子 28）；无引导线直线段自主行驶（航向保持）（句子 28）
- 对应模块：pid（灰度循迹 + PID 闭环控制：8 路灰度传感器一字排开朝下看地面，黑线在哪个探头下就知道车偏了多少，算出偏差后交给 PID 调节左右轮转速把车拉回线上——巡线小车最核心的一块。接线：8 路灰度输入 + 电机驱动输出，灰度默认 8 个 GPIO（stm32 PB12-PB15/PA8/PB3/PB6/PB7，mspm0 PA22-PA27/PB6/PB7）。驱动接口：gray_init() 初始化 8 路灰度、line_error_calc() 出巡线偏差、line_pid_track() 由偏差直接输出左右轮速度、pid_cal() 是通用 PID 计算（位置式/增量式）、motor_target_set() 设左右轮目标速度（含轮速校准）；巡线用 PD 即可，参数在参数卡上调。适用于巡线/循迹/速度闭环类赛题功能；路口判断、停车、模式切换等决策逻辑归生成骨架，不写进模块。）、motor（TB6612 双路直流电机驱动（双平台统一 API）：motor_set_duty 调速 + motor_set_direction 方向 + motor_encoder_read 编码器读数（读后清零）。）、imu_uart（UART 串口陀螺仪（新 IMU 器件）MSPM0 驱动：115200 9 字节帧 + CRC16(Modbus) 校验 + 帧间隙噪声过滤，中断解析出 yaw 角度与角速度（raw×0.1），供车头朝向闭环使用。）

### 3 A→C→B→D→A一圈，每经过点声光提示，不超过40秒（发挥 · 30 分）

- 操作：演示「A→C→B→D→A一圈，每经过点声光提示，不超过40秒」，对应句子 33、34、35、36、37、38
- 预期现象：「A→C→B→D→A一圈，每经过点声光提示，不超过40秒」按题面要求呈现
- 对应功能需求：无引导线直线段自主行驶（航向保持）（句子 33）
- 对应模块：imu_uart（UART 串口陀螺仪（新 IMU 器件）MSPM0 驱动：115200 9 字节帧 + CRC16(Modbus) 校验 + 帧间隙噪声过滤，中断解析出 yaw 角度与角速度（raw×0.1），供车头朝向闭环使用。）

### 4 按路径3自动行驶4圈停车，用时越少越好（发挥 · 30 分）

- 操作：演示「按路径3自动行驶4圈停车，用时越少越好」，对应句子 39
- 预期现象：「按路径3自动行驶4圈停车，用时越少越好」按题面要求呈现
- 对应功能需求：四圈重复路径行驶后停车（句子 39）
- 对应模块：ntb_time

### 5 设计报告（未分区 · 20 分）

- 操作：演示「设计报告」，对应句子 40、41
- 预期现象：「设计报告」按题面要求呈现
- 对应功能需求：未关联到具体需求
- 对应模块：未命中库内模块

## 补充功能需求演示

### 采用TI MSPM0系列MCU控制（句子 49）

- 对应模块：未命中库内模块
- 操作：演示「采用TI MSPM0系列MCU控制」
- 预期现象：「采用TI MSPM0系列MCU控制」相关现象正常呈现

## 演示注意事项

> 请按实物调整：本脚本由生成器自动产出，实际演示请以现场硬件与评委要求为准。
