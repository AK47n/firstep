弹窗正文区域里：

- **有图片**：显示了一张嵌入的截图，内容像是一个代码编辑器/IDE 窗口（左侧有项目文件树，右侧是代码区，底部有输出/编译栏），整体是灰白/浅色调，并不是那种彩色圆角屏幕示意图。
- **没有代码块**：没有看到深色底、带语法颜色高亮的 Markdown 代码区；图中那些代码文字是图片本身的内容，不是代码块渲染出来的。
- **正文排版**：弹窗正文里除了这张图片外，没有看到标题、段落、列表等文字内容，所以看不到正常的 Markdown 文字排版效果。上方显示的是路径、批次、目录、大小、修改时间等元数据，不属于正文排版。
