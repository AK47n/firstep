# 03 — 补图注两级化：矢量图标注文字布局优先，视觉兜底

**要做什么：** 取历史赛题题面时，若原 PDF 的「图N」附图是**矢量图**（视觉提取不到栅格图对象），改为提取图内**标注文字并重建空间布局**（visitor_text 带坐标 + cm×tm 复合矩阵 → 按 y 聚类成行、行内按 x 排序），生成 `[图N 标注]` 段写回题面——零额度，2026C/2024H 这类矢量图题从此「AI 能读懂标注与线段的位置关系」。文字层为空（扫描件 / 无文本层）才走既有视觉兜底。幂等：题面已含 `[示意图` 或 `[图N 标注` 跳过。

**被谁阻塞：** 无——可立即开始（02 的 enrich 接线在，本工单替换其图注生成层）

**状态：** resolved

- [x] PDF 标注提取：逐页 visitor_text 取段（cm×tm 复合还原页面坐标）→ 行聚类（y 容差 + 行内 x 排序）→ 「图N」标题行定位 → 短行归属（窗口 + 长行隔离 + 最近标题）→ `[图N 标注]` 布局段；坏 PDF / 无图 / 无标注 → 空串降级
- [x] enrich_topic_image_notes 两级化：文字标注优先（非空即用，不调视觉）；空 → 视觉兜底；幂等判定含 `[图N 标注`
- [x] 测试：纯逻辑（窗口 / 短行过滤 / 排序 / 多图归属 / 无标题空串）4 个 + 提取链（假 PdfReader visitor 喂段 / 缩放坐标复合 / 坏 PDF）2 个 + enrich 两级（优先文字不调视觉 / 文字空走视觉 / 幂等更新）3 个；既有测试全绿
- [x] 全量 pytest 绿（2014）；CONTEXT.md「赛题库」词条补两级口径；**真机验证通过：2026C 取题面返回 `[图1 标注]` + `[图2 标注]` 两个干净布局块**（区域标签 / 左 -45°~-15° / 右 15°~45° / 尺寸链 60cm→100cm×3 / 高度 37 / α，零额度；页码行与伪标题已滤除）
