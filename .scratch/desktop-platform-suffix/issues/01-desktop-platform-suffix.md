# 01 — 桌面目录名带平台后缀（后端纵切 + 测试）

**要做什么：** 勾选「生成到桌面赛题文件夹」生成 stm32 → 桌面出现 `<题名>_STM32` 目录；同一赛题换 mspm0 → `<题名>_MSPM0` 独立目录，两版并存互不冲突；同平台同题再次生成仍 400 拒绝。历史无后缀目录不迁移不写入。手动输出目录模式零变化。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [ ] 平台 → 后缀映射单源（`stm32 → _STM32`、`mspm0 → _MSPM0`，未知平台大声失败）
- [ ] 桌面输出目录解析装配：历史赛题（topic_id）与粘贴题面（AI 题名）两路径均拼接后缀后再裁决
- [ ] 同题 stm32 / mspm0 端到端两次生成 → 两个带后缀目录、响应 output_dir 正确、无 400
- [ ] 同题同平台第二次生成 → 400 中文、目录内容原样（护栏保持）
- [ ] 手动模式 → output_dir 原样使用、无后缀（回归）
- [ ] 「请先删除或改题名」400 文案补充「换平台会自动生成带平台后缀的新目录」
- [ ] 全量 pytest 绿；CHANGELOG 中文记录
