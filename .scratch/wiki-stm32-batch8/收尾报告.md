# 批次 8 收尾报告（wiki-stm32-batch8）— 无线/红外六件（stm32 线）

## 结论

2026-09-07 收官。批次 8 = **无线/红外六件全 A 类**（as32 / hc05 / nrf24l01 / rc522 / ir_remote / ir_remote_tx——mspm0 条目均已存在、本批零改动），全部补 **stm32 平台条目**：API 与 mspm0 `<slug>.h` 现名逐字对齐；规格落入 `.scratch/wiki-stm32-batch8/spec.md` + issues/01-06（照批次 7 管线）。

## 六件完成情况

| 工单 | slug | 形态 | 默认脚（stm32） | 关键点 | 提交 |
|---|---|---|---|---|---|
| 01 | as32 | UART 轮询透传 | TX=PB10 / RX=PB11（UART_3/ZIGBEE 互替同脚） | 9600；关 RXNEIE 轮询（页面 RX+IDLE 中断改 SR 轮询）；cap 截断修正（%MAX 回绕 bug）；无 rx_handler（不进 isr.c 聚合） | f8720f1e |
| 02 | hc05 | UART 中断环形缓冲 | TX=PA9 / RX=PA10（UART_1/UWB 互替同脚）+ STATE=PA8 / KEY=PB4 | 9600；**hc05_rx_handler 入 isr.c 聚合**（pinwriter `_UART_CALLS_ROLES` + 母版 isr.c `__weak` + `USART1_IRQ_CALLS` 首次扩展）；环形缓冲满丢新字节 | 3fab7d0c |
| 03 | nrf24l01 | 软 SPI 六脚位操作 | CLK=PB10 / MOSI=PB11 / MISO=PB4 / CSN=PB12 / CE=PB13 / IRQ=PB5（全端口 B 单 PORT 宏） | 零延时位操作（72MHz 达标）；IRQ 只读不注册 EXTI；DYNAMIC_PACKET 页外符号剔除 + `_write_buf` OR 写位；不占硬件 SPI/TIMER | 573a2629 |
| 04 | rc522 | 软 SPI 五脚位操作 | CS=PB0 / RST=PB1 / SCK=PB6 / MOSI=PB4 / MISO=PB5（全端口 B 单 PORT 宏） | 200us 半周期页面时序；UID 复制 4 字节修正（6→4 越界）；CalulateCRC/RC522_Rese 拼写保留；`_antenna_off` 未引用裁剪（防 UV4 #177-D） | f9979a10 |
| 05 | ir_remote | 轮询忙等解码 | OUT=PA10（gpio_in） | 20us 拍（delay_us 步进）；EXTI2 改轮询（不注册/不占 TIMER）；反码严格校验（页面错乱修正）；与发射 PA9 刻意错开 | e22f5e68 |
| 06 | ir_remote_tx | 38kHz 载波 | OUT=PA9（gpio_out） | delay_us(13) 半周期（mspm0 delay_cycles 先例换算，±0.5us notes）；周期数公式 us×38000/1e6（mspm0 code-review 修正防回潮）；MSB 先；页面 UART 指令形态归骨架 | 6bd0acc3 |

## 基础设施改动（本批首次）

- `src/contest_generator/pinwriter.py`：`_UART_CALLS_ROLES` 增 `("HC05_UART", "hc05_rx_handler")`（as32 轮询件不登记——绑定换实例不影响 IRQ 分组）。
- 母版 `library/masters/stm32/pin_config.h`：+ 33 宏（6 件）+ `USART1_IRQ_CALLS` 增 hc05_rx_handler()（默认分组 = digit+coord+uwb+hc05）。
- 母版 `library/masters/stm32/isr.c`：+ `__weak void hc05_rx_handler(void) {}`。
- 测试：test_pins STM32_MACRO_VALUES +33 宏 + CALLS 钉值更新；test_pin_unlock_uart 重分组期望跟进（USART1 含 hc05、swap 后 USART2=uwb+zigbee 不变）；test_default_layout WHITELIST 12 组引脚登记（互替件同脚/不同框语义）+ conflict_groups_resolved 分组断言同步；test_pin_bindings uart 共享组测试适配（as32/nrf24l01 绑走恢复纯 zigbee UART 家族组——PB10/PB11 现含 gpio_out 混入 → 需一并绑走）。

## 验证记录

- 六件 UV4 单选编译矩阵：**0 error / 0 module warning**（`C:/Keil5/Core/UV4/UV4.exe`，脚本 `.scratch/wiki-stm32-batch8/matrix/run_<slug>_matrix.py`，产物 `matrix/<slug>/build.log`）。
- 全量 pytest：**3765 passed, 0 failed**（批次 7 基线 3739 → +26）。
- 一键快检：`.scratch/wiki-stm32-batch8/sweep_6_modules.py` **6/6 OK**（存在性/verified/hardware_bound/wordlist 挂接/kit+source_url 地阔星原页判据/依赖/宏值/pins 形状/代码守卫/notes needles/mspm0 存在性）。
- 词表：六件零补录（mspm0 批已挂接 lib_modules）。
- mspm0 零改动：仅 manifest description 双平台化（无平台条目变更）。

## 编码与评审发现（首轮 UV4 修复）

1. as32/hc05 `NULL` 未定义（headfile.h 不含 stddef）——按 ball-detect-null-fix/01 先例补 `#include <stddef.h>`。
2. hc05_stm32.h 注释 `USART_*/GPIO_*` 含 `*/` 提前终结块注释 → UV4 10 错误——措辞改为「USART 与 GPIO API 换算」。
3. rc522 `_antenna_off` 未引用 → UV4 #177-D 告警——裁剪（mspm0 版保留，语义等价，notes 记录）。

## code-review 两轴

### 规格轴（子代理 1210e85f，2026-09-07）

- 总体：规格符合度很高——六件 API 与 mspm0 头文件现名逐一对齐、33 条新宏、`_UART_CALLS_ROLES`/`__weak hc05_rx_handler`/`USART1_IRQ_CALLS` 聚合扩展、软 SPI 全 B 口单 PORT 宏、rc522 UID 4 字节修正、as32 关 RXNEIE + cap 截断、mspm0 侧零改动；87 项相关测试全绿（本批全量 pytest 3765）。
- (a) 缺失：`test_pin_bindings` as32/nrf 绑走适配 + CONTEXT 补录仅存在于工作树未提交改动（六工单提交集不含）——**收尾提交落地**（本报告随提交）。
- (b) 蔓延：ir_remote 位判定「两窗口皆不中→整帧失败」比页面更严（spec 只要求反码修正）——notes 已透明记录，判定为可接受透明的轻微蔓延；FEATURE 0x07 / AS32_BAUDRATE / HC05_BAUDRATE 与 mspm0/工单结论一致，不计。
- (c) 疑似错误（均继承 mspm0 或本批语义记录，已 notes 回填）：
  1. **hc05×UART_1 波特率互覆**（本批特有）：9600 与同实例共享角色（DIGIT/COORD/UWB 115200）为同物理 USART1——双选默认时最后 init 者定波特率、另一角色静默失效；mspm0「每实例独立 targetBaudRate」先例不完全同构 → **notes 已补 stm32 特有语义记录**（互替同选概率最低、同选绑定换实例成对消解）。
  2. nrf24l01 tx 超时不清 STATUS（与 mspm0 逐字节同源继承）→ **notes 已补边界记录**（flush_* + 重试归调用方）。
  3. ir_remote 引导高 150-250 拍窗口重叠（与 mspm0 阈值逐字一致）→ **notes 已补阈值窗口重叠记录** + 位判定从严透明化。

### 标准轴（两个独立子代理，2026-09-07）

- **v1（51d794b7）**：硬违例 1 条 + 判例 5 条。
  - **硬违例（已整改）**：pin_config.h as32 宏段注释缺重叠主体/同选概率最低理由（信息在 manifest notes、非标准位置缺失、低严重度）→ **补全推理注释**（互替件同脚 + 9600 + 轮询 + 页面默认串口2 不照抄）。
  - 判例（已整改/记录）：① isr.c 头部注释过时（UART_1 未含 HC05）→ **补「+HC05 共享」**；② PB4/NJTRST 释放约束未记录 → **pin_config.h hc05 段注释补一行**（key/relay 先例同一约束，本批 hc05 KEY/nrf MISO/rc522 MOSI 沿用）；③ **nrf24l01 RF_SETUP 0x26 位值与「1Mbps」注释不符**（0x26 置 bit5=RF_DR_LOW=250kbps——与 mspm0 批 1 逐字同源遗留）→ **代码注释改准确 + manifest notes 记录**（按 mspm0 先例保留 0x26；要 1Mbps 调 set_speed(NRF24L01_SPEED_1M)，真机复核留后续）；④ **IR_TX_HALF_CYCLES 宏名不表意**（Mysterious Name）→ **改名 IR_TX_HALF_PERIOD_US**（mspm0 版名按 delay_cycles 命名；stm32 无该原语随语义改）+ 注释 + 测试守卫同步；⑤ 测试模板「mspm0 零改动守卫」缺失——**复核为误报**：六件 test_module_*.py 均保留 mspm0 批已有 test_<slug>_mspm0_single_select_generation / syscfg 实例断言（文件内既有零改动守卫）。
- **v2（8e568959）**：**硬违规 0**（语言规范/ADR 0009/零引脚字面量/manifest 四要素/UART 聚合扩展均通过）；判例 8 条（rc522 魔法寄存器/无界等待——页面原样+注释、rc522 `_select((uint8_t*)uid)` const 强转——与 mspm0 同源瑕疵（后续小工单关注）、软 SPI 原语跨模块同构——独立切片惯例、IR_TX_MSB_FIRST 条件编译有注释、PWR_18DB 命名（实为 -18dBm）——mspm0 同源、as32 CR1 位操作按 INST->SR/DR 先例扩展接受、hc05 UART_1 四 handler 共享承接既定先例）；无阻断项。
- 两轴合计：**0 硬违规遗留**（v1 硬违例已整改）；判例整改 4 项（isr.c 注释/PB4 注释/RF_SETUP 注释+notes/宏改名）+ 1 项误报澄清；其余判例按先例/记录保留。

## 遗留

- 上板真机验证（六件）：LoRa 透传/对码、HC05 蓝牙对码与 AT、nrf24l01 位时序与双板连通、rc522 200us 时序与卡片交互、红外 38kHz 载波与收发配对（发射头频谱/阈值）——notes 均已注明「未上板」。
- 范围外（notes 记录）：nrf24l01 EXTI 中断接收方式（页面方案）、ir_remote_tx UART 指令解析（归生成骨架）、as32 AT 配置（MD0/MD1 未接线）、软 SPI 时序真机标定。
