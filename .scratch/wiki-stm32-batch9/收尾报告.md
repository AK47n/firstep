# 批次 9 收尾报告（wiki-stm32-batch9）— 语音/电机/新无线七件（stm32 线）

## 结论

2026-09-08 收官。批次 9 = **语音/电机/新无线七件**：前 4 件 A 类（jq8900 / syn6288 / fingerprint / l298n——mspm0 条目均已存在、本批零改动，全部补 **stm32 平台条目**、API 与 mspm0 `<slug>.h` 逐字对齐）+ 后 3 件 **B 类新 slug**（neo_6m / esp01s / ec01g——仅 stm32 条目、无 mspm0 对照、wordlist 补录）。规格落入 `.scratch/wiki-stm32-batch9/spec.md` + issues/01-07。

## 七件完成情况

| 工单 | slug | 形态 | 默认脚/实例 | 关键点 | 提交 |
|---|---|---|---|---|---|
| 01 | jq8900 | 软 UART TX | OUT=PA15（叠 BUZZER 提示输出互替） | delay_us(104)+gpio_set 位时序 + 0xAA+cmd+data+求和帧；只发不收不占串口实例；API 与 mspm0 11 函数同名同型 | 9e639b01 |
| 02 | syn6288 | 软 UART TX | OUT=PC14（叠 LED_YELLOW 输出指示互替；与 jq8900 错开） | 0xFD+Len(文本+3)+命令+参数+文本+异或帧 + 200 文本上限；NULL 补 stddef（UV4 首轮 3 错修复）；API 与 mspm0 6 函数同名同型 | 4d8f0e1e* |
| 03 | fingerprint | 真实 UART 帧协议 | UART_1/PA9-10（与 K230 身份识别互替）+ TOUCH=PB5 | 57600 重配；**isr.c 聚合扩展**（pinwriter `_UART_CALLS_ROLES` + `__weak` + USART1_IRQ_CALLS）中断状态机帧头 0xEF01FFFFFF + Len 大端精确收 12/16（页面 u2_recv_length 无上限越界修正）；API 与 mspm0 全族 10 函数 | * |
| 04 | l298n | PWM×2 方向互切 | IN1=PA6/IN2=PA7（TIM3_CH1/CH2 页面原脚） | L298N_PWM_PERIOD 2000u 宏对齐 mspm0（换算 500Hz/2000 步骤）+ 限幅；无 EN（跳线帽范围外）；notes：I2C 总线同脚冲突⚠ + TIM 门禁默认×默认不拦 | * |
| 05 | neo_6m | B 类 · UART + NMEA | UART_1/PA9-10（与 UWB 定位互替） | `$`→GPRMC→行尾收帧状态机（256 缓冲截断——页面 255 越界修正 + memcpy 无长度检查修正）+ get_position 出十进制度（ddmm 换算/N-S E-W 符号，手写浮点零标准库）；isr.c 聚合 | * |
| 06 | esp01s | B 类 · UART + AT | UART_1/PA9-10（与 HC05 手机遥控互替） | AT 透传（OK 命中/超时）+ parse_ipd（+IPD,id,len:data 有界解析）；**4 项缺陷修正**（%200 回绕→线性截断、IDLE 断串→按长度终结、无界扫→有界、buff[50]→200 上限）；AT 模板/MQTT 归骨架范围外 | * |
| 07 | ec01g | B 类 · UART + AT | UART_3/PB10-11（与 Zigbee/LoRa 无线互替） | AT 底座（NULL 入参守卫——页面 rev_buff=NULL 崩溃修正 + 有界扫）；%2096 回绕→线性截断；**页面无 GPS 代码**（GPS 归范围外）+ HTTP 天气/JSON demo 归范围外；isr.c USART3_IRQ_CALLS 首扩 | * |

（* 提交号为批 8 收尾之后的顺序提交，见 git log b84c16db..HEAD；每件「工单提交 + 自动 CHANGELOG」成对。）

## 基础设施改动

- `src/contest_generator/pinwriter.py`：`_UART_CALLS_ROLES` 增 4 条目（FINGERPRINT_UART / NEO_6M_UART / ESP01S_UART / EC01G_UART）。
- 母版 `library/masters/stm32/pin_config.h`：+46 宏（jq8900 2 + syn6288 2 + l298n 4 + fingerprint 8 + neo_6m 6 + esp01s 6 + ec01g 6）+ 默认 USART1_IRQ_CALLS=7 handler / USART3_IRQ_CALLS=2 handler。
- 母版 `library/masters/stm32/isr.c`：+4 `__weak` 空兜底 + 头部注释同步（+FINGERPRINT/批次 9）。
- `src/contest_generator/wordlist.json`：定位模块组 GPS 方案补 lib_modules（neo_6m）+ models NEO-6M；无线通信模块组 ESP8266 方案补 lib_modules（esp01s）+ models ESP-01S + 新增 EC-01G 方案 + models NB-IoT/EC-01G。
- `src/contest_generator/llm.py`：`WORDLIST_PROMPT_BYTES` 8120→8300（默认词表完整 wire 7994 > 7954 fit 上限——注释链第 15 次上调记录；词表段全量 +40B，全文预算不动）。
- 测试：test_pins STM32_MACRO_VALUES +46 宏 + CALLS 钉值更新；test_default_layout WHITELIST 9 组引脚登记（PA15/PC14/PA6/PA7/PA9/PA10/PB5/PB10/PB11——互替件同脚/输出互替/物理冲突⚠ 语义）+ conflict_groups_resolved 分组断言同步；test_pin_unlock_uart 重分组期望跟进（USART1 7 handler/USART3 debug+ec01g）；test_pin_bindings uart 共享组扩充（fingerprint/neo_6m/esp01s→UART1 家族 + ec01g→UART3 家族共享组测试）。

## 验证记录

- 七件 UV4 单选编译矩阵：**0 error / 0 module warning**（`C:/Keil5/Core/UV4/UV4.exe`，脚本 `.scratch/wiki-stm32-batch9/matrix/run_<slug>_matrix.py`，产物 `matrix/<slug>/build.log`）。
- 全量 pytest：**3802 passed, 0 failed**（批次 8 基线 3765 → +37；中途三轮红证已修：wordlist 预算红证→8300、模块独立性死依赖红证（esp01s/ec01g deps [] 修正）、普适性题词红证（l298n 头注释 2026H 中性化））。
- 一键快检：`.scratch/wiki-stm32-batch9/sweep_7_modules.py` **7/7 OK**（存在性/verified/hardware_bound/wordlist 挂接/kit+source_url 地阔星原页判据/依赖/宏值/pins 形状/代码守卫/notes needles/mspm0 存在性（A 类）/B 类无 mspm0）。
- mspm0 零改动：A 类 4 件仅 manifest description 双平台化；B 类 3 件无 mspm0 条目。

## 编码与评审发现（首轮 UV4 修复）

1. syn6288_stm32.c `NULL` 未定义（headfile.h 不含 stddef）——按 ball-detect-null-fix/01 先例补 `#include <stddef.h>`（UV4 3 错误）。
2. （预判先例复用：注释含 `*/` 提前终结块注释——本批头注释已按批 8 教训规避，零复发。）

## code-review 两轴

### 规格轴（子代理 7d4fe1d7 / 35722b1e 双独立评审，2026-09-08）

- 总体：核对点全部通过——4 件 A 类 API 与 mspm0 .h 逐函数同名同型（jq8900 11/syn6288 6/fingerprint 10/l298n 3+2000u）、46 宏值全对、聚合登记（_UART_CALLS_ROLES+4、__weak+4、USART1=7/USART3=2）、B 类三件仅 stm32+wordlist 补录、缺陷修正 7 项逐码验证、范围外全遵守（无 RX/EN/AT 模板/天气/GPS）。
- (a) 缺失：无实质缺失（spec 收尾清单闭环）。
- (b) 蔓延（均透明记录、判定合理）：① neo_6m 增帧头全串判定/字段显式长度（超工单 ①②）；② L298N_PWM_FREQ=500 新宏（工单只要求 2000u 宏）；③ fingerprint TOUCH=PB5 补脚（API 全族终校；notes 记录）；④ esp01s/ec01g deps=[]（工单原文即 []，delay_ms 走母版 ml_delay）。
- (c) 实现错误：**l298n_stm32.c set_duty 占空比刻度错位（硬错，已整改）**——ml_pwm 的 pwm_update 按 0~MAX_DUTY(50000) 归一化写 CCR（duty/50000×(ARR+1)，motor/servo 先例），首版把 0~1999 直传：duty=1999 仅 ≈4% 占空比（应 100%）、UV4 只验编译成假绿 → **已修复为 `l298n_pwm_duty()` 换算 duty×50000/2000 再传**（l298n_stm32.c + 测试守卫断言 + l298n 矩阵重跑 0/0）。
- 判例（已整改/记录）：① isr.c/pin_config.h 聚合段注释失实（仍写「UART_1=DIGIT+COORD+UWB+HC05」）→ **已补全 7 角色/4 件登记**；② esp01s send_cmd 仅匹配 OK（工单「OK/ERROR/+...」口径收窄）→ notes 已言明（超时/非 OK 返 0，失败语义等价），保留；③ neo_6m_get_position 返回码 0/1/2 工单未定义 → 头文件已文档化（调用约定补充）；④ 4 UART 件 notes 缺「UART 实例上限 3、4 件以上同选物理不可行」（spec⑥）→ **四件 manifest notes 已补录**。

### 标准轴（子代理 570be165，2026-09-08）

- **无硬违例**（提交信息全中文、头注释含立创 URL+版权、零引脚字面量/无 StdPeriph·printf 残留、注释 `*/` 配对 0 不平衡、`_UART_CALLS_ROLES`↔`__weak` 一一配对、wordlist 字段完整、测试风格与 test_module_hc05.py 模板一致、165 项相关 pytest + 矩阵 0/0）。
- 判例（已整改/记录）：① Duplicated Code——esp01s/ec01g 的 `_buf_contains`/rx_handler/send_cmd/available/receive 与 jq8900/syn6288 的 `_tx_bit/_tx_byte` 近乎逐字相同（模块自包含公约既定取舍——每件独立落盘、无共享助手先例，批 8 同款），**记录不整改**；② 注释漂移（同规格轴判例①）→ 已整改；③ fingerprint `_rx_head/_rx_expect` 命名弱项（与 hc05 同名异义）——模块内可读，记录；④ **防御不一致：ec01g 有 NULL 守卫、esp01s 无 → 已整改（esp01s send_cmd/send_string/receive/parse_ipd 补 NULL 守卫 + stddef include + 测试断言，esp01s 矩阵重跑 0/0）**。
- 两轴合计：**0 硬违例/0 遗留实现错误**；判例整改 4 项（l298n 刻度换算 + 注释漂移两处 + esp01s NULL 守卫 + 4 件 notes UART 上限）、记录保留 5 项（esp01s/ec01g 重复、fingerprint 命名、send_cmd OK 口径、neo_6m 返回码、生成侧既定量词）。

## 遗留

- 上板真机验证（七件）：软 UART 位时序 ~104us/bit、L298N 500Hz/2000 步骤占空比与方向互切、fingerprint 57600（市售 9600 出厂版差异）与帧状态机、neo_6m NMEA 字段/浮点换算、esp01s/ec01g AT 应答时序与固件版本差异——notes 均已注明「未上板」。
- 范围外（notes 记录）：jq8900/syn6288 RX 响应回读（真实串口方案）、l298n B 路（IN3/IN4/ENB）与 EN 引脚（跳线帽态）、NMEA 日期/时间/速度字段、ESP01S/EC01G AT 模板（CWMODE/CWSAP/CIPSERVER/MQTT）与 HTTP 天气/JSON demo、EC01G GPS 功能（页面无代码——官方 AT 资料另议）、软 SPI/总线真机标定。

## 已知偏差记录（透明化）

1. **fingerprint TOUCH=PB5**：工单「pins 2 行（TX/RX）」，实施终校 mspm0 fingerprint.h 全族（API 含 `fingerprint_is_touched` 需触摸脚）→ 补第 3 个 pin（gpio_in PB5——与编码器 A/称重/粉尘低频重叠、指纹门禁不同框）；manifest notes 与工单结论均记录。
2. **esp01s/ec01g deps=[]**：delay_ms 走母版 ml_delay（headfile.h 内嵌）；stm32 delay 模块 files=[]（声明依赖无法对应 include——模块独立性测试红线）；与 as32/ec11 B 类口径一致。
3. **l298n deps=[]**：无 delay 调用（纯 PWM）——工单写 ["delay"] 但 mspm0 manifest 现状 = []，按现状。
4. **l298n PWM 频率 500Hz/2000 步骤**：工单只要求 `L298N_PWM_PERIOD 2000u` 宏对齐 mspm0；换算细节（页面 1kHz/1000 步骤 vs mspm0 16kHz/2000 步骤差异）实施自定——notes 记录真机可调 L298N_PWM_FREQ。
