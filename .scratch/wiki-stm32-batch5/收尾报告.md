# wiki-stm32-batch5 收尾报告（2026-09-06）

## 完成情况

| 工单 | slug | 形态 | 默认脚 | 矩阵 | verified |
|---|---|---|---|---|---|
| 01 | mq2 | ADC 薄封装（页面 ADC 序列收敛 ml_adc） | AO=PA5 | 0/0（UV4） | true |
| 02 | mq135 | ADC 薄封装 | AO=PA5 | 0/0（UV4） | true |
| 03 | mq5 | ADC 薄封装 | AO=PA5 | 0/0（UV4） | true |
| 04 | photoresistance | ADC 薄封装（**反向自洽保留**） | AO=PA5 | 0/0（UV4） | true |
| 05 | rain | ADC 薄封装（**方向修正核心**——页面反向式 vs 正文矛盾 → 正向） | AO=PA5 | 0/0（UV4） | true |
| 06 | s12sd | ADC 薄封装 + 档位表 0-11 | SIG=PA5 | 0/0（UV4） | true |
| 07 | soil | ADC 薄封装 | AO=PA5 | 0/0（UV4） | true |
| 08 | gp2y1014au | ADC 薄封装 + LED 脉冲时序（器件必需） | AO=PA5 + LED=PB5 | 0/0（UV4） | true |

- 批次 5 全部 8 件完成（01-08 工单 status = resolved）。8 件默认 AO 全 = ADC_Channel_5（PA5——页面原脚即共读点），**ADC 共享组**（与 flame 共读 PA5 + 白名单 PA5 组登记；同一物理脚只能接一件器件，多件同测需外部分路器/分时切换——notes）；gp2y LED 默认 **PB5**（页面 PA2=DEBUG_UART TX 不照抄——白名单 PB5 +1）。

## 测试与一致性

- 全量 pytest：**3686 passed**（批次 4 为 3654；+32 = 8 测试文件 stm32 扩展 + 10 宏表 + 白名单断言微调）。
- 一致性快检 sweep_8_modules.py：**8/8 OK**（含 rain×photo 成对守卫 / gp2y 时序守卫 / s12sd 档位表守卫 / 无 DO / 宏值 / 白名单 / wordlist / mspm0 零改动面）。
- UV4 矩阵八件：单选生成 → UV4 真编译 **0 error、0 module warning**（exit 0）；build.log 存 `.scratch/wiki-stm32-batch5/matrix/<slug>/`。
- wordlist 零补录：8 slug 已在 wordlist.json 的 lib_modules（mspm0 条目同 slug 挂接），diff 零词表改动。
- mspm0 侧零改动：八件 mspm0 文件/配置未动（评审 + sweep 实证）。

## code-review 两轴（固定点 c88e36b2 = 批次 4 收官；随机抽 s12sd/gp2y1014au 深审 + 其余 6 件结构对仗——mspm0 批 11 裁决沿用）

### Standards 轴（编码标准）
- **0 硬违规**（ADR 0009 纯驱动切片 / ADR 0010 引脚宏单源 / ADR 0005 简介判据 / 语言规范全中文）。
- 判断项 5 条：① 工单 06 实施清单 dependencies 误写 ["adc","delay"]（自 08 拷贝遗留）——**已整改**为 ["adc"]；② s12sd 11 级 if 级联（Repeated Switches）——保留（mspm0 同形 + 页面阈值表原式 + 测试逐档钉死）；③ gp2y filter 参数名 m（页面 Filter 原型名遗留）——保留（与 mspm0 对齐）；④ gp2y led_on/led_off 单行转发（Middle Man 边缘）——保留（承载极性语义，与 mspm0 对齐）；⑤ gp2y 电平字面量 0/1——保留（有极性注释佐证，与 mspm0 对齐）。

### Spec 轴（规格忠实）
- **零硬违反**：8 工单 checkbox 全部对应产物在 diff；spec「测试决策」全落地（模板/10 宏/白名单/矩阵/verified）；rain×photo 成对守卫对仗成立（rain 无「1.0f - 」、photo 有）；s12sd 档位表逐档与页面一致；gp2y 与 mspm0 .c 逐行对齐（LED 极性经 ml_gpio.c 实证、5 次采样 ≈100us<280us 亮窗）；DO 全不声明；mspm0 条目零改动。
- 范围蔓延：无（CHANGELOG chore 为管线惯例）。
- 1 条流程性提示：sweep/矩阵脚本与 CONTEXT 未随批提交——本次收尾提交一并落定；spec L60「["adc","delay"] 类」措辞已按 mspm0 现状澄清（7 件 ["adc"]、gp2y ["adc","delay"]）。

## 附带产物

- `.scratch/wiki-stm32-batch5/matrix/run_{mq2,mq135,mq5,photoresistance,rain,s12sd,soil,gp2y1014au}_matrix.py`（8 个矩阵脚本，与批次 2/4 同配方）。
- `.scratch/wiki-stm32-batch5/sweep_8_modules.py`（批次 5 一致性快检）。
- CONTEXT.md 平台行补录「stm32 线批次 5」块（ADC 薄封装群一：收敛 ml_adc/共享组/方向修正成对守卫/档位表/LED 时序/宏表/矩阵 0-0）。

## 遗留（后续）

1. 双平台模块 description「mspm0 纯驱动」措辞批量更新（batch4 备注延续——建议并入批次 6 收尾）。
2. 上板真机验证（八件全部未上板——notes 已注明：百分比相对值/粉尘 0.17 系数/档位表阈值真机校准留后续）。
