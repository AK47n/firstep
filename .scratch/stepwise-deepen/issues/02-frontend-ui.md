# 02 — 前端：进度总览 + 步骤报告 + 卡片「下一步要做」（全链路前端）

**要做什么：** 任务推进区升级为「逐步深化」可视化：顶部进度条 + 状态汇总（已完成/待上板/失败/进行中/待做/已跳过）；每步执行结果面板新增「步骤报告」区块（AI 做了什么 + 接下来你要做什么，含接线/上板指引，空值有中文兜底文案）；任务卡上持久显示「接下来你要做什么」摘要，直到该步上板确认；轮次历史行附「做了什么」摘要；入口标题与说明文案更新为「逐步深化（任务推进）」。

**被谁阻塞：** 01 — 步骤报告 LLM 操作 + 轮次字段 + 进度事件（后端全链路）。

**状态：** resolved

- [x] 前端纯函数（fx/task.js）：进度总览渲染（按状态分段的进度条 + 汇总文案，口径：已完成=verified、待上板=unverified、已跳过单列不计完成、进行中/失败/待做分列）；步骤报告渲染（两块：AI 做了什么 / 你接下来要做什么，空值中文兜底）；卡片「下一步要做」粘性摘要（最新一轮 user_action 非空且该步未上板确认时显示）；轮次历史行追加「做了什么」摘要（截断显示）。
- [x] 既有 `tasksProgressText`（进度 N/M）保持原口径与文案不变（既有测试不得翻红）。
- [x] 任务卡渲染集成「下一步要做」摘要；任务网格集成进度总览（新增容器）。
- [x] 单任务执行结果面板插入「步骤报告」区块（位于验证状态与反馈备注之后、diff 之前）；监听 `task_reporting` 事件显示「AI 正在总结本步…」状态文案（可选增强，不得破坏现有事件处理）。
- [x] 页面装配：任务推进区标题改「逐步深化（任务推进）」、说明文字更新（拆解 → 逐任务执行 → 每步编译验证 → 上板反馈）、新增进度总览容器与样式；「直接深化（跳过修订）」按钮文案补快速兜底含义，功能不变。
- [x] tests/js：新增断言（进度总览分段与汇总文案 / 步骤报告渲染与兜底 / 卡片下一步摘要显隐 / 轮次历史摘要 / taskCardHTML 集成）；既有断言保持绿（含「进度 2/3」）。

**答复（code-review 双轴评审整改）：**
- Standards 轴：`truncate`（fx/core.js 既有共享件）替换三处内联 40 字截断；新增私有 `lastIteration(task)` 收敛「取最新一轮」（taskLatestFeedbackNote / taskStepReportHTML / taskNextActionHTML 三处）；进度条去掉不贴切的 `role="progressbar"` + aria-*（分段分布条非单调填充，视觉恒满）；generate-tasks.js 头注释事件词表镜像补 `task_reporting`；两处跨簇重置级联保留（与既有模式一致，纯一致性复制）。
- Spec 轴：任务卡 unverified 状态新增「待上板」徽章（spec「总览与卡片都标注待上板」）；步骤报告空结果兜底文案改为中性（「本步说明为空（报告未生成或 AI 未总结）」——空结果 ≠ 必然降级，spec 允许字段为空）。
- 全量 JS 测试 530 passed（含既有「进度 2/3」与新断言）。

**提交：** 见 git 历史（工单 02 提交）。
