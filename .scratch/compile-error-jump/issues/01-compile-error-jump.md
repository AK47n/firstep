# 01 — main.c 错误行点击跳转预览定位（前端纵切 + 测试）

**要做什么：** 修复中心错误列表点击 main.c 的错误行 → 页面滚到 main.c 预览卡（折叠自动展开）+ 预览内选中该行（浏览器原生滚动定位）+ 选区高亮样式持续显示；再点其他错误行选区切换；非 main.c 文件错误行维持现状（展开源码行片段）；预览为空时 toast 提示不跳转。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [ ] 行偏移纯函数（第 N 行行首/行尾字符偏移，空行与末行正确）+ main.c 路径判定纯函数（`main.c` / `./main.c` / 绝对路径命中，其他文件不命中）
- [ ] 错误行点击分派：main.c → 跳转定位；其他 → 现状展开源码行（回归不变）
- [ ] 跳转：展开折叠卡 + `scrollIntoView` + `setSelectionRange` + `::selection` 高亮样式
- [ ] 预览为空（未生成骨架）→ toast 提示不跳转
- [ ] tests/js 纯函数单测绿（正则抽取先例）+ headless 冒烟探针断言选区偏移与卡片可见
- [ ] 全量测试绿；CHANGELOG 中文记录
