# 02 — 全量重抓 70 篇 + 质量扫描 + 索引重建

**要做什么：** 用新转换器 `--force` 全量重抓地猛星批次 70 篇手册（后台任务），
重建 `模块索引.md`/`网盘索引.md`；质量扫描脚本对全部产物断言可读性
（无 token 逐行 / 无行号残留 / 无 `\u200b` / 围栏行数非零 / 章节结构存在），
并把「素材批次内容已修复 + 重抓方式」记入 CONTEXT.md。

**被谁阻塞：** 01（转换器与单页验证先行）。

**状态：** resolved

**结论：** 2026-09 完成。全量 `--force` 重抓 70 篇成功（0 失败，后台任务，日志 refetch-full.log）；质量扫描 70/70 通过 0 问题（批次内 md 数 72 = 70 页 + 2 索引；索引行数 70 一致）；跨分类抽查 4 篇（sensor/rf/screen/control 各一）人工过目；图片引用 10 处（10 个带图页面）全部存在零缺失；CONTEXT.md Markdown 资料库行已更新（wiki_md 转换器 + 资产端点 + 重抓事实）。

- [x] 全量重抓成功（失败页数 0，重试语义保持现状）。
- [x] `模块索引.md`/`网盘索引.md` 重建，与 70 篇页面计数一致。
- [x] 质量扫描（`.scratch/wiki-materials/scan_quality.py`）跑通：70 篇全过断言，
      输出扫描报告（每篇 代码块数/图片数/是否含异常形态）。
- [x] 抽查 ≥5 篇（不同分类）产物人工过目：像真文档。
- [x] CONTEXT.md 更新素材批次行（内容质量修复 + 重抓命令记录）。

- [ ] 全量重抓成功（失败页数 0，重试语义保持现状）。
- [ ] `模块索引.md`/`网盘索引.md` 重建，与 70 篇页面计数一致。
- [ ] 质量扫描（`.scratch/wiki-materials/scan_quality.py`）跑通：70 篇全过断言，
      输出扫描报告（每篇 代码块数/图片数/是否含异常形态）。
- [ ] 抽查 ≥5 篇（不同分类）产物人工过目：像真文档。
- [ ] CONTEXT.md 更新素材批次行（内容质量修复 + 重抓命令记录）。

## 真机项集中挂账（2026-09-09 在途盘点）

- 本单仍未勾的验收项属**真机工具链 / 浏览器 CDP / 真实 LLM 额度 / 人工取源 / 历史流程**类，
  已集中到 `.scratch/real-acceptance/issues/01-real-machine-acceptance.md`（那里不写代码，
  验完一项回勾本单对应项即可）；后续盘点不再逐张重判这些项。
