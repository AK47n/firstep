# 03 — Toast 轻通知

**要做什么：** 右上角 toast 组件（ok 青 / error 红 / info 灰，2.5s 自动消失，最多 3 条，可点关闭），接入关键成功/失败事件；行内提示保留。

**被谁阻塞：** 无（与 01/02 独立）。

**状态：** resolved

- [x] `#toast-root` 容器 + `toast(kind, text)`（ok 青 ✓ / error 红 ✕ / info 灰 ℹ；插入、超 3 条丢最旧、点击关闭、2.5s 自动移除、`&`/`<`/`"` 转义防注入）+ 入场/出场动画
- [x] 接入 9 处：上传抽取成功/失败、取题面成功、生成完成/失败、编译通过、交接复制成功、修订完成、深化完成（行内提示全部保留）
- [x] CDP 验证：叠 4 丢 1（3 条）、点击关闭（3→2）、自动消失（→0）、XSS 转义（`&lt;b&gt;` / `&amp;`）；截图目检（三色堆叠、无重叠）通过
