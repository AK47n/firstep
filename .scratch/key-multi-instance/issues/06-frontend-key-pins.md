# 06 — 前端：实例选脚候选按模块首 pin 类型（gpio_in）

**要做什么：** key 实例在板图上能正确选脚：实例选脚模式（点板图 IO 脚直接绑
目标实例）的候选与提示文案从硬编码「gpio_out 能力」改为按模块首 pin 角色类型
（key = gpio_in，led = gpio_out 不变）；key 多实例卡自动出现并可增删改、改名、
回填，实例随 expanded 全集出入。

**被谁阻塞：** 02（manifest 生效即前端自动识别多实例）。

**状态：** ready-for-agent

- [ ] 板图选脚候选按模块多实例的首 pin 角色类型过滤（key 核心候选 = gpio_in 能力脚；
      led 仍 = gpio_out）；选脚提示文案不再写死 gpio_out。
- [ ] key 实例卡：添加 / 删除（上限 8 超限禁用）/ 显示名 / 变体（自由文本，显示
      「变体 = function」）/ 板图选脚 / 删除实例清选脚目标，全部可用。
- [ ] AI 推荐回填 / 平台默认预填（按键 × 1）经既有管线出现在 key 实例卡；模块移除
      后实例清单清理不残留；依赖带入（如将来 beep→…，不涉及本特性）形态不回归。
- [ ] led 实例全部既有行为零回归（选脚候选、默认预填、增删改）。

**验收标准备注：** 本单为前端行为验收（手工 + 现有前端纯函数单测增量）；
域层校验（backend 400）不在此单。
