# 第十六轮待修项 · 新会话提示词（直接整段复制）

用法：**一单一会话**。下面每段都是自包含的——复制粘贴即可，不用再补上下文。
会话档位（CLAUDE.md「Session handoff preference」口径）：标 **max** 的走 max，其余 high。

> 公共收尾约定（每段提示词里都重复了一遍，不必手改）：
> 全量 `$env:PYTHONPATH='src'; python -m pytest -q` + `node --test tests/js/*.test.mjs` 绿；
> 勾该工单验收项；在 `.scratch/tracker-audit/2026-09-09-在途盘点.md` 追加一段收口；
> git 提交信息用中文（钩子拒英文），**不要 push**。

---

## ① 单 02 — mspm0 编译判读缺口（🔴 先做这个，档位 max）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/02-mspm0-compile-verdict-parse-gap.md 全文。

背景一句话：2026H/mspm0 真机编译 exit=2、SysConfig 报 7 条 Resource conflict，而产品的
parse_compile_errors 返回 0 条、summarize_compile_output 报 {0,0}——「编译失败」被报成「0 错 0 警」，
修复链还把原始输出当「未定位到可修复文件」白跑一轮。这是真机编译验收的判读底座，先修它。

红证文本已在盘：.scratch/real-run/verify-16-A8-mspm0-2026H-buildlog.txt（真机原样输出）。
相关现场：.scratch/real-run/verify-16-A9-stm32-2026C-reuse.txt（stm32 线判读正常，作为对照）。

任务按工单「修复方向」三条做：① _UV4_SUMMARY_RE 放宽大小写让 summary 至少说真话；
② 新增 SysConfig 冲突行识别（error: NAME(/ti/driverlib/XXX) ... Resource conflict），
产出能列出「DC_MOTOR × SERVO_PWM 抢 PA7」这类人话冲突的条目；③ 修复链识别「配置级冲突」
（没有源码可改）不要喂给 /api/fix-errors 白烧一轮，给用户指路（改引脚绑定 / 去冲突模块）。
红证先行：把那段真机输出喂 parse_compile_errors / summarize_compile_output，现状 {0,0} 即红。

文件边界：src/contest_generator/fix_errors.py（解析域单源）+ 必要的调用方（compile_runner / webapp 的错误列表装配）
+ tests/test_fix_errors.py / tests/test_generate_check_contract.py；前端若要展示配置级冲突再加 static/js + tests/js。
不要另写平行正则（判读单源在 fix_errors）。

服务前置（本机已在跑，别重启）：webapp 127.0.0.1:8000（若端口空：$env:PYTHONPATH='src'; python -m contest_generator.webapp）。
真机工具链都在盘：UV4 C:\Keil5\Core\UV4\UV4.exe；gmake C:\ti\ccs2050\ccs\utils\bin\gmake.exe；
mspm0 生成产物 .scratch/real-run/out_2026H_mspm0（现成的、会复现这 7 条冲突的工程）。

真机验收：python .scratch/real-run/generate_check.py --topics-dir library\topics --modules-dir library\modules
--platform mspm0 2026H（要用真实推荐就带 --reuse-recommend，缓存已在 .scratch/real-run/cache/），
期望轮次文案与错误列表都能看到 7 条冲突，而不是「0 条 Warning」。

收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；勾工单 02 的验收项；在
.scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口（第十七轮，写清红证/改动/真机结果/基线）；
回勾 .scratch/real-acceptance/issues/01-real-machine-acceptance.md 的 A8（mspm0 线）；
git 提交信息用中文，不要 push。
```

---

## ② 单 03 — 推荐域拒绝文案 + 一次可恢复重试（🟠，档位 max）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/03-recommend-domain-reject-opaque.md 全文。

背景一句话：SelectionError（「模块 oled 不支持多实例，不能带 instances」「库外建议的硬件名不在硬件词表中：X」）
在 llm.py:1953 被译成 LLMError(kind="client")，errors.py 的 client 分支不看 message 一律换成
「AI 服务拒绝了本次请求（可能是 API key 无效、账户余额不足…）」，且 _retry_parse 对 client 直接 break
——一次模型手滑 = 整次推荐终态失败 + 文案指错方向。本轮 14 轮真实推荐里 9 轮栽在这里。

真实理由已逐条留档：.scratch/recommend-domain-reject/verify-16-recommend-*.txt 里的 [PROBE16][域拒绝] 行
（探针 .scratch/recommend-domain-reject/probe-16-recommend-live.py 用内存注入诊断行取证，磁盘 llm.py 零改动，
可复用）。

任务按工单「修复方向」：① 文案必做——让「本地域判决」与「上游 HTTP 4xx」在 errors.py 可区分，
域拒绝保留 LLMError.message 原文（+ 一句人话引导），别再用通用话术；② 给域拒绝 ≥1 次带理由的重试
（重试请求里带上被拒理由作为追加指令），上限 1 次防烧钱；③ 词表闸「降级而非拒收」先只做评估与建议
（会动 SelectionError 语义边界，需先确认与 wordlist / 买件指引链路口径一致，不确定就在工单里留决策点）。
红证先行：tests/test_llm.py 补「首轮域拒绝 → 次轮修正通过」用例；errors.py 的表驱动契约测试补两分支。

文件边界：src/contest_generator/errors.py（文案与映射表）+ llm.py（_retry_parse 与域拒绝翻译点）
+ 必要的 selection.py（若采纳降级方向）+ tests/test_llm.py / tests/test_errors*.py。

真机验收：python .scratch/recommend-domain-reject/probe-16-recommend-live.py --topic 2022C --platform stm32 --attempts 3
（本题历史上连续 3 次被词表闸拒绝）——期望要么自愈收敛，要么失败文案带真实理由。
另跑 --topic 2026H --platform mspm0 复核（历史上被「huidu/oled 不支持多实例」拒过）。

收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；勾工单 03 验收项；在
.scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交中文，不要 push。
```

---

## ③ 单 04 — 推荐层互斥组收敛（🟠，档位 high）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/04-exclusive-group-recommendation.md 全文。

背景一句话：提示词（recommend-exclusive-groups/03）已写「同组互斥，同一题内只推荐一个」，
但 2026C/stm32 真机推荐把 zigbee-rx 组的两名成员（zigbee_uart + zigbee_link）都推荐了，
一路走到 /api/generate 才 400——用户点「生成」才炸。真实载荷在
.scratch/real-run/cache/recommend_2026C.json（exclusive_groups[0].recommended 两个成员）。

任务：在解析层做确定性收敛（首选，零额外额度）——build_module_selection 遇到同组多成员时
保留一个（依据模型的 reason / requirements 引用或清单顺序），其余剔到「同组候选（未选中）」，
落进 done 载荷的可见字段（别静默丢弃）；exclusive_groups[].recommended 语义收紧为「组内 ≤1」。
生成门禁保留作兜底，不动 generator.py 的门禁。

红证先行：用上面那份真实缓存载荷做 fixture 喂解析层，现状两成员都进 selected 即红。

文件边界：src/contest_generator/selection.py（解析 + 载荷组装）+ 需要时的前端选择卡渲染
（static/js，选择卡已有 _group_card 的 recommended 字段）+ tests/。

真机验收：python .scratch/real-run/generate_check.py --topics-dir library\topics --modules-dir library\modules
--reuse-recommend 2026C —— **不带 --drop** 期望跑绿（现状必须 --drop zigbee_link 才能生成）。

收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；勾工单 04 验收项；在
.scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交中文，不要 push。
```

---

## ④ 单 05 — 请求预算账本复算（🟡，建议与 03 同批，档位 max）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/05-request-budget-reality-gap.md 全文。

背景一句话：budget.py 注释推算「推荐请求最坏形态 ≈119.5KB」，但真机实测 2021F 单次 select
就是 122161 字节（上限 MAX_REQUEST_BYTES=128*1024，余量 6.8%），加一段澄清历史后 124475；
模型点名读参考全文那条路实测 149674 字节被预检拒发。账本与实测不符 = 后续任何加提示词段的改动都会先撞上限。

实测数字来源（含逐段记账）：.scratch/recommend-vision-qa/verify-16-C1-clarify-vision.json 的 request_sizes
（探针 .scratch/recommend-vision-qa/probe-16-clarify-vision.py 在 Transport 层记账）。

任务按工单三条：① 把最坏形态推导改成**可执行**的结构测试（按真实库 86 条瘦身摘要行 + 2021F 题面
拼最坏载荷，断言 ≤ 上限 − 目标余量），注释里的手算被实测数字替换；② 段级预算重分配（基础段先扣，
剩余才给参考全文/澄清历史，REFERENCE_FULLTEXT_BYTES 与 CLARIFICATION_HISTORY_CAP 随实测重取）；
③ 贴边（≥90%）与超限时留下可判读信号（telemetry 记账「哪一段占了多少」），别只剩一个总字节数。

文件边界：src/contest_generator/budget.py + llm.py（段预算与发送前预检）+ tests/test_llm.py 等预算测试。
不要动 SSE / 前端 / fix_errors。

真机验收：python .scratch/recommend-domain-reject/probe-16-recommend-live.py --topic 2021F --platform stm32
（会带澄清历史与参考全文注入路径）——不再出现「请求体过大」。

收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；勾工单 05 验收项；在
.scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交中文，不要 push。
```

---

## ⑤ 单 06 — CCS 三件套混搭说明（🟡 热身单，档位 high）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/06-ccs-toolchain-mix-and-match.md 全文。

背景一句话：compile_runner 的三件套各取目录名最大，于是本机探到「编译器在 ccs2050、
SDK/SysConfig 在 ccs2051」的跨安装目录组合（生成工程 Debug/makefile 逐字引用这三条路径，
实测能编过），但环境体检只报 found/path，不说明这一点，用户按「一个 CCS 版本 = 一套工具链」排查会走偏。

任务：① 体检文案补一句「三件套独立探测，可能来自不同 CCS 安装目录」（可给本机实况举例）；
② 可选增强：把每件的安装根一并带出；③ 若认为跨目录混搭不安全，走「同根优先」探测改动前
**先在工单里记下这是行为变更、需要用户拍板**，别自己改。

文件边界：src/contest_generator/static/js/fx/env.js（文案）+ compile_runner.py（仅当采纳安装根/同根探测）
+ 相应文案守卫测试（照 tests/ 现有 env 文案先例）。

验收：体检页能看到说明；本机三件来源与工单表格一致（编译器 ccs2050 / SDK ccs2051 / SysConfig ccs2051）；
默认不改探测行为。

收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；勾工单 06 验收项；在
.scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交中文，不要 push。
```

---

## ⑥ 单 07 — 浏览器验收姿势坑固化（🟡，可与任一单合并，档位 high）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/07-browser-acceptance-pitfalls.md 全文。

背景一句话：第十六轮 B24 浏览器段三次红/挂，全是脚本姿势问题而非产品缺陷——四个坑：
① 第 11 步卡默认折叠 + 卡内页签式（#revise-tabs .revise-tab[data-tab="revise"]），不展开不切页签时
内部元素 display:none，fill/click 等 30s 超时；② playwright waitForFunction(fn, arg, options) 的
超时必须放第三个参数，放第二个会被当 arg → 默认 30s（表现为「写了 15 分钟却 30 秒红」）；
③ page.evaluate 里 await 长流程 = 单次 CDP 调用挂几十秒（分析实测 28s）→ 要「kick off 不 await +
node 侧轮询 DOM」；④ reviseApply / reviseRollback 各有一层 confirmModal，不点确认 POST 根本不发
（服务端日志只有 analyze 就是这个原因）。

现场证据：.scratch/revise-deepen/verify-16-revise-{live,render}-run.txt、verify-16-revise-analyze-trace.txt、
verify-16-revise-hang-diag.txt（诊断探针证明 kick off + 轮询时页面一切正常：t+28s 分析完成、零 pageerror）。

任务：① 在 .scratch/cdp-harness.mjs（或新增 .scratch/browser-harness.mjs）加 expandCard(page, cardId, tabSelector)
与 pollUntil(page, fn, {timeoutMs, every}) 两个助手；② 挂账单 01 的「B 组统一前置」段落补四坑清单
（逐条一句 + 正确姿势）；③ 把 .scratch/revise-deepen/verify-16-revise-render.mjs 改为使用新助手作为回归样本。

文件边界：.scratch/ 共用件与脚本 + .scratch/real-acceptance/issues/01-real-machine-acceptance.md 的 B 组前置段；
产品代码零改动。

验收：改后的 B24 脚本真机复跑仍全绿（19 项，含分析渲染 / 执行 SSE / 回滚 149 项 sha 复原）；
node --test tests/js/*.test.mjs 绿（若新增纯件测试）。

收尾：勾工单 07 验收项；在 .scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；
git 提交中文，不要 push。
```

---

## ⑦ 人工项会话（不需要改代码，档位 high；也可以你自己做，不用开 agent）

```
读 CLAUDE.md、CONTEXT.md，然后读 .scratch/real-acceptance/issues/00-待修清单-新会话入口.md 的「三、需要人」段。

我（用户）来做下面这几件需要人出面的事，你负责：把工程准备好、把步骤和期望结果列清楚、
把结果落盘归档并回勾工单。具体：

1) A4 Keil5：打开 .scratch/real-run/out_2026C_stm32/user/Project.uvprojx → F7 Build，
   期望 0 Error(s), 0 Warning(s)（命令行 UV4 已绿，见 .scratch/real-run/verify-16-A9-stm32-2026C-reuse.txt）。
2) A5 CCS：导入 .scratch/real-run/out_2026H_mspm0 → Build，期望复现命令行那 7 条 SysConfig 引脚冲突
   （这是交叉印证单 02 的现场，不是回归）。
3) A7 CCS Theia：打开 .scratch/real-run/out_16_mspm0_min（最小工程，命令行 0/0）→ GUI 编译复验。
4) D1：6 个器件（beep / ir_beam / key / led / led_beep / step_motor）的 kit + source_url 由我给，
   你按 .scratch/identity-fields/issues/06-pending-identity-sources-human.md 的「关闭流程」收尾
   （填 manifest → 移出 IDENTITY_BACKLOG → 删对应 xfail → 跑 .scratch/library-audit/audit.py 对账 → 全量 pytest）。
5) E1：干净机器 install.bat → .venv → start-app.vbs 全流程（我在另一台机器上跑，你给操作清单与期望输出）。
6) E2 超时态：需要劣化环境，我按你给的配方造。

每做完一项：结果落盘到对应 .scratch/<slug>/verify-*.txt（含截图路径或控制台原样输出），
回勾 .scratch/real-acceptance/issues/01-real-machine-acceptance.md 对应项 + 来源工单该项；
全部做完后在 .scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交中文，不要 push。
```

---

## 附：不在工单里的两项（供你决定要不要做）

- **生成期引脚冲突门禁**（SysConfig 语义级 `associatedPins` 互斥，现门禁覆盖不到）：
  已作为单 02 的附件事实记录，**未独立开单**——修复方向未定，等 02 落地后再看。
- **O-2 澄清门停滞**（同题两次跑，一次被问一次不问）：非产品缺陷（CLI 脚本语义 + 模型概率性），
  已并入单 07 的「真机验收怎么不被它拖住」。
