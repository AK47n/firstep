# 真机挂账单 · 第十六轮计划（待用户确认）

> 依据：`.scratch/real-acceptance/issues/01-real-machine-acceptance.md`（A/B/C/D/E/F 六组）
> + `.scratch/tracker-audit/2026-09-09-在途盘点.md` 第十四/十五轮。
> 现状盘点：**未勾 26 / 已勾 24**（B 组未勾 3：B24 剩余 / B26 / B27；A 组 11 全未勾；
> C 组 5 全未勾；D 2；E 2；F 3 仅登记）。

## 0. 本轮开工前的实测（不是凭记忆，已逐条探测）

| 项 | 实测 | 影响 |
|---|---|---|
| UV4 | `C:\Keil5\Core\UV4\UV4.exe` 在盘 ✓ | A1/A8/A9/A10 可跑 |
| gmake | `C:\ti\ccs2050\ccs\utils\bin\gmake.exe` 在盘 ✓（自动探测 `C:/ti/ccs*/ccs/utils/bin/gmake.exe`） | mspm0 编译可跑 |
| CCS 编译器 | `C:\ti\ccs2050\ccs\tools\compiler\ti-cgt-armllvm_4.0.4.LTS` ✓ | makefile 集可生成 |
| MSPM0 SDK | `C:\ti\mspm0_sdk_2_11_00_07`（含 `.metadata/product.json`）✓ | 同上 |
| SysConfig CLI | `C:\ti\sysconfig_1.20.0\sysconfig_cli.bat` ✓ | 同上 |
| webapp | `127.0.0.1:8000` 监听中（`python -m contest_generator.webapp`，PID 54656/55268） | 全链走它 |
| 配置 | `~/.contest_generator/config.json`：主 key 已配、`vision_api_key` 空（按仓库口径复用主 key）、`uv4_path`/`gmake_path`/CCS 三件套均空（走自动探测） | C1 可跑但要烧额度 |
| **推荐缓存** | `.scratch/real-run/cache/` **不存在**（零缓存） | **A9/A10 的 `--reuse-recommend` 无缓存可复用，必须先跑一次真实推荐 2026C 落缓存** |
| **题库路径** | `generate_check.py:31` 仍写 `~/.contest_generator/topics`（**已不存在**）；题库现落 `library/topics/`（2026C / 2021F 都在） | **A9/A10 现在直接跑必然读题面失败**，需要先改脚本口径（见下「必需的小修」） |

## 1. 本轮计划

### A 组（11 项）— 打头

**CLI 可跑（5 项，本轮做）**

| 项 | 怎么跑 | 口径 |
|---|---|---|
| A1 | `python .scratch/b1-adc-servo/compile_matrix.py` | 4 例（default/bound × stm32/mspm0）0 error 0 warning + 门禁同闸 |
| A8 | `generate_check.py --topics-dir library/topics 2026C`（stm32）+ `--platform mspm0`（2026C）各一次真实链路 | 真机 UV4 / gmake 0 Error；mspm0 骨架用 2026C 题面（不是 2026H——见「口径修订候选」） |
| A9 | `generate_check.py --topics-dir library/topics --reuse-recommend 2026C` | 门禁全过（产物树语料跑生产 `run_generation_gates`） |
| A10 | 同 A9（缓存复用），并在产物里注警一次以驱动第 2 轮 `previous_fixes` 回喂 | CLI 全链含修复轮回喂 |
| A11 | 临时把 `compile_runner.COMPILE_TIMEOUT_SECONDS` 调到 1s（**不改仓库文件**：探针内 monkeypatch / 直接用 `run_compile_command(timeout=1)`），跑首编 → 断言 `timed_out=True` + 脚本「初次编译超时，已停止」即停 | 首编超时 = 终端状态，不烧修复轮 |

**必须 GUI（1 项，需用户点一次）**

- A4（Keil5 打开生成工程 → F7 编译）、A5（CCS 导入 mspm0 工程 → Build）、A7（CCS Theia 打开
  + GUI 编译复验）——三个动作都在 IDE 里。我会把工程目录、导入步骤、看什么写在
  「需用户点一次」清单里，用户点完把截图/输出贴回，我落盘归档。
  （A4 与 A8 的 stm32 线内容重叠、A5 与 A8 的 mspm0 线重叠——A8 绿了可显著降低 GUI 项的不确定性，
  但 GUI 项本身是「IDE 里能不能打开并编译」的独立事实，不能互相顶替。）

**可跑但本轮先不做（1 项，等确认）**

- A6：2024H → 桌面 `2024H_Auto_Car_MSPM0` + gmake exit=0 + `.out`。需要 1 次真实推荐 + 1 次骨架；
  且会**扫描桌面已存目录做冲突裁决**（当前桌面无 `2024H_Auto_Car*`，会新建目录）。额度够就做，
  否则顺延。

**A2 / A3（浏览器 + 真编译，各 1 项）** —— 本轮并入 B26 一起跑（同一段修复中心流程）：
CDP 起 Chrome headless → 打开生成页 → 选真实工程目录 → 一键编译修复（注入 1 处警告）→
断言横幅四态（未编译 / 编译中 / 通过 / 失败）+ 错误列表 + 终态「0 错 0 警」。

### C 组（5 项）— 随后，**每批开跑前先报预计调用次数与额度**

| 项 | 内容 | 真实 LLM 调用 |
|---|---|---|
| C1（= B27 主链路 + `recommend-vision-qa/03`） | 2021F 库内条目 + ccs 平台走 `/api/recommend`：真视觉主链路（图内尺寸类澄清被自动消化）+ 三条降级路径（无视觉配置 / 粘贴无图题面 / 视觉答不上） | 4 次推荐链路（每次 2–4 轮 + 可能补问） |
| C2 | 重启服务后 2024H 推荐 → 不再问「起始方向 / 声光形式 / 几路」 | 1 次推荐链路 |
| C3 | 超大中文上下文打 `/api/fix-errors` → 不再「请求体过大」 | 1 次修复调用 |
| C4 | 真机贴文本修复一次（修复中心 → 事件流 → 回滚） | 1–3 次修复调用 |
| C5 | AI 推荐抽验 2026F / 2022C / 2026C（多实例猜测合理性） | 3 次推荐链路（只到推荐，不生成） |

### B 组尾巴（3 项）

- **B27**：与 C1 同批（2021F）。五条子项里「未配 key 400」可离线验（临时把 key 置空 → 断言 400 中文），
  其余四条随 C1 一起跑。
- **B26**：浏览器注入未用变量 → 自动续跑修复轮 → 复编到 0 错 0 警（与 A2/A3 同批，共用 CDP 会话）。
  CLI 对偶（`warn_probe` 那套）已在 2026-08-14 真机闭环，本轮补的是**前端浏览器面**。
- **B24 剩余**：分析 → 影响/diff 卡、确认执行 → SSE 进度、结果 diff / 验证状态 / 回滚。
  可确定性的一半（7 项）已在 `verify-05-stage-card.mjs` 全绿；剩余三处要真实 LLM + 工具链。
  用一个**历史目录**（如桌面 `2021F\21F` 或本轮 A8 产出的 `out_2026C_stm32`）当舞台，跑一次修订。

### D 组 / E 组（需用户出面，见第 2 节）

## 2. 需要用户出面的（D / E 组）

| 项 | 需要用户做什么 | 我能不能替 |
|---|---|---|
| **D1** | 6 个器件补 `kit` + `source_url`：`beep` / `ir_beam` / `key` / `led` / `led_beep` / `step_motor`。**机器不许编链接**（工单硬性）。或者你直接拍「永久不补」→ 我把它们改判 `MODULE_KIND` 内部件/协议切片并写明理由 | ❌ 只能你来给链接或拍裁决 |
| **D2** | 「跨分类抽查 ≥5 篇」：这条本单记「现 4 篇（sensor/rf/screen/control 各一）」。我可以用仓库视觉通道 + 落盘原话把篇数与抽查机器化（每篇一问），**但「人工过目」的字面语义只有你能给**——要么我机器化代办（口径记进来源工单），要么你抽 5 分钟看 5 篇 | ⚠️ 可机器化代办，需你拍 |
| **E1** | 全新机器 `install.bat` → `.venv` → 双击 `start-app.vbs`。本机**已有全局 Python 3.14.6 且仓库没有 `.venv`**，跑 `install.bat` 会真建 `.venv` 并可能改动当前环境；「全新机器」语义本机给不了 | ⚠️ 可在本机跑「无 python / 有旧版 python」两态文案检查（不建 venv），完整态建议你在干净机器/虚拟机做 |
| **E2** | `start-app.bat` 三态：正常态无黑窗 / **端口被占中文弹窗** / 超时弹窗带日志路径。正常态与「端口被占」态都要动当前 8000 服务（要么我临时停它，要么你另开一个端口跑） | ⚠️ 需你选：让我临时停 8000 / 你自己在别的机器试 / 跳过 |

## 3. 额度预算（按 ~/.contest_generator/config.json 的 DeepSeek 单价估算，非账单）

单价：输入（缓存未命中）¥1.5/M、输入（命中）¥0.05/M、输出 ¥4.5/M。

| 批次 | 真实调用 | 估算 |
|---|---|---|
| A 组 CLI（A8×2 骨架 + A9/A10/A11 骨架 + 首次 2026C 真实推荐） | 推荐 1（2–4 轮）+ 骨架 4 + 可能修复轮 0–3 | ¥1.0 – 2.2 |
| A6（2024H，如做） | 推荐 1 + 骨架 1 | ¥0.4 – 1.0 |
| B 组（B24 修订 + B26 修复轮 + B27） | 推荐 1 + 影响分析 1 + 深化 1–2 + 修复 2–4 | ¥1.0 – 2.5 |
| **C 组** | **推荐 8（C1×4 / C2×1 / C5×3）+ 修复 2–4 + 视觉 ~10 次单图描述** | **¥2.5 – 6.0** |
| 合计 | **约 25–45 次真实 LLM 调用 + ~10 次视觉调用** | **¥5 – 12（量级：一顿饭）** |

**主要成本不是钱，是墙钟时间**：单次推荐 10–15 min（首轮静默窗口可达分钟级），C 组 8 次推荐
≈ **2–3 小时**；A/B 组编译+骨架 ≈ 40–60 min。所以建议**分批开跑、每批跑完我报一次实测用量**
（`/api/llm-workflows/recent` 的 usage 是服务商上报，非估算）。

## 4. 必需的小修（改 `.scratch/` 工具 + 口径留痕，产品代码零改动）

1. **`generate_check.py` 题库路径过期**（`~/.contest_generator/topics` → `library/topics`）：
   加 `--topics-dir` 覆盖（缺省保持旧值，向后兼容），命令行显式传 `library/topics`。
   理由与现状写进挂账单 A9 项 + 来源工单 `gate-corpus-closure/01`。
2. （视情况）**A8 的题面**：挂账单写「gmake（2026H mspm0 0 错）」——2026H 是 mspm0 巡线题，
   但题面文件在 `library/topics/2026H/topic.md`，可用。是否坚持用 2026H 而不是 2026C，
   取决于你想验「另一道题的模块组合也能编过」还是「只要 mspm0 线编过」。**默认按挂账单原文用 2026H**。

## 5. 收尾（照第十四/十五轮先例）

- 每项证据落 `.scratch/<slug>/verify-*.txt` / `shot-*.png` / `*.vision.txt`，目视走
  `.scratch/vision-eyeball.py`（每图一问、原话落盘）。
- 验完一项：勾挂账单对应项 + 回勾来源工单该项；口径变更写「原口径 → 现状 + 理由」。
- 本轮收尾在 `.scratch/tracker-audit/2026-09-09-在途盘点.md` 新增「第十六轮」段。
- 基线：`$env:PYTHONPATH='src'; python -m pytest -q` + `node --test tests/js/*.test.mjs`。
- 提交中文信息，**不 push**。
