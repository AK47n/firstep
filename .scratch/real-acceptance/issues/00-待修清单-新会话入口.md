# 第十六轮（2026-09-10 真机验收）待修清单 —— 新会话开工入口

**状态：** resolved（清单**全部落地**：02 / 03 / 04 / 05 / 06 / 07 / 08 / 09 各见本单表格行的落地注记；
**2026-09-18 归档复核**：第四节两笔「自己冒出来的待办」也已落地，本单再无未落地项——台账口径
同步修（`backlog.md` 第 6 节标题原先停在「6 张工单待做」），剩余工作量全部在第三节「需要人」）

本轮把挂账单里能跑的都跑了（`.scratch/real-acceptance/issues/01-real-machine-acceptance.md`，
A–E 组 45/50 勾完 —— **2026-09-18 实测口径：A–E 共 47 项（原记 50 有误）、已勾 42 / 未勾 5**：
A 8/11 + B 27/27 + C 5/5 + D 1/2 + E 1/2；未勾 = A4/A5/A7（需用户点一次）+ D1（用户给链接）
+ E1（干净机器），另 E2 的「超时态」与 F 组 3 项仅登记。**C 组 5 项本轮按在盘产物补勾**
（第十六轮已真机 PASS，只是当时没回勾挂账单，见挂账单 C 组开头说明）；过程中验出/记下一批
**待修项**。这张单是它们的**唯一入口**：
每个待修项一份工单（各自带红证、文件边界、验收标准、可直接粘贴的新会话提示词）。

## 一、新会话怎么开（照抄）

建议**一单一会话**（每个工单开工前读 `CLAUDE.md` / `CONTEXT.md` + 该工单全文），
顺序按下面的优先级。三句话版本：

```
读 CLAUDE.md、CONTEXT.md，然后读 <工单路径> 全文。
按该工单的「修复方向」与「验收标准」做，红证先行；产品代码改动遵守工单里的文件边界。
收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；工单勾完 + 在 .scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交用中文（钩子拒英文），不要 push。
```

## 二、待修清单（按优先级）

| # | 工单 | 类型 | 为什么排这个位置 |
|---|---|---|---|
| 1 | `02-mspm0-compile-verdict-parse-gap.md` | 🔴 真缺陷 | **真机编译验收的判读底座**：不修，mspm0 线任何失败都可能被报成「0 错 0 警」，后面所有编译类结论都不可信。红证文本已在盘（`verify-16-A8-mspm0-2026H-buildlog.txt`），可独立开工。**✅ 第十七轮落地**（resolved） |
| 2 | `03-recommend-domain-reject-opaque.md` | 🟠 真缺陷 | 本轮 **14 轮真实推荐里 9 轮**栽在它上面（域拒绝 → 通用话术 + 免重试）。修完直接降低后续所有真机/额度类验收的浪费。**✅ 第十七轮落地**（kind=domain 分流 + 带理由重试 1 次；③ 词表闸降级只做评估、留决策点） |
| 3 | `04-exclusive-group-recommendation.md` | 🟠 真缺陷 | 推荐同时给同组两个成员 → 生成才 400（用户点「生成」才炸）。有真实缓存载荷可当 fixture，可独立开工。**✅ 落地**（resolved）：解析层确定性收敛（保留一个、其余进可见的「同组候选未选中」）+ `exclusive_groups[].recommended` 语义收紧为组内 ≤1；真机 `--reuse-recommend 2026C` **不带 `--drop`** 跑绿 |
| 4 | `05-request-budget-reality-gap.md` | 🟡 口径/预算 | 请求体实测 122161 字节（余量 6.8%），而账本写「最坏 ≈119.5KB」——后续任何加提示词段的改动都会先撞上限。**✅ 落地**（resolved）：根因不是漏算段而是三种记账病（账本的数来自修复侧 / 两套余量魔数 / 最坏形态漏算预筛注记且合成测试小 34KB）；段级账本改为可执行 + 统一余量单源 `REQUEST_RESERVE_BYTES` + 全文段重分配 25600→23400 + 修掉 `_fit_segment_wire` 标注超预算；真机 2021F/stm32 4 轮收敛终态 done 无「请求体过大」 |
| 5 | `06-ccs-toolchain-mix-and-match.md` | 🟡 文案/体检 | 三件套跨安装目录混搭（编译器 ccs2050 + SDK/SysConfig ccs2051）实测可用但体检不说明。最小改动，适合当「热身单」。**✅ 落地**（resolved）：体检补 `ccs-note` 说明行（三件逐件独立探测 / 可能跨目录 / 覆盖项留空 = 各取最新）+ 逐件**安装根**（`ccs_tools_status` 新增 `root`，自定义布局不猜）+ 设置页三行提示；真机探针逐字对上工单表格（`probe-06-ccs-roots.txt`）+ 线上载荷真渲染（`probe-06-env-render.txt`）。**探测行为未改**：「同根优先」是行为变更，已在本单「未采纳的方案」记下待用户拍板 |
| 6 | `07-browser-acceptance-pitfalls.md` | 🟡 工具姿势 | 浏览器真机验收四个坑（折叠+页签 / `waitForFunction` 超时位置 / `evaluate` await 长流程 / 模态确认不点）固化成助手 + 前置清单——省的是**下一个做真机验收的会话**的时间。**✅ 落地**（resolved）：`.scratch/browser-harness.mjs` 两助手（`expandCard` / `pollUntil`，零依赖、假 page 可测）+ 挂账单 01「B 组统一前置 · 姿势清单」+ B24 脚本改用它真机复跑 **19/19 全绿**；纯件测试 `tests/js/browser-harness.test.mjs` 9 条 |
| 7 | `08-wordlist-solution-name-domain-gap.md` | 🟠 数据单（新开） | **决策已拍**（`solutions` = 导览文本，`models` 是唯一合法 name 域）+ 数据已量清（85/87 方案名不在合法域、B1 增量 +1942 字节）：**唯一产品改动是 `wordlist.json` 数据**，`SelectionError` 语义零改动。治的是「模型照抄提示词里就有的方案名 → 被闸拒收」这类**反复发生**的失败（2022C 连续三轮挂在同一类名字上），也是后续真机/额度类验收里最便宜的一笔止血。**✅ 落地**（resolved）：入 15 条裸名 + 预算配套（全文段 27000→25600）；2022C / 2026H 双题真机 done、域拒绝 0 条。**38 条顺延**（27 条预算不足 + 11 条按可达性让位）是已知工作面，下批须先瘦身 models 或重议 2KB 自建边界 |
| 8 | `09-recommend-output-blame-key.md` | 🟠 真缺陷（新开） | 用户报障：推荐失败被报成「AI 服务拒绝…查 key / 余额」，而实测是 `http_status=200` 的**本地输出**失败（`parse_error`）——HTTP 200 套那句通用话术按定义就是错的。**✅ 落地**（resolved）：`ERROR_KIND_OUTPUT` 分流 + 文案 + 可重试形态重试 |

**依赖关系**：01–07 相互独立；**03 与 05 建议同批**（同一片预算/错误文案代码）；
07 可并入任意一单顺带做。**08 / 09 是后开的单**（08 来自 03 的评估节决策点、09 来自
用户报障的推荐失败文案），两单均已落地。

**下一单（2026-09-11 收口后的口径）**：清单 01–09 **全部落地**，本单可关。剩下的是第四节那两笔
「自己冒出来的待办」（**生成期引脚冲突门禁**评估 / **提交主题 BOM** 小修）——都不是 01–09 的延续，
谁先做都行；再往后就是挂账单 01 里「需要人」的那几项（第三节）。

**归档（2026-09-18 复核）**：上面那两笔「自己冒出来的待办」**已全部落地**——
- 生成期引脚冲突门禁 + 一键配置真能解默认撞脚：`.scratch/pin-conflict-gate/01-02`（均 resolved，
  真机 `--bindings` 解掉 servo/motor 撞脚后 gmake exit=0）；
- 提交主题 BOM：`.scratch/commit-subject-bom/01`（resolved）。

另：08 顺延批的 27 条方案裸名也已在 `real-acceptance/10` 落地；`instances` 幻觉在
`real-acceptance/11` 落地。**本单自此无未落地项**，剩余工作量全在第三节（需要人）。
台账口径同步：`backlog.md` 第 6 节标题原先停在「6 张工单待做」（不实），本轮已修。
**探针小尾巴（同日）**：`09` 的复现探针原先有两份 runpy 副本（锚点版 + 抓包版），第三个源码锚点
随 `05` 尾巴改缩进失效、每次跑静默丢一路取证 → 合并进
`.scratch/recommend-domain-reject/probe-16-recommend-live.py --capture-raw`（附 UTF-8 stdout），
两份副本已删，`09` 文内的引用同步更正。

**清单一里一外还有两笔自己冒出来的待办**（不在 01–07 清单内，都有落档）——✅ **两笔均已落地，
见上面「归档（2026-09-18 复核）」**，本段保留原始记录供追溯：
- **生成期引脚冲突门禁**（SysConfig 语义级 `associatedPins` 互斥）：单 02 的附件事实，
  未立项。修好判读后用户能看到「7 条冲突 + 请改引脚绑定」，但更理想的是**生成时就拦下**
  或自动 `auto_assign_bindings` 消解——修复方向未定，建议单 06/07 之后再评估。
- **提交主题 BOM 打穿 CHANGELOG 跳过清单**（盘点「新发现」第 6 条，修法已写好）：
  `_is_displayable` 先 `lstrip("\ufeff \t")` + 一条 `\ufefftest: → False` 用例。
  当前污染面很小，属待办小单。

## 三、需要人（不是 agent）的剩余项

**2026-09-18 复核后的实况**：agent 侧无剩余（本单第二节全落地）；下表逐行核对过——
A8 行已因 `pin-conflict-gate` 落地而**闭环**；**C 组 5 项不是待办**：第十六轮就已真机跑完 PASS，
只是挂账单没回勾（本轮按在盘产物补勾，零新增额度），第三节下表原先把 C 组列成「待验」是错的；
其余确为「需要人」。可勾项与实况：挂账单 01 现为 **A–E 42/47**（A 8/11 + B 27/27 + C 5/5 +
D 1/2 + E 1/2，未勾 5 项见下表）。

| 项 | 谁 | 说明 |
|---|---|---|
| A4 / A5 / A7 | 用户点一次 | Keil5 编 `out_2026C_stm32`；CCS 编 `out_2026H_mspm0`（**现可直接看门禁拦下的 7 条冲突说明**，见 `pin-conflict-gate/verify-01-real-machine-generate-check.txt`）、Theia 编 `out_16_mspm0_min` |
| ~~A8（mspm0 线）~~ | ✅ 已闭环（2026-09-17/18） | 判读缺口（单 02）与真实撞脚（`pin-conflict-gate/01-02`）都已修：真机带 `--bindings` 解掉 servo/motor 撞脚后 **gmake exit=0（0 错 0 警）**，挂账单 A8 已勾 |
| D1 | 用户给链接 | 6 个器件 `kit` + `source_url`（或拍「永久不补」→ 改 `MODULE_KIND`） |
| E1 / E2-超时态 | 干净机器 / 劣化环境 | 见 `.scratch/newcomer-onboarding/verify-16-E2-three-states.md` |
| ~~C1–C5（真实 LLM / 视觉额度）~~ | ✅ 非待办（2026-09-18 更正） | 第十六轮已全部真机 PASS（C1 注入式主链路 + 三条降级 / C2 2024H 0 补问 / C3 229120B 不打「请求体过大」/ C4 事件流 + 回滚 sha 复原 / C5 三题载荷落盘），当时只写进盘点、漏回勾挂账单 → 本轮按在盘产物补勾 |
| ~~D2 复核（wiki 抽查）~~ | ✅ 非待办（2026-09-18 更正） | 第十六轮已按「每分类 1 篇」跑满并留原话（库内实际只有 4 个分类，「≥5 篇」不可达 → 口径变更）；原话复核仍可由用户随时做（可选，非阻塞） |

## 四、本轮已顺带闭环、无需再做的

- `--topics-dir` / `--modules-dir` / 输出目录先清场（`generate_check.py` 三处口径修订，已落地）；
- `b1-adc-servo/compile_matrix.py` 的 `servo_init` 双参修订（已落地）；
- 挂账单 A1/A2/A3/A6/A9/A10/A11 + B24/B26/B27 + C1–C5 + D2 + E2（两态）已勾并回勾来源工单。
