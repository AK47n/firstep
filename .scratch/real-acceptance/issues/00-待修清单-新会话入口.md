# 第十六轮（2026-09-10 真机验收）待修清单 —— 新会话开工入口

**状态：** ready-for-agent（清单仍有未做完的项：**02 / 03 已于第十七轮落地**
（各见本单表格行的落地注记），04–07 未开工；第三节「需要人」的项等用户出面）

本轮把挂账单里能跑的都跑了（`.scratch/real-acceptance/issues/01-real-machine-acceptance.md`，
A–E 组 45/50 勾完），过程中验出/记下一批**待修项**。这张单是它们的**唯一入口**：
每个待修项一份工单（各自带红证、文件边界、验收标准、可直接粘贴的新会话提示词）。

## 一、新会话怎么开（照抄）

建议**一单一会话**（每个工单开工前读 `CLAUDE.md` / `CONTEXT.md` + 该工单全文），
顺序按下面的优先级。三句话版本：

```
读 CLAUDE.md、CONTEXT.md，然后读 <工单路径> 全文。
按该工单的「修复方向」与「验收标准」做，红证先行；产品代码改动遵守工单里的文件边界。
收尾：全量 pytest + node --test tests/js/*.test.mjs 绿；工单勾完 + 在 .scratch/tracker-audit/2026-09-09-在途盘点.md 追加一段收口；git 提交用中文（钩子拒英文），不要 push。
```

## 二、待修清单（按优先级）

| # | 工单 | 类型 | 为什么排这个位置 |
|---|---|---|---|
| 1 | `02-mspm0-compile-verdict-parse-gap.md` | 🔴 真缺陷 | **真机编译验收的判读底座**：不修，mspm0 线任何失败都可能被报成「0 错 0 警」，后面所有编译类结论都不可信。红证文本已在盘（`verify-16-A8-mspm0-2026H-buildlog.txt`），可独立开工 |
| 2 | `03-recommend-domain-reject-opaque.md` | 🟠 真缺陷 | 本轮 **14 轮真实推荐里 9 轮**栽在它上面（域拒绝 → 通用话术 + 免重试）。修完直接降低后续所有真机/额度类验收的浪费。**✅ 第十七轮落地**（kind=domain 分流 + 带理由重试 1 次；③ 词表闸降级只做评估、留决策点） |
| 3 | `04-exclusive-group-recommendation.md` | 🟠 真缺陷 | 推荐同时给同组两个成员 → 生成才 400（用户点「生成」才炸）。有真实缓存载荷可当 fixture，可独立开工 |
| 4 | `05-request-budget-reality-gap.md` | 🟡 口径/预算 | 请求体实测 122161 字节（余量 6.8%），而账本写「最坏 ≈119.5KB」——后续任何加提示词段的改动都会先撞上限。建议与 **03** 同批做（03 的复算要碰同一片预算代码） |
| 5 | `06-ccs-toolchain-mix-and-match.md` | 🟡 文案/体检 | 三件套跨安装目录混搭（编译器 ccs2050 + SDK/SysConfig ccs2051）实测可用但体检不说明。最小改动，适合当「热身单」 |
| 6 | `07-browser-acceptance-pitfalls.md` | 🟡 工具姿势 | 浏览器真机验收四个坑（折叠+页签 / `waitForFunction` 超时位置 / `evaluate` await 长流程 / 模态确认不点）固化成助手 + 前置清单——省的是**下一个做真机验收的会话**的时间 |

**依赖关系**：01–06 相互独立；**03 与 05 建议同批**（同一片预算/错误文案代码）；
07 可并入任意一单顺带做。

## 三、需要人（不是 agent）的剩余项

| 项 | 谁 | 说明 |
|---|---|---|
| A4 / A5 / A7 | 用户点一次 | Keil5 编 `out_2026C_stm32`、CCS 编 `out_2026H_mspm0`（会重现单 02 的 7 条冲突）、Theia 编 `out_16_mspm0_min` |
| A8（mspm0 线） | 等单 02 修好 | 修好后复跑 `generate_check.py --platform mspm0 --reuse-recommend 2026H` 再勾挂账单 |
| D1 | 用户给链接 | 6 个器件 `kit` + `source_url`（或拍「永久不补」→ 改 `MODULE_KIND`） |
| E1 / E2-超时态 | 干净机器 / 劣化环境 | 见 `.scratch/newcomer-onboarding/verify-16-E2-three-states.md` |
| D2 复核 | 用户（可选） | 4 篇抽查原话已落盘 `sources/materials/lckfb-地猛星移植手册` 对应的 `.scratch/wiki-md-repair/*.review.txt` |

## 四、本轮已顺带闭环、无需再做的

- `--topics-dir` / `--modules-dir` / 输出目录先清场（`generate_check.py` 三处口径修订，已落地）；
- `b1-adc-servo/compile_matrix.py` 的 `servo_init` 双参修订（已落地）；
- 挂账单 A1/A2/A3/A6/A9/A10/A11 + B24/B26/B27 + C1–C5 + D2 + E2（两态）已勾并回勾来源工单。
