docs: 小尾巴收口——台账口径修正 + real-acceptance/00 归档 + probe-05 两份副本合并进 probe-16

产品代码零改动（src/ 与 static/ 未触碰），全是台账、探针与证据件。

一、台账口径修正
- backlog.md 第 6 节标题长期停在「🔶 6 张工单待做」，而 real-acceptance/02–11 十一张单
  早已全部 resolved：改为「✅ 全部落地（2026-09-18 复核）」，06/07 两行补落地摘要。
- real-acceptance/00 归档复核：第四节两笔「自己冒出来的待办」（生成期引脚冲突门禁 /
  提交主题 BOM）在 2026-09-17 已随 pin-conflict-gate/01-02 与 commit-subject-bom/01 落地，
  标题却仍指向它们 → 补「归档复核」段、第三节逐行核对（A8 行闭环、A5 行改用可解形态复验）。
- 「A–E 组 45/50 勾完」更正为实测 35/50（A 8/11 + B 27/27；剩 12 项待验 + F 组 3 项仅登记）：
  45/50 是第十六轮中途口径，之后 A1–A3/A9–A11 由各自工单收口时另勾，从未回写。
- 挂账单 01 的 A8 补闭环证据并勾满（真机带 --bindings 解掉 servo/motor 撞脚后 gmake
  exit=0 / 0 错 0 警）；A5 改为「可用解形态生成后再导 CCS」——原 out_2026H_mspm0 那 7 条
  冲突工程现在生成期就被门禁拦下，已不能当复验样本。

二、探针合并（唯一有代码改动处，改的是 .scratch/ 探针）
- 删除两份 runpy 套壳副本：probe-05-select-client-error.py（锚点太泛 + Windows GBK
  控制台打 ⚠ 直接 UnicodeEncodeError 自炸）与 probe-05-transport-capture.py（第三个锚点
  「超长守卫」随 05 尾巴改源码缩进后永久失效，每次跑静默丢一路取证）。
- 能力并入 recommend-domain-reject/probe-16-recommend-live.py --capture-raw：Transport
  边界猴子补丁逐次打印原始 status / body 字节 / finish_reason / content 字符数 / usage
  + 四支判读（超长守卫 / max_tokens 截断 / 空 content / 上游 4xx）+ 抓取条数汇总。
- 探针两处硬化：① 启动即把 stdout/stderr reconfigure 成 UTF-8（errors=replace），
  防再因编码炸在诊断行；② 注入体检从「只在失败时打一行」改成每次打印「锚点注入 N/M 处」，
  锚点漂移不再静默。
- 校验（假响应 + 锚点唯一性，零网络零额度）：patch 命中 UrllibTransport、四支判读逐一
  命中、三个源码锚点计数全为 1；校验脚本用完即删。
- 证据件入库并归一：probe-05-transport-capture.txt 由 PowerShell 重定向写出的 UTF-16LE
  （FF FE 开头，读文件工具判 binary）转为 UTF-8（内容零改动，仅加一行归一说明），与
  三份 probe-05 载荷 json 一并入库；09 文内对已删脚本的引用同步更正。

三、回归面
- python -m pytest -q → 3994 passed, 1 warning（与上一轮基线持平；本就无 src/ 改动）。
- node --test tests/js/*.test.mjs → 1444 pass / 0 fail。
