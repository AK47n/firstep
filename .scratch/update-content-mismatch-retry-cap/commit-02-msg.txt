修复：持久的「内容与清单不符」连续 5 次转终态（工单 update-content-mismatch-retry-cap/02）

决策单选 1（N=5）：网络是**瞬时**不确定性（重试有救），内容不符可能是**持久**的
（重试改变不了服务器上那份字节），两类不该共用同一条无上限策略。改之前真机 150 秒
观察窗内 6 次重试、每轮整卷重下、永远进不了失败态（线上完整包 765 MB/卷 ≈ 45 GB/小时
量级），而 spec 第 177 行承诺的那句「重下不会有变化」一次都不会出现。

下载域新增终态错误 DownloadContentMismatchPersistentError（进不可重试表、error_kind=verify、
文案不再承诺「会重新下载整卷」）+ 模块级常量 MAX_CONTENT_MISMATCH_ATTEMPTS=5：
重试循环里**连续**计数（中途别的错误清零），到上限即抛终态；前 4 次行为逐字不变；
新关键字 max_content_mismatch 只给测试假剧本覆盖。两条链路由下载域自动同时生效。

守卫：单测三格（永远坏 → 5 次转终态且文案/分类/盘面都对；坏-断-坏 → 连续不等于累计；
网络失败不受影响）+ 两条修复的接缝断言（终态错误必须被 is_terminal_verify 认下、任务层
清干净半成品与边车）+ 判据强度探针 3/3 转红。两支探针都加了前置干净性检查（上一轮被
强杀没复原时拒绝开跑，避免误读成探针写坏）。聚焦 148 passed。
