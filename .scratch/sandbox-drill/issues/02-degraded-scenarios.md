# B2 — 三极端场景：弱网中途取消 / 断线重试 / 校验失败

**要做什么：** G1 后三步（一直只在代码侧覆盖过）在**真机**上跑一遍，判据用产品可观察量：半成品留不留、边车写没写、重试从哪接着下、报错是不是中文、旧版本还能不能用。

**被谁阻塞：** B1（用升好级的沙箱）。

**状态：** pending

- [ ] **弱网中途取消**：起本地可控服务器（弱网/慢速）→ 走产品下载 → 中途取消 → 断言：状态 `cancelled`、`updates/` 下半成品与 `.partial.json` 边车**都在**、工具仍可用；再点重试 → **从已下字节接着下**（不是从 0）→ 终态校验通过
- [ ] **断网重试**：连接被切断 → 断言：自动重试（`retry_count` 增长、话术含「正在自动重试」、从 X% 接着下）→ 网络恢复 → 续传完成，**终态字节数与 sha256 都对**
- [ ] **校验失败**：改坏一个卷 → 断言：报**中文**错误且指明校验失败、半成品被清、`updating.lock` 已释放、备份目录在、**旧版本仍可用**（服务还能起来、版本还是旧的）
- [ ] 每格独立可复跑、自带超时；「卡住」与「红」分开记
- [ ] 脚本 `.scratch/verify-gate-drills/drill-02-degraded.py` + 原始输出 `verify-02-degraded.txt/.json`
- [ ] 若暴露产品缺陷：按 `docs/agents/issue-tracker.md` 开工单，**演练本轮如实记账**（不在脚本里就地改产品）
