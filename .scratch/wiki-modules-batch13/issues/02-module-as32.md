# 02 — as32 AS32-LoRa 无线数传模块（真实 UART 双向，手册 rf--as32-lora-wireless-communication-module.md）

**要做什么：** 模块库新增 `as32` 条目（仅 mspm0）：从手册提炼 AS32-TTL-100 LoRa 数传驱动为纯驱动切片——**真实 UART 双向透传**（UART 放置决策按批次 4/10 结论取证后落定方案①：「裁剪后独占」独立实例照 fingerprint/open_mv4 先例）：新 UART 实例 `AS32_UART`（默认 $assign=**UART3**（宿主 ZIGBEE_UART——LoRa 与 Zigbee 无线数传互替、同选概率最低，单选裁剪后独占 UART3，同选时经引脚绑定换实例/换脚消解）、targetBaudRate=**9600**（页面/AS32 默认）、enabledInterrupts=[] **轮询接收**（无 UART_3_INST_IRQHandler 强符号——zigbee_uart 为 RX 中断件，轮询避免同实例名 ISR 重复；fingerprint/open_mv4 先例）、默认 TX=**PA26**/RX=**PA25**（ZIGBEE_UART 原脚——互替件同脚先例：OPENMV4_UART/PA8/PA9 与 DIGIT_UART 同构））；API = `as32_init()`（清接收状态——页面 LOAR_Init 的 NVIC 使能随轮询裁剪）+ `as32_send_string()`/`as32_send_hex()`（页面 LOAR_USART_send_String/HEX 原样：DL_UART_isBusy 忙等 + 逐字节发送）+ `as32_receive(buf, max_len)` 返回接收字节数（0=无数据——页面 Anakysis_Data「读到即清」语义：轮询排空 FIFO → 入缓冲（≤max_len−1 截断 + '\0'，**页面 LOAR_RX_LEN 模运算回绕会在满缓冲覆盖首字节——人工复核修正为截断保护，notes 记录**）+ 一次性返回并清缓冲标记）+ `as32_flush()`（页面 Clear_LOAR_RX_BUFF 语义）；缓冲上限保留页面 300（AS32_RX_BUF_MAX）；**AT 配置不落码**（页面无 AT 指令代码——「参数的修改是通过上位机进行设置」+ MD0/MD1 硬件模式，页面驱动未接线；notes 写明需 AT 配置时可经 as32_send 在配置模式直发或上位机预配置）；选中后生成工程打开即可编译、可调用，不再"需自备"。

**被谁阻塞：** 无——可立即开始（UART 决策本批 spec 已取证拍板：批次 4 spec「UART 资源可行性调研」+ 批次 10/04 结论 + 母版现状 = UART0-3 全被实例占用无空外设，但「裁剪后独占」先例成立、方案①与 open_mv4 同构；方案②软 UART RX 无先例不取、方案③砍件不触发；与 01/03/04 独立）。

**状态：** resolved

**结论：** 2026-09-06 完成并提交。as32 AS32-TTL-100 LoRa 无线数传模块（仅 mspm0，无依赖）：真实 UART 双向透传——独立实例 AS32_UART（UART3 默认（ZIGBEE_UART 宿主——LoRa 与 Zigbee 无线数传互替、单选裁剪后独占）、9600、enabledInterrupts=[] 轮询接收（无 IRQHandler 强符号）、默认 TX=PA26/RX=PA25（ZIGBEE_UART 原脚——互替件同脚先例））；API = as32_init（清接收状态，页面 LOAR_Init 的 NVIC 段随轮询裁剪）+ as32_send_string/send_hex（页面 LOAR_USART_send_String/HEX 原样：DL_UART_isBusy 忙等 + DL_UART_Main_transmitData）+ as32_receive（页面 Anakysis_Data「读到即清」语义：轮询排空 FIFO → 入缓冲（≤max_len-1 截断 + '\0'——页面 LOAR_RX_LEN 模运算回绕覆盖首字节修正）+ 一次性返回并清缓冲标记）+ as32_flush（Clear_LOAR_RX_BUFF）；缓冲上限 AS32_RX_BUF_MAX 300（页面原值）；AT 配置不落码（页面无 AT 代码——范围外记录）；母版 syscfg AS32_UART 实例 + INSTANCE_CONSUMERS 登记（顶部注释补 UART3 宿主说明）；wordlist「无线通信模块」models 加 AS32 + 「LoRa 数传」solution 补 lib_modules as32 + note/interface 更新（页面「占 SPI」措辞按 AS32=串口版修正）；测试（test_module_as32.py + test_pins +2 行 + test_pin_bindings PA25 5→6/PA26 6→7/UART3=2 + test_syscfg_prune 断言）；编译矩阵 PASS（UART3/PA26/PA25 过 SysConfig CLI，0 error/0 warning）→ verified；全量测试 3549 pytest 全绿；未上板（LoRa 透传距离/波特率真机验证留后续）。

**实施清单：**
- [ ] 代码提炼：手册「代码块」抽 `bsp_lora.c/h` → `code/as32.c` + `code/as32.h`：去 main.c 演示与 printf、`LOAR_Init` → `as32_init`（NVIC 使能随轮询裁剪——页面含 NVIC_ClearPendingIRQ/EnableIRQ）、`LOAR_USART_Send_Bit/send_String/send_HEX` → `as32_send_byte`（静态）/`as32_send_string`/`as32_send_hex`（页面原样：DL_UART_isBusy 忙等 + DL_UART_Main_transmitData）、`Clear_LOAR_RX_BUFF` → `as32_flush`、`Anakysis_Data` → `as32_receive`（返回长度、读后清缓冲——页面标志位语义；**页面 UART_1_INST_IRQHandler 中断缓冲改轮询排空**（照 open_mv4「IRQHandler 随轮询裁剪」先例：DL_UART_isRXFIFOEmpty 忙等排空入缓冲——无 ISR 强符号）；**截断保护修正**（页面 `LOAR_RX_LEN=(LOAR_RX_LEN+1)%LOAR_RX_LEN_MAX` 模运算满缓冲回绕覆盖首字节——改 cap 截断 + '\0' 收尾，notes 记录）；页面无帧结构（透传字符串——行分帧/校验归调用方骨架，ADR 0009）；`LOAR_RX_LEN_MAX 300` 保留为 `AS32_RX_BUF_MAX`
- [ ] 母版 `mspm0.syscfg`：新 UART 实例 `AS32_UART`（$assign=UART3、targetBaudRate=9600、enabledInterrupts=[]、TX=PA26/RX=PA25（ZIGBEE_UART 原脚）、enableFIFO=false 等照 OPENMV4_UART 形态；注释写明：UART 实例上限 4 现实约束 + 与 ZIGBEE_UART 同外设（LoRa 与 Zigbee 无线数传互替、同选概率最低——互替件同脚先例 OPENMV4_UART/DIGIT_UART，同选时经引脚绑定换实例/换脚消解）+ 轮询无 IRQHandler 强符号 + 9600=页面/AS32 默认
- [ ] `syscfg_instances.py` INSTANCE_CONSUMERS 登记 `"AS32_UART": ("as32",)` + 顶部注释补 AS32_UART（UART3 宿主、与 ZIGBEE 互替、轮询）说明
- [ ] `manifest.json`：dependencies []；mspm0 平台条目；pins AS32_UART_TX/RX（uart_tx/uart_rx，default PA26/PA25）；kit+source_url；notes 含手册路径+原页+网盘链接（`https://pan.baidu.com/s/1_vM6zLzpOc40qAKlwc7zIg?pwd=qg1m` 提取码 qg1m）+采购链接（taobao：LORA 模块 433 无线串口 SX1278 中继组网）+厂家资料（ashining AS32-TTL-100 页 + 上位机 soft_asds.zip）+改造要点（页面「PA8/PA9 附加串口 1」改挂 UART3/PA26/PA25——UART1 已被 DIGIT_UART+OPENMV4_UART 占（视觉互替），UART3=ZIGBEE 宿主（数传互替）、同选概率最低——与 FINGERPRINT_UART/UART0、OPENMV4_UART/UART1 同构；IRQHandler 改轮询 + 截断保护修正；页面无 AT——范围外）+**UART 放置决策依据**（批次 4 实证引述：同外设多实例 Resource conflict/实例上限 4=外设数/8 实例定义每工程先裁剪「裁剪后独占」；方案②软 UART RX 无先例不取、方案③砍件不触发——LoRa 与 NRF24L01（SPI 互替）/HC05（蓝牙）/UWB（定位）/Zigbee（串口数传互替）重叠说明：as32×zigbee 同选 = UART3 双实例 CLI 拒绝——互替件同选概率最低、消解=引脚绑定换实例（open_mv4×digit_uart 同款）；UART 实例上限 4 现实约束——as32 与另 ≤3 件 UART 模块同选在限内、4 件以上 CLI 拒绝）+与库内分工（zigbee_link 串口透传互替——zigbee 2.4G 组网 vs LoRa 433 远距 3km 级超远距离、20dBm/2.0-5.5V；nrf24l01 短距 2.4G；hc05 蓝牙近距；选用场景说明）——verified 初始 false
- [ ] wordlist.json：「无线通信模块」models 加「AS32」+ 既有「LoRa 数传（SX1278 等）」solution 补 `lib_modules: ["as32"]` + note 更新（库内 as32 已打通：mspm0 as32_init/as32_send_string/as32_send_hex/as32_receive——真实 UART 轮询双向透传、默认 UART3 PA26/PA25（与 Zigbee 互替、同选时经引脚绑定换实例消解）、9600——页面「占 SPI」措辞按 AS32=串口版修正）
- [ ] 测试：`test_pins.py::MSPM0_DEFAULT_MAP` 增 AS32_UART_TX/RX 2 行；`test_pin_bindings.py` 刻意表 PA26 6→7/PA25 5→6 + UART3 新增 =1；`test_syscfg_prune.py` 增 AS32_UART 断言；新增 `tests/test_module_as32.py`：manifest 结构 + 单选生成（syscfg 含 AS32_UART、文件落盘、main.c 调 init/send/receive 过静态门禁）+ 守卫（`9600`/`AS32_UART_INST`/`isRXFIFOEmpty` 轮询/无 `IRQHandler`/无 `NVIC`/`AS32_RX_BUF_MAX` 300/发送 `DL_UART_isBusy`+`DL_UART_Main_transmitData`/receive 清缓冲+截断保护/无 printf/页面无 AT 注释）+ manifest notes 子串（UART3/实例上限/zigbee/页面无 AT）
- [ ] 编译验证：`run_as32_matrix.py`（.scratch/wiki-modules-batch13/）单选生成 → SysConfig CLI（UART3/PA26/PA25 合法性）→ gmake 0 error/0 warning；结果回写 manifest verified=true + notes
- [ ] 中文提交 → 状态 resolved → code-review（深审：UART 决策依据/轮询改造/截断修正/帧语义）
