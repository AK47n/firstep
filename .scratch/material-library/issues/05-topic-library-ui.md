# 05 — 赛题库管理 UI（浏览 / 拆条录入 / 删除）

**What to build:** 赛题库页签（样式沿用参考文件库页签，PR #10 已立样板）：浏览列表 + 搜索、长 PDF 拆条录入（含逐条校对）、删除单条。后端补两个薄路由：`GET /api/topics`（列表，现只有单条 `GET /api/topics/{key}`——工单 01 没做列表）与 `DELETE /api/topics/{key}`（删除，工单 01 没有删除能力）；topic_library.py 补 `list_topics` / `delete_topic`（删除 = 条目目录移除，参考 reference_library.delete_reference 模式）。拆条录入流程依赖工单 04 的分块拆条。

**参考文件库 UI 已完成（PR #10，不用重做）**：参考文件库页签 = 三路搜索（标题/类型/锚定）+ 录入表单（文件文本区 + AI 简介草稿 + 锚定二选一 + kit 词表下拉）+ 删除确认；提炼报告判定加"归档为该题参考文件"按钮（载荷 `{path, topic, reason}` 透传）。本工单只做赛题库侧，风格保持一致。

**页面内容：**
- 浏览：按 年份 / 编号 过滤的列表（题面预览截断显示）；每条显示关联模块（复用 `discover_related_modules`，API 已有 related_modules 字段）；
- 拆条录入：上传长 PDF → 拆条（工单 04）→ **逐条校对表**（年份 / 题号 / 题面文本框可改，可删条目）→ 确认入库（`POST /api/topics/confirm`，multipart：pdf + payload JSON）；校对是工单 01 设计的核心环节，必须可改可删、确认前不落盘；
- 删除：确认对话框后删条目目录；
- 错误信息沿用现有 error 风格（400 中文原样显示）。

**测试：** 后端薄路由 + list/delete 用现有 topic_library 测试风格（假库目录）；前端浏览器自验（起独立端口实例，参考 PR #10 验收方式）；真机：现有 8 条题库（2018C/2019A/2020C/2021F/2022C/2022H/2024H/2026C）浏览可见、2026C 命中、删除一条再补回（确认不留脏数据）。

**Blocked by:** 04 — 拆条分块（录入流程的前置）；01 — 赛题库录入后端（已有）

**Status:** resolved

## Comments

- 2026-08-08: 工单 05 完成（PR #12）。赛题库管理 UI：浏览列表（年份 / 编号过滤 + 题面预览截断 + 每条关联模块）+ 拆条录入（上传 PDF → 拆条 → 逐条校对表可改可删、确认前不落盘 → confirm 入库）+ 删除（确认对话框后删条目目录）；后端补 `GET /api/topics` 列表与 `DELETE /api/topics/{key}` 薄路由。
- **实现取舍**：① 列表 related_modules 后端一次性算好（`discover_related_modules` 逐条调用在服务端循环内完成），前端不再按条回查——工单明确要求，避免 N 次前端调用；② 前端过滤走客户端（与参考库服务端搜索不同）：题库条目少、列表一次取回即可，后端按工单只做列表不做过滤参数；③ 拆条录入依赖工单 04 的确定性分块（超长自动分流，前端无感知），短 PDF 仍走 LLM 拆条；④ 校对表只含年份 / 题号 / 题面三字段，`program_dirs` 不在本工单范围（payload 固定空数组，与工单 01 契约形状一致）。
- 核心层：`list_topics`（遍历 topics/ 按编号排序；损坏 manifest 大声失败，与模块库 / 参考库浏览同哲学，不静默跳过）、`delete_topic`（条目目录移除，参考 `delete_reference` 模式；编号先过格式校验拦截路径穿越；查无此条与 `resolve_number` 同文案明确报错）。TopicError 已登记错误映射，无新异常类型；llm 层与 `confirm_topics` 契约未动。
- 测试：tests/test_topic_library.py 新增 17 条（核心 + HTTP 两层）：列表按编号排序、库根散文件不影响浏览、损坏 manifest 大声失败、删除后 resolve_number 报错、查无此条删除报错、非法编号删除拒绝（含路径穿越）、HTTP 层 400 中文；`pytest tests/ -q` 667 全绿，`mypy src` 干净。
- 真机验证：现有 8 条题库（2018C/2019A/2020C/2021F/2022C/2022H/2024H/2026C）浏览可见且按编号排序；2026C 命中（关联模块 lock_control/zone 正确带出，2021F 带出 pid）；长 PDF（2017-2025 汇总）经真实 `/api/topics/split` 拆出 69 条草稿；2018A 走通 拆条→校对→确认 全流程（真机确认后已删除清理）；删除 2026C 后用原题面 / 附带程序（`C:/Users/luoji/Desktop/2026C`）/ 原 PDF 补回，题面逐字一致、manifest 与初始完全一致；重复确认报 400 中文"题库中已存在该编号的赛题：2018A"；库最终还原初始 8 条，无脏数据。
- 浏览器自验：独立端口实例（127.0.0.1:8765）+ headless Chrome 渲染通过（赛题库页签 / 浏览表 / 校对表区 / 过滤控件齐全，init 脚本无错），node --check 内联 JS 语法通过。
- 两轴 code review 修复：标准轴——`list_topics` 双重排序（`sorted(iterdir())` 被末尾按 key 排序覆盖，死代码）删除、前端客户端过滤取舍加注释；规格轴——拆条成功置绿后早退路径（无文件 / 空校对表）报错仍显绿色修复（早退前 remove "ok" 类）、浏览行 `title` 全量题面截断至 200 字符（原可达 20K 字符）。
- 残留风险（如实说明）：① 交互流（文件选择、文本框编辑）未在 headless Chrome 下真实点击驱动——以"前端代码与参考文件库页签同构 + API 真机全流程走通"佐证，用户首次真机使用时若有交互问题可反馈；② 拆条草稿的残留风险与工单 04 一致（非连续同字母标记拆出同编号草稿由 confirm 编号重复校验拦截；缺题年份静默跳过；非标准格式长 PDF 大声失败）。
