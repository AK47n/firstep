# 01 — 判定边界双源头（候选 8，评估后裁决不做）

**What to build:** （无——裁决不做，本工单 = 评估留痕，防未来评审再建议同一共享）

**Blocked by:** 无

**Status:** resolved（2026-08-09 裁决不做，理由见 Comments）

## 评估

架构评审候选 8（Speculative）：JUDGMENT_SCOPE 散文（llm.py:139-149）重述 categories.py 规则分类法，"确定性 vs AI 判定"边界有两个未互相校验的源头，改规则类别需改规则表 + 改散文。

实查结论（2026-08-09）：

1. **两者不是同一事实的两份拷贝**：JUDGMENT_SCOPE = 给 AI 的语义哲学陈述（"官方外设库 / 平台基础设施 / 通用基础封装 → keep"——语义归类，无后缀枚举）；categories.py RULE_CATEGORIES = 确定性处理类别的机械实现（后缀 / 原因 / 生命周期）。"启动 / system / CMSIS / 链接脚本"这类语义描述**不可从规则表推导**。
2. **表→散文投影收益面小**：投影只能覆盖散文里的后缀枚举段（"（.uvprojx / .uvoptx / .cproject / .project 等）"），且散文用"等"字 + 语义归类——`.uvoptx` 甚至不在配置表（属残留/IDE 用户选项类别），散文的归类本就宽松。生成散文会让提示词措辞受表结构束缚。
3. **风险不对称**：提示词喂 flash 模型，措辞改动 = 行为风险；而规则类别自工单 04 以来未新增过，改动频率极低；test_llm 双端契约测试已防"只改一端"漂移（ticket 06 教训已制度化）。
4. **轻量一致性测试的断言对象模糊**：散文非枚举（"等"字 + 语义归类），机械断言会误伤；要先修 .uvoptx 措辞（零收益——"等"字兜底，工程配置路径本就全部越界拦截）。

## 裁决

**不做**。留痕理由（防再建议）：JUDGMENT_SCOPE 是语义哲学陈述、规则表是机械实现，投影只覆盖枚举段且捆绑提示词措辞；风险不对称（flash 措辞敏感 vs 类别改动频率极低）；test_llm 双端契约已防漂移。**前置条件**（若未来实施）：新增规则类别频率上升（如一年数条）且提示词实测受类别漏述影响时，再做"类别名投影"（只投影类别名清单，不生成整段散文）。

## 文件边界

无代码改动。

## Comments

- 2026-08-09 立项（架构评审 2026-08-09 候选 8，用户授权代决）：评估证据——JUDGMENT_SCOPE:139-149 散文含语义描述（不可表推导）；"（.uvprojx / .uvoptx / .cproject / .project 等）"中 .uvoptx 不在 PLATFORM_CONFIG_FILE_SUFFIXES（残留/IDE 选项类别），散文宽松归类 + "等"字兜底 = 非枚举契约；categories 自工单 04 后零新增类别；test_llm 双端契约（ticket 06 教训制度化）已防单端漂移。裁决 = 不做，留痕 + 前置条件记录
