# 01 — README 补生成物清单与「生成后怎么继续」指引

**要做什么：** 生成工程里的 README 目录结构章补上生成时新增的产物行（含「可选」标注），并新增「生成后怎么继续」章节（第 10/11/12 步用途与 .contest_* 内部状态文件说明）——学生打开工程文件夹即知每个文件是什么、哪些自己编辑、接下来去哪继续。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

**完成记录（双轴评审 + 整改）：** 目录结构章按平台补生成物行（README 自身 / 演示脚本.md / 设计报告草稿.md（可选）/ main.py（可选）/ .contest_context.json / .contest_wiring.json，两平台共用生成物元组；mspm0 makefile 集情报在 Debug/ 母版行行注——评审后记录进 spec，避免与 Debug/ 行重复）；新增「生成后怎么继续」章（第 10 修复中心 / 第 11 任务推进·参数速调·交付·修订·新想法·全局商量 / 第 12 交接提示词 + .contest_* 状态文件「勿手改、打包自动排除」）。渲染保持纯确定性、签名不变。测试：渲染器直测（新章标题与话术 / 生成物行全渲染 / 可选标注 / 常量同步）+ 流程级扩展（.contest_context.json 行经既有流程用例断言）+ 母版同步守卫调整（生成物白名单豁免方向一，「生成物必列」由 test_render_readme_generated_rows_all_rendered 实测目录表逐行核对——评审发现的恒真空断言已移除）。评审无硬性违规；判断项（裸字面量 / UI 文案 3 处联动 / main.py 无常量）按项目惯例保留。全部相关测试 198 通过，全量 2851 通过。

- [ ] 目录结构章按平台补生成物行：README 自身、演示脚本.md、设计报告草稿.md（可选）、.contest_context.json、.contest_wiring.json、K230 副产物 main.py（可选）、mspm0 Debug/ 下 makefile 集（可选）
- [ ] 新增「生成后怎么继续」章（验证顺序清单之后）：第 10 步修复中心 / 第 11 步任务推进（主路径）/ 参数速调 / 交付 / 第 12 步交接提示词 + .contest_* 状态文件「工具维护、勿手改、打包自动排除」说明
- [ ] 渲染保持纯确定性（静态话术，不依赖工具链探测），渲染签名不变
- [ ] 测试：渲染器直测新章节标题 / 生成物行 / 可选标注；流程级 README 断言扩展；母版同步守卫加白名单豁免 + 「白名单必列」方向四
