工单 index-html-ui-redo/01：上批在共享检出做的文字 UI 美化被主会话 reset --hard 抹掉，本次在最新 main（含板级引脚配置全套）基础上于独立 worktree 重做。

五块改动（仅 index.html，+111/−38，零后端零数据）：

1. **firstep 品牌标题**：title 标签与页面 h1 换为 firstep——JetBrains Mono 700 双色（first 白 + ep 青色微光）+ 尾部青色下划线光标 1.1s 终端式闪烁（内嵌字体，零网络请求）
2. **标题层级**：h2 升 16px/600/白，生成页 11 个步骤序号 `1.`~`11.` 换 mono 青色与品牌呼应；h3 灰→白 + 青色 `▸` 前缀；进度日志折叠头加 600
3. **设置页拆三卡**：AI 模型 / 数据目录 / 编译工具链（保存按钮在编译卡底部，全部输入 id 与 JS 逻辑零改动，保存仍是整页三卡一起存）
4. **第四步参考资料收短**：搜索框（按名称/说明/平台/关联实时过滤，无匹配提示）+ 列表 320px 滚动（列头吸顶）+ 已选 N 条计数 + 清空勾选；选中状态逻辑零改动
5. **第六步模块清单**：条目从一行挤 → 多行排布（模块名青色 + 徽章 + 移除右对齐 / 描述 / 依赖 mono / 平台备注独立行）+ 列表 320px 滚动收口 + 右上角计数（已选 N 个 / 展开后共 M 个模块）+ `#module-pool` min-width:0（select 长选项撑爆卡片右边的元凶）+ 长文本 break-word

回归保障：

- 引脚配置卡（卡 7）代码零触碰：板图 SVG / 旋转 90°/180° / 引脚绑定 / ⚠ 警示清单全部原样
- 移除模块按钮回调保留 `renderPinCard()` 调用（清单变化后回到占位态）
- node --check 过（脚本段提取）；pytest **1457 绿零扰动**；改动仅 index.html 单文件
