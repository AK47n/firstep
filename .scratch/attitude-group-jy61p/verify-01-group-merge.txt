# jy61p 并入航向保持組（attitude-hold）验收探针
# 仓库：C:\Users\luoji\Desktop\firstep

==========================================================================
① 真库功能组聚合（manifest 声明 → collect_exclusive_groups）
==========================================================================
  imu_uart     role=UART 串口陀螺仪（9 字节帧 + CRC16，yaw/角速度直出，接 PA28/PA31）
  jy61p        role=软 I2C + 器件内卡尔曼融合（roll/pitch/yaw 直出，接 PA28/PA31，JY61P 模块）
  ml_mpu6050   role=I2C + 官方 DMP 姿态解算（pitch/roll/yaw 100Hz，接 PA0/PA1，MPU6050 套件）
  → 成员 = ['imu_uart', 'jy61p', 'ml_mpu6050']（label=航向保持 / 姿态传感器）
  PASS：三个姿态件同组（imu_uart / jy61p / ml_mpu6050）

==========================================================================
② 判据来源：三者都是「接上就能出姿态角」的同一功能件
==========================================================================
  imu_uart     UART 串口陀螺仪（9 字节帧 + CRC16，yaw/角速度直出，接 PA28/PA31）
  jy61p        软 I2C + 器件内卡尔曼融合（roll/pitch/yaw 直出，接 PA28/PA31，JY61P 模块）
  ml_mpu6050   I2C + 官方 DMP 姿态解算（pitch/roll/yaw 100Hz，接 PA0/PA1，MPU6050 套件）
  jy61p 自己的 notes 早就写明与 imu_uart 「姿态互替」：命中
  PASS：库内证据支持同组

==========================================================================
③ 解析层收敛（用户现场形态：句子 12 推 jy61p + 另一句推 imu_uart）
==========================================================================
  顶层 modules（进工程集）= ['pid', 'jy61p']
  dropped_exclusive_members = {'attitude-hold': ('imu_uart',)}
  收敛规则（既有设计，本单未动）= 保留**模型清单里先出现的那个成员**：
    本例第 2 条需求先提到 jy61p ⇒ 留 jy61p、剔 imu_uart（两者不再同时进工程集）。
  PASS：组内只留一个姿态件，另一个落进可见的 dropped 字段

==========================================================================
④ 前端判据（fx/module.js 的纯件语义，按同一个载荷推演）
==========================================================================
  groupOfSlug(groups, 'jy61p') 非空 ⇒ 需求句走 groupRequirementNote：
    渲染成「jy61p（已在『功能组选择』中，<理由>）」灰注，不再是可移除 chip —— PASS
  renderGroupCards 里 jy61p 出现在 attitude-hold 组卡的成员行（radio 可点）：
    isRec=False（AI 没把它当首选）+ isDropped=True ⇒ 标「同组互斥·未选中」，点它即换选 —— PASS

==========================================================================
⑤ 反证（红证）：把 jy61p 的组声明**删掉**，同一份载荷再跑一次
==========================================================================
  删掉声明后 attitude-hold 成员 = ['imu_uart', 'ml_mpu6050']（jy61p 不在其中）
  同一载荷的顶层 modules = ['pid', 'jy61p', 'imu_uart']
  ⇒ 那时 jy61p 既不入组卡、也不吃收敛：需求句里以可移除 chip 出现——
     用户看到的正是报告里那张图（同屏一个姿态传感器单选框 + 一个裸 jy61p chip）。
  PASS：红证成立

==========================================================================
判读汇总
==========================================================================
  ① 三件同组           PASS
  ② notes 自证同等     PASS
  ③ 解析层收敛          PASS
  ⑤ 红证             PASS
  合计：4/4 PASS
