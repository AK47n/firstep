# 待办计划（backlog）

后续可做的优化清单（2026-08-19 记录，按需立项走 workflow）。

## 1. 推荐缓存加模块库指纹校验

**问题**：`validate_recommend` 只校验题面 / 平台 / 赛题键，**不查模块库内容**——库变了（模块增删 / description 变化）缓存照旧命中，直出旧推荐结果。实测：2026C 缓存是旧模块库时代生成的（OLED/LED 被归库外建议），库切换后缓存仍直出。CONTEXT 声称"库内命中每次现算，库会变"，但实现是整体直出。

**方向**：缓存校验加模块库指纹（如模块 slug + 摘要 hash）；库变 → 缓存失效走真实推荐。改动面：recommend_cache.py + webapp 装配点 + 测试。

**触发**：2026-08-19 用户遇到"库内明明有 oled 却提示需自备"（旧缓存直出）。

## 2. 多实例配置：AI 没猜实例时的自动兜底

**问题**：AI 猜实例（module-multi-instance/06）依赖模型按题面数量输出 instances——题面没写数量（如 2026C"声光提示"）时模型不猜，推荐后实例卡为空，用户要手动添加。

**方向**：推荐结果装配时，命中多实例模块（led）且 instances 缺省 → 补默认实例（平台默认：stm32 红黄绿 3 实例 / mspm0 单实例，与"不配置"的生成行为等价，零回归），实例卡直接可见可改；AI 猜了则用 AI 结果（现行为不变）。

**待确认**：~~默认实例形态（数量 / 变体）~~ 已定（2026-08-19 用户确认）：按平台默认自动填（stm32 红黄绿 / mspm0 单实例），已实现并合入（工单 instance-default-fallback/01）。

**触发**：2026-08-19 用户"能不能自动先帮我配置好，有问题我再手动改"。
