# 02 — 摘要段预算入账与真实库预算回归测试

**要做什么：** 把模块摘要段正式纳入推荐请求预算账本，并用真实库驱动的结构测试把「入库即红」守卫补上——模块库再涨一个模块，预算测试立即红（照词表段逐批红证校准先例）。

**被谁阻塞：** 01（需要预筛纯函数才能构造真实预筛载荷做校验）。

**状态：** resolved

**实施记录：** `budget.py` 注释追加「模块摘要段入账」段，`REFERENCE_FULLTEXT_BYTES` 60100 → 27000（推导：固定段 ≈56.6KB + 摘要段预算 40000 → 全文 ≈29.4KB 上界；红证取 28000 时 mspm0 最坏 128605B 距 129024 边界仅 419B 过紧，降 27000 保 ~3KB 呼吸）。红证：修复前 160705B > 129024 红；修复后 300 用例 test_llm 全绿。连带修正 3 个全文嵌入契约测试（全文样例降至新预算内：45000→25200、33KB→24KB、54KB→25.2KB wire，均保留「超 4000 字符上限仍全文在」语义）。mypy 清；全量 pytest 待 03 后最终确认。

- [x] `budget.py` / `llm.py` 记账注释按预算推导修订：摘要段从「14 条 ≈ 7.6KB」改为「按 MODULE_SUMMARY_BYTES 截断后形态」；`REFERENCE_FULLTEXT_BYTES` 配套下调，红证实测校准并保持最坏形态余量 ≥2KB
- [x] `test_llm.py` 新增 `test_recommend_real_library_budget`：扫真实 `library/modules`，mspm0 / stm32 两平台分别构造最坏形态（满额参考全文 + 20 条长澄清历史 + 15 条相关候选 + 真实词表 + 预筛后摘要），断言 `json.dumps` 序列化 ≤ `MAX_REQUEST_BYTES` 且余量 ≥ 2KB
- [x] 断言前先红证：用当前真实库确认未修复形态超标（照 measure_select_wire 实测口径），修复后转绿
- [x] 现有固定 14 条样例结构测试保留（最小结构测试），与真实库测试独立并存
- [x] 全量 pytest 无回归（词表段 / 预算相关既有测试不受影响）
