# 05 — 真机演练：拿线上真实包，在 8020 沙箱里当一次新用户

**要做什么：** 把改动**当成新用户**真跑一遍并留下证据：拿线上已发布的真实完整包（本机 `Desktop\firstep-pack\`
已有一份，免重下），用 **Windows 自带解压**（不用 7-Zip）解开，只按 README 与 `START-HERE.txt` 做，
在 **8020 沙箱**里跑完 `install.bat` → 桌面快捷方式 → 浏览器进入页面。
跑完必须能回答一句话：**一个新用户会不会卡住，卡在哪一步。**

**被谁阻塞：** 01（README 口径）+ 02（包内 START-HERE）。

**状态：** ready-for-agent

**分两轮（同一份协议与证据文件，用轮次标记）：**

| 轮次 | 何时跑 | 验什么 |
|---|---|---|
| **L1（立刻能跑，不等发版）** | 01 完成后 | README 指向的文件在 Releases 上**真的存在**、能被 Windows 自带解压；不存在的形态已从文档消失 |
| **L2（包内容改动后）** | 02 完成后 | 解压目录里 `START-HERE.txt` 在场，且**只照它做**能一路到网页能开 |

- [ ] 协议与证据落在 `.scratch/newuser-download/E2E-8020.md`（含每步命令、预期、实测、留痕路径）
- [ ] 证据文件 `verify-L1-*.txt` / `verify-L2-*.txt`，贴原始命令与输出（不是转述）
- [ ] **前置校正**：沙箱 `install.bat` 当前是旧版（2582 B，无第 5 步桌面快捷方式）——演练前用真身 v1.1.1 的同名文件同步一次，
      并在证据里记下「同步前 → 同步后」的字节数，否则演练的是历史版本
- [ ] 沙箱数据目录与端口口径正确：数据落 `C:\Users\luoji\.contest_generator_sim`，端口 **8020**，**不碰真身的 8000**
- [ ] L1：README 提到的完整包资产名 → `gh api` 确认**在同 tag 的 release 上存在**且字节数与 README 口径一致
- [ ] L1：用系统自带解压（Explorer / `Expand-Archive`，**非 7-Zip**）解开，确认解压耗时与产物根目录文件清单
- [ ] L1：按 README/START-HERE 逐行操作，**只允许使用文档明说的入口**；任何需要「猜」的地方记为卡点
- [ ] L2：解压后第一眼能看到 `START-HERE.txt`，且其内容与实际行为一致（步数、耗时口径、日志路径、端口）
- [ ] L2：`install.bat` 五步全过；桌面 `firstep` 快捷方式三属性正确（Target/Arguments/WorkingDirectory）
- [ ] L2：双击后浏览器自动打开 `http://127.0.0.1:8020`，首页可见（**到「配 key」前停住，不写真实 key**）
- [ ] L2：`%USERPROFILE%\.contest_generator_sim\webapp.log` 有本次启动记录、配置落点正确、真身数据目录**未被触碰**
- [ ] L2：连跑两次 `install.bat` → 幂等（第二次不重建快捷方式、不报错）
- [ ] 卡点清单：把每一处「文档没说、靠猜、走错路」的地方列出来 → 回填给 01/02 或新开小工单
- [ ] **诚实口径**：本机 Python 3.14.6 + Windows 11（26200），**不是**干净机器；演练结论里必须写明这一点，
      不得把本机结果说成「已在全新环境验证」

## 备注

- 沙箱的 `src/contest_generator/__init__.py` 被人为钉在 `1.1.0`（`local-environment.md` 记的故意不真实项），
  演练结束**不需要**改回来，但结论里要提一句它不影响下载/安装链路。
