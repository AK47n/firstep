# code-review — 工单 newuser-download/01

**审的对象**：`README.md`、`docs/agents/releasing.md`、`tests/test_onboarding_docs.py` 三处改动（`git diff` 201 增 / 46 删）。
**审的基准**：工单 `issues/01-readme-download-truth.md` + `.scratch/newuser-download/spec.md`。

## 轴一：Standards（是否符合本仓库明写的规范）

| 检查项 | 结论 | 依据 |
|---|---|---|
| 语言规范（用户/代理文档中文） | ✅ | 新增文字全中文；`Status:` 等标签值保持英文 |
| 不用英文提交信息 | ⏳ 待提交时遵守 | `.githooks/commit-msg` 会拦 |
| 测试只测外部行为、不测实现细节 | ✅ | 门禁断言的是「README 里有没有让人去找不存在的文件」这类**用户可观察事实**，不断言具体措辞结构（表格用「行内含关键词」而非精确列位） |
| 不高估守卫：守卫必须真会红 | ✅ | 探针 `verify-01-guard-effectiveness.txt` 用**旧 README 原文**做回归样本，5 条原因全部命中；另有 5 条违规写法逐条命中、9 条防坑写法逐条放行 |
| 注释解释「为什么」 | ✅ | 否定式判据的两处踩坑（Python 变长 look-behind 不支持、可选否定前缀会被引擎从动词处另起匹配）都写进了注释，避免下次有人"简化"回去 |
| PowerShell 脚本 UTF-8 BOM | ✅ 不适用 | 本工单未新增/修改 .ps1 |
| 不引入跨包字节口径风险（工单 full-download/08） | ✅ | `tests/test_pack_update.py` 12 条全过（含跨包逐字节不变量） |

**Standards 侧发现（1 条，已修）**：门禁初版把「**不用装** 7-Zip」这句**防坑提醒**判成了违规——
守卫会误伤好文案。已改为「命中工具名 + 回看否定词（按否定词长度给窗口）」的判据，并把该边界写进探针。

## 轴二：Spec（是否就是工单/规格要的东西）

| 工单验收项 | 结论 |
|---|---|
| 「获取方式」不再出现已下线形态 | ✅ 探针 + 门禁双验 0 命中 |
| 主路径写死在第一行：资产名 + 体积 + 系统自带解压 | ✅ 第 9~11 行 + 表格首行 |
| 「哪个文件给谁下」的表 | ✅ 4 行（完整包 / 小发版 / 清单类 / git clone），每行「给谁用」列都断言了 |
| 轻量路径保留但明确其定位 | ✅ 「不要资料库、且本机已有 git 与 Python 3.13+ 的人」 |
| 指向 `/releases/latest` 而非写死 tag 深链 | ✅ 并实测 HTTP 200 → `releases/tag/v1.1.1` |
| 「三步装好」补「以后双击桌面快捷方式」 | ✅ 第 2 步重写（顺带修掉「首次启动也让用户双击 start-app.vbs」的旧口径） |
| 全文不再二次描述体积/形态 | ✅ 顺带清掉两处「6 GB」（第 32、80 行）——那是 7z 时代的体积口径，留着就是第三处不一致 |
| 守卫扩展（负向 + 正向） | ✅ 新增 `test_readme_download_channel_is_live_shape` + 章内禁用词 |
| 全量回归 | ✅ pytest 4342 passed / 1 skipped；`node --test tests/js/*.test.mjs` 1554 pass |

**Spec 侧发现（2 条，已处置）**：

1. **越界但必要**：`docs/agents/releasing.md` 不在工单字面范围内，但它的「大发版」一节把 6.2 GB / 4 个 7z 分卷
   当作**新装主渠道**写着——不改它，README 改完就等于「文档内部两处口径打架」，而且下次发版会照它执行。
   处置：按 spec「7z 渠道口径下线」一并改，并补「Release 说明模板」与「附件命名注意」的中文名实测要求。
2. **我自己引入的时序错误（已修）**：表格首行原本写「解压后照包里的 `00-START-HERE.txt` 走」，
   但那个文件要到**工单 02** 才进包——等于让用户去找一个当期不存在的东西，正是本工单要根除的病。
   处置：改成指向 README 自己的「三步装好」；`00-START-HERE.txt` 的口径等 02 落地后再由 02 补写（门禁留给 02）。

## 结论

改动能通过 Standards 与 Spec 两轴，两处发现均已处置。**建议 resolved**。
唯一遗留：`00-START-HERE.txt` 的 README 指向要在工单 02 完成时补上（已写进 02 的验收项，防漏）。

---

# code-review — 工单 newuser-download/02（包内 `00-START-HERE.txt`）

**审的对象**：`00-START-HERE.txt`（新增）、`src/contest_generator/full_pack.py`（白名单）、
`tests/test_full_pack.py`（4 条新守卫）、`README.md` 与 `docs/agents/releasing.md` 的名字同步。

## 轴一：Standards

| 检查项 | 结论 | 依据 |
|---|---|---|
| 文件编码 | ✅ | UTF-8 **无 BOM**（守卫钉住；过程中被 PS 5.1 加过一次 BOM，当场被守卫抓住） |
| 面向使用者、不是贡献者 | ✅ | 全篇只讲「点哪个、等多久、去哪看」，不含开发/合规内容 |
| 守卫必须真会红 | ✅ | 摘掉白名单 → 两条 FAIL（有原始输出）；还原后逐字节干净 |
| 守卫不测实现细节 | ✅ | 断言的是「第一个可见文件是它」「README 点名的文件真在包里」这类用户可观察事实 |
| 注释解释「为什么 00-」 | ✅ | 白名单注释与守卫 docstring 都写了实测排序数据（#11/14、#17/22），防后人「顺手去掉这个奇怪的 00-」 |
| 不高估自己 | ✅ | 工单里明确写了本机不是干净机器、依赖未从零装这两条局限 |

**Standards 侧发现（1 条，已修）**：批量替换 `.scratch` 文档时套出 `00-00-` 双前缀（6 文件 / 28 处）。
已全部修正并复查为 0；教训（批量替换要幂等 + 断言条数）写进 02 的验收记录。

## 轴二：Spec

| 工单验收项 | 结论 |
|---|---|
| 根目录新增说明书、UTF-8 无 BOM、≤30 行 | ✅ 29 行，含三步 + 三条预期行为 + 出问题看哪里 |
| 登记进顶层白名单 | ✅ 并在打包真跑中验证进了 zip 与清单 |
| `scan_tree` / `excluded_paths` 双向守卫 | ✅ |
| 文档守卫（三步链路 needle） | ✅ 6 个 needle + `.venv` + 「重复」 |
| README 指向补上 | ✅ 改成「解压完第一个文件就是它」，并有跨文件守卫防再次指向不存在的文件 |
| 本机验证（真打包） | ✅ 8,776 文件 / 770.2 MB；清单 SHA256 与盘上一致 |
| L2 真机演练 | ✅ 全链路 PASS（见 05） |

**Spec 侧发现（1 条，已改设计并留痕）**：工单原定名 `START-HERE.txt`，实测排序掉到 README 之下
（等于没有），改名 `00-START-HERE.txt`。这是**对工单原文的有意偏离**，理由与证据都留在工单里。

## 结论（02）

两轴通过，`resolved`。

---

# code-review — 工单 newuser-download/04（`install.bat` 文案）

**审的对象**：`install.bat`（GBK 无 BOM + CRLF）、`tests/test_onboarding_docs.py`（2 条新守卫）、
`.scratch/newuser-download/patch-04-install-bat-copy.py`。

## 轴一：Standards

| 检查项 | 结论 |
|---|---|
| 字节口径（GBK 无 BOM + 全 CRLF） | ✅ 补丁脚本内置复验（无 BOM、裸 LF 计数为 0），既有守卫也仍在跑 |
| 改法可复现 / 有幂等断言 | ✅ 脚本化补丁，每条锚点断言命中**恰好 1 次**（不是「改完就算」） |
| 不动无关逻辑 | ✅ `git diff` 仅 4 增 2 删，全是文案；流程/步骤编号/快捷方式负载未动 |
| 守卫钉住新口径 | ✅ 「不用再」「桌面」与两个失败分支的 `python.org` + 「重新运行本脚本」 |

## 轴二：Spec

| 工单验收项 | 结论 |
|---|---|
| 完成提示讲清「以后不用再跑本脚本」 | ✅ 并对应 L2 卡点 K5 |
| `need_python` / `old_python` 补一句 | ✅ |
| 快捷方式失败分支的手动入口未丢失 | ✅ 本次未改该分支，既有守卫仍绿 |
| 真机跑一次 | ✅ 演练环境退出码 0、新提示在场、幂等 |

## 结论（04）

两轴通过，`resolved`。
