# 合并走查发现 → 改进包清单（2026-08-29 三子代理 + 范围外候选 + CSS 审计）

来源：findings-generate-flow.md（R1 生成主流程 23 条）、findings-library-settings.md（R2 库管理设置 14 条）、findings-feedback-consistency.md（R3 反馈一致性 25 条）+ ux-polish-02「范围外」候选 + css-consistency-audit/findings.md 剩余项。

## P0（数据丢失/主流程信息架构风险）
- **A. 危险操作确认补齐**：①覆盖式「重生成」reviseApply 加 danger confirmModal（展示模块集 diff + 将覆盖重建输出目录并整树备份）R1-1；②平台切换清空下游（模块+实例+引脚）确认 R1-3；③「全部还原默认」确认+toast 计数 R1-5。纯前端。
- **B. 6.5 多实例卡纳入步骤体系**：步骤6卡顶部徽章「还差 N 个实例」、未配实例计入摘要、实例配/清/删触发步骤7同步 R1-2/R1-6。纯前端。
- **C. 覆盖重生成 .bak 应用内恢复**：确认框补「找回」说明（桌面把 X.bak 改回 X）+ 结果区「恢复覆盖前备份」按钮 + 后端 rename 端点（is_unsafe_path 校验）R3-15。前端+后端小改。

## P1（首次使用/配置摩擦、等待与错误体验）
- **D. 设置页保存体验**：sticky 保存条 + 成功 toast + AI API 卡内「保存并连接」R2-4。纯前端。
- **E. 体检与工具链言行一致**：CCS 三件套纳入体检（逐行列出 SDK/编译器/SysConfig）、未就绪项「去设置填」跳转、统一叫法「Keil UV4（stm32）/ CCS+MSPM0 SDK（mspm0）」R2-6/R2-7；顺带工具链三件套内联探测回显（范围外候选）。前端+后端 env 探测扩展。
- **F. 录入/编辑绕路**：模块/参考录入「AI 出简介草稿」按钮挪到简介框同行、提交按钮贴末区、参考 3 区默认展开；参考条目「改文件内容」同名即覆盖一次 PUT（去掉先删再加）R2-1/R2-2；模块编辑弹窗合并「保存身份/推送新文件」为一个「保存」R2-3（含未推送计数角标）。前端为主，参考覆盖逻辑视后端 PUT 语义微调。
- **G. 错误人话化+可复制**：errors.py OSError 按 errno/winerror 分派中文+修复步骤（WinError32 关 Keil / 磁盘满 / 只读）、500 去异常类型名、backup_id 文案人话化（fix_errors.py:660/663、revision.py:96/99）；前端 500/长错误常驻内联块（可复制）+ toast 加复制按钮并延长时间；SSE 兜底统一 parseError R3-1/2/4/5/6、R1-12。前端+后端小改。
- **H. 长任务可感知进度+取消**：统一「已等待 X 分 Y 秒 + 当前阶段」秒表（复用 progress.js fmtClock）：推荐/修订/参数/编译修复/商量五类 R3-7/8/14；推荐端点发 start 带中文阶段 R3-7；生成端点伪阶段轮播改通用「正在生成工程（约几分钟）」R3-9；收敛进度条收敛态处理（converged→85%「正在出最终结果」）R3-12；分钟级加取消（AbortController 统一）R1-14。前端为主+推荐端点小改。

## P2（打磨，可选）
- **I. 删除类确认与撤销补齐**：最近记录/想法草稿删除接 confirmModal（+toast 撤销）；母版/PDF 删除迁 confirmModal 工厂；工厂默认 confirmText 加 console.warn R3-16/17/R2-10。
- **J. PDF 任意删除**：非重复 PDF 也给「回收」删除（复用 .trash-pdf 可恢复、被引用时确认框说明）R2-9。
- **K. 生成页状态按钮一致性**：「检查能否生成」忙碌态+就绪桥接（绿色总结「硬判据就绪→点生成工程」+软⚠中性化）R3-19/R1-22；步骤8双主色按钮降级 R3-18；生成成功不抢滚屏（停步骤9结果面板）R1-19；展开检查按钮 busy R1-16；预读失败按钮复位 R1-7；上传提取加载反馈 R1-8。
- **L. 术语与文案对齐**：桌面「自动加时间」→实际行为改写 R1-15；术语表补 自备/词表/重推/选型/补问 R1-23；「深化」三入口统一命名 R1-13；「已验证」词汇表统一 R3-20；新手指引「右上角」→「顶部导航」R2-13。
- **M. 键盘与可达性**：顶栏页签 ARIA tabs+方向键+roving tabindex；裸输入 aria-label；确认弹窗焦点管理（焦点陷阱+还焦）R3-21/22/23。
- **N. CSS 令牌化**：--radius-sm/md/lg/full + --space-* + .mt-* 工具类；按钮收敛 3 类；13.5→13/14、10/10.5→11；.tok-* 语法色/紫色族/青 rgba 裸值/绿完成族令牌化；topic-page img 白底等注释说明（css-consistency-audit + R3-24/25）。
- **O. 库页面小修**：参考/PDF 默认按最近更新降序 R2-8；加载失败清占位 R2-11；赛题库加载延迟 150ms R2-12；库目录卡 5 目录只读展示+切换确认+体检补行 R2-5。
- **P. 「和 AI 聊」入口收敛**：多入口（全局商量/每卡问这里/买件商量/参数速调问AI/12步交接）信息架构收敛 + 长跑屏幕（提炼/编译修复/修订）加「有问题？去问 AI」直达链接 R3-13 + 范围外候选。信息架构级，变更面最大，建议独立一期。
- **Q. 欢迎卡 compact 态加入口 + 窄屏响应式**（范围外候选，小；响应式未走查，风险中）。

## 推荐默认（P0+P1 = A-H 八包，全前端或前端+小后端，不与真实 LLM 推荐路径冲突）
A+B+C+D+E+F+G+H。P2 按需追加。
