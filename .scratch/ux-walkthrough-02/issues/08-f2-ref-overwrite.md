# 08 — F2 参考条目「改文件内容」同名即覆盖（一次 PUT）

**要做什么：** 编辑参考条目时，新增文件名与已有文件重名 = 用新内容覆盖该文件，一次保存完成（不再要求「先勾删保存、再回来添加」两步）；弹窗顶部提示文案同步更新，说明「同名文件将用新内容替换」。

**被谁阻塞：** 07（F1 编辑流程）——同文件（reference.js）改动，顺序执行避免冲突。

**状态：** resolved

- [x] 编辑弹窗提示文案改为「同名文件将直接替换」；删除「先删再加」步骤说明
- [x] doSave 识别同名新增并作为覆盖发送（一次保存请求完成替换与新增、删除）
- [x] 若后端文件计划语义不支持直接覆盖，则后端做最小扩展并补 pytest（同名替换保留原 metadata 如锚定/备注）
- [x] 既有「勾删」路径仍可用（删除已不想要的文件）
- [x] node --test +（后端若改）pytest 全量通过
