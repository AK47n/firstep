# 19 — M 键盘与可达性（顶栏页签 ARIA / 输入 aria-label / 确认弹窗焦点）

**要做什么：** ①顶栏页签遵循 ARIA tabs：容器 tablist、按钮 tab + aria-selected、roving tabindex、左右方向键循环切换（与页内页签行为一致）；②大量裸输入补 aria-label（名称与可见 placeholder/卡片标题一致）；③确认弹窗打开时焦点移入（默认焦点到确认或取消按钮）、Tab 焦点陷阱限制在弹窗内、关闭后焦点还给触发按钮。

**被谁阻塞：** 15（I 删除类确认与撤销）——确认弹窗工厂先统一调用方，再做焦点管理。

**状态：** resolved

- [x] 顶栏页签：方向键左右循环 + Home/End（每组独立）；aria-selected 随激活更新；roving tabindex 按组内生效（每组恒有一个 Tab 可达）
- [x] 裸输入均有 aria-label（a11y.js 补齐表；名称与可见 placeholder/卡片标签一致；有 label for 关联的不重复声明）
- [x] 确认弹窗：打开即聚焦（danger→取消钮/普通→确认钮）、Tab 不逃逸（首尾循环）、Esc/关闭后还焦触发钮（级联清理前捕获 + disabled 跳过）
- [x] 不破坏既有鼠标交互与页内页签行为（probe 冒烟无 JS 错误）
- [x] node --test 全量通过（nav-tabs-guard 更新 + a11y-labels 守卫）
