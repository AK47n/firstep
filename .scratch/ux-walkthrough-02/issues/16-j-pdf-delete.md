# 16 — J PDF 库支持回收任意单份

**要做什么：** 普通健康 PDF 与「疑似重复」一样可删除：①非重复 PDF 行与详情弹窗均提供「删除（回收）」按钮；②删除走既有回收目录策略（.trash-pdf，可恢复）；③被参考条目引用的文件在确认框中说明「删除后条目文件将无法打开」；④删除确认形态与其它库一致（走共享确认弹窗）。

**被谁阻塞：** 15（I 删除类确认与撤销）——确认形态统一后再接线，避免二次返工。

**状态：** resolved

- [x] 非重复 PDF 行/详情有删除入口；点击确认后进入回收目录并刷新列表
- [x] 被引用文件确认框含影响说明（若条目依赖此文件）；不被引用的说明「可恢复」；查询失败中性提示
- [x] 回收后列表统计与筛选随之更新；删除确认走共享弹窗
- [x] fx/ 纯函数（删除确认文案/引用判断）有 tests/js 用例（引用判断后端 pytest 兜底）；node --test 全量通过
