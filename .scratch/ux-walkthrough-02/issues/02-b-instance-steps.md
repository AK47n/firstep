# 02 — B 多实例配置卡纳入步骤体系（6.5 卡可见性与同步）

**要做什么：** 多实例模块展开后出现的实例配置卡不再游离：①步骤系统接受非整数步骤标识（如 6.5），使其可被导航/总览识别；②步骤 6 卡在存在多实例模块时顶部显示「还差 N 个实例」徽章（未配实例数）；③实例的配/清/删操作同步步骤 7 的就绪判定与徽章（与角色绑定路径一致）。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 存在多实例模块时，实例配置卡带非数字步骤标识并进入步骤导航/总览
- [x] 步骤 6 卡显示「还差 N 个实例」徽章；全部配齐后徽章消失或转为完成态
- [x] 实例配/清/删后步骤 7 徽章与就绪状态即时更新（无需刷新）
- [x] 实例缺口计算为 fx/ 纯函数并有 tests/js 用例（未展开、部分配、全配、多模块混合）
- [x] 既有 node --test 全量通过
