# 01 — A 危险操作确认补齐（覆盖式重生成 / 平台切换 / 还原默认）

**要做什么：** 三处危险操作在真正执行前都弹共享确认框，并给出影响说明：①「确认并执行修订」的覆盖式重生成（说明将覆盖重建输出目录、已整树备份，并展示模块集变更摘要）；②切换平台（说明将清空已选模块/引脚/实例）；③「全部还原默认」（说明将还原 N 个绑定，确认后用 toast 反馈还原数量）。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 覆盖式重生成触发前出现 danger 确认框，含模块集变更摘要（新增/移除/保留计数）与「覆盖重建 + 整树备份」说明；确认才执行，取消不改状态
- [x] 切换平台（非重按当前平台）前出现确认框，说明将清空已选模块/实例/引脚绑定；取消则保持当前平台与选择
- [x] 「全部还原默认」点击出现确认框；确认后清空绑定并 toast「已将 N 个绑定还原默认」
- [x] 新增摘要/文案生成逻辑为 fx/ 纯函数并有 tests/js 用例覆盖（含 0 项、全新增、全移除、混合场景）
- [x] 既有 node --test 全量通过
