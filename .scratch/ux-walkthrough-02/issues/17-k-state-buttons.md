# 17 — K 生成页状态按钮一致性

**要做什么：** ①「检查能否生成」加忙碌态（拉取/校验时禁用 + 转圈）与面板顶部绿色总结「硬判据就绪 → 点生成工程即可」；软性警告改中性「可选项」措辞，避免误导新手；②步骤 8 两个骨架主色按钮降级一个为次按钮，并各加一行短说明（生成骨架 vs 生成自检骨架）；③生成成功后不再自动抢滚屏跳到修复中心（停步骤 9 结果面板，修复中心仅给「自动编译中」提示）；④「展开依赖并检查平台」加进行中反馈并禁防连点；⑤预读失败后按钮复位「预读题面」；⑥上传并抽取文字加加载反馈（禁用 + 抽取中）。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 检查能否生成：点击后禁用+转圈，完成后恢复（行内容先同步渲染不被网络门控）；面板顶部有绿色「就绪→点生成」总结盒与「可选项」中性标注
- [x] 步骤 8 两个按钮主次分明（自检降为次按钮）、各有短说明
- [x] 生成成功不自动滚动；结果面板停在步骤 9 可见区，修复中心显示「自动编译中」提示而非抢占视口
- [x] 展开检查：进行中禁用+文案；预读失败：按钮复位可再点；上传抽取：禁用+「抽取中…」
- [x] node --test 全量通过（涉及状态文案的纯函数有用例）
