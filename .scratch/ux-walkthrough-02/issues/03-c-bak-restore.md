# 03 — C 覆盖重生成 .bak 应用内恢复

**要做什么：** 覆盖式生成（桌面同名完整工程）后，用户可在应用内恢复覆盖前的备份：①覆盖确认框与成功提示补充「如需找回旧工程，把桌面 X.bak 改回 X」说明；②生成结果区新增「恢复覆盖前备份」按钮；③后端新增安全恢复端点（仅允许把同名 .bak 目录重命名回原名，同款路径安全校验），恢复后前端给出结果提示。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 覆盖确认框 message 含 .bak 找回说明；成功 toast 仍保留备份名提示
- [x] 存在 .bak 时结果区/相关区域出现「恢复覆盖前备份」按钮；点击后调用新端点并反馈结果（成功改名 / 目标已存在冲突提示）
- [x] 后端端点有 pytest：正常恢复、目标已存在（拒绝）、路径越界（拒绝）、无 .bak（友好报错）
- [x] 路径校验基于既有 is_unsafe_path 同款策略；不触碰生成/LLM 链路
- [x] node --test + pytest 全量通过
