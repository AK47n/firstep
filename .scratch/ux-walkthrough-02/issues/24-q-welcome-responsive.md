# 24 — Q 欢迎卡 compact 态入口 + 基础窄屏响应式

**要做什么：** ①欢迎卡 compact 态（已配 key、无草稿）补主行动入口（如「去生成」或「打开新手指引」），与 full 态路径呼应；②关键断点基础响应式：顶部导航、生成区、库工具栏在窄屏（约 ≤900px）不横向溢出、按钮不挤压（最小改动，不重排整页）。

**被谁阻塞：** 无——可立即开始（放最后避免与其它工单的 index.html 改动冲突）。

**状态：** resolved

- [x] compact 态欢迎卡显示至少一个主入口按钮；点击进入对应页签/动作
- [x] 窄屏（900px 与 720px 两档）顶部导航与生成区无横向溢出、按钮可用
- [x] 宽屏布局不受影响（回归截图确认）
- [x] 欢迎卡 HTML 生成纯函数有用例（compact 含入口 / full 与 hidden 不变）；node --test 全量通过
