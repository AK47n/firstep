# 12 — H1 长任务秒表与阶段文案统一（推荐/修订/参数/修复/商量/预读）

**要做什么：** 所有分钟级等待都有「已等待 X 分 Y 秒 + 当前阶段」的可感知状态：①既有计时格式化复用为一个 fx 纯函数（或既有 fmtClock 直接复用）；②AI 推荐、修订/深化、参数速调、编译修复、AI 商量、赛题预读六类等待统一显示已等待时长与一行当前阶段/说明文案（阶段来源：已有事件/operation 时取用，无则用通用「AI 正在处理…（可能要几分钟）」）；③不改变既有完成/失败的展示逻辑。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 六类长任务等待区域出现「已等待 X 分 Y 秒」计时（进页面即开始、结束即停）——推荐流复用 recPanel 双计时，其余五流 makeWaitClock
- [x] 有阶段的场景显示中文阶段名（如「正在选模块」「正在校验」）；无阶段场景显示通用提示（WAIT_GENERIC_LINE；推荐 start 事件具体标签见工单 13）
- [x] 秒表格式化纯函数有 tests/js 用例（0 秒/1 分/小时级/进位）；与既有 fmtClock 行为兼容（waitLabel 直接复用）
- [x] node --test 全量通过
