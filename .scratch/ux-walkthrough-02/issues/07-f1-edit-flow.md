# 07 — F1 录入/编辑绕路（草稿按钮同行、提交贴末区、参考全展开）

**要做什么：** 模块与参考条目的录入表单不再跨区绕路：①「AI 出简介草稿」按钮移到简介输入框同一行（点击后结果直接显示在简介框毗邻，不再散落其它区）；②「校验并入库」/「入库」提交按钮紧贴最后一区（不再在 3 个折叠区之外的最底部）；③参考录入的 3 个折叠区默认全展开（仅保留一个总收起）。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 模块录入：草稿按钮与简介框同行，草稿结果落到简介框（可改可提交）
- [x] 模块录入：提交按钮在最后一个折叠区之后紧邻（或随区常驻），不再远离
- [x] 参考录入：3 区默认展开，草稿按钮/简介框/反馈集聚同区同线；提交按钮位置同样贴近
- [x] 展开/收起状态不破坏既有校验与提交逻辑（表单提交仍走原接口）
- [x] node --test 全量通过
