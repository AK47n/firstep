# 05 — E1 后端体检扩展（CCS 三件套 + 派生库目录存在性）

**要做什么：** 环境体检接口新增两类探测并逐行返回：①MSPM0 的 CCS 三件套（SDK 路径 / 编译器 / SysConfig，从设置配置探测实际可用性）；②赛题 / 参考 / PDF 库目录的存在性与可写性（派生自模块库目录）。既有 stm32（UV4）与 gmake 探测保持不变。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] /api/env/status 响应含 mspm0 三件套探测结果（每项：已配置/未配置；存在的路径判定有明确规则）
- [x] 响应含 topic/reference/pdf 库目录存在性行
- [x] 探测不要有副作用（只读文件系统状态）；未配置时返回「未设置」而非报错
- [x] pytest 覆盖：未配置 / 部分配置 / 路径存在与不存在 / 目录派生关系断言
- [x] pytest 全量通过
