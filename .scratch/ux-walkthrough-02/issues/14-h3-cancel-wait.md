# 14 — H3 分钟级长任务「取消本次等待」

**要做什么：** 分钟级同步长任务（拆解/执行/深化/参数速调/AI 对话/预读）等待期间提供「取消本次等待」：①用 AbortController 中止客户端请求；②取消后状态区明确显示「已取消，工程/数据未被修改，可安全重试」或对已产生的部分结果说明去向；③取消按钮复用统一样式，不与既有完成/失败状态冲突；SSE 运行器的「连接中断可重试」提示也给出取消路径。

**被谁阻塞：** 12（H1 秒表结构）——取消入口附加在等待状态条上。

**状态：** resolved

- [x] 六类同步长任务等待期出现「取消本次等待」按钮；点击后请求中止、状态区给出取消反馈（含任务卡商量/买件商量）
- [x] 取消语义明确：SSE 流水线（后端继续）＝「后台继续执行」；同步对话（后端落盘）＝「会话以后端为准」；预读（不落盘）＝「未保存可重试」
- [x] AbortController 封装为 fx/abortable.js 可复用模块，有 tests/js 用例（中止触发、幂等、begin 先取消旧实例、isAbortError 识别）
- [x] node --test 全量通过
