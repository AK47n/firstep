有。编辑区里能看到括号被高亮成 **3 种不同颜色**：

- **黄色/金色**：第 2 行 `void f(void) {` 里的 `(`、`)` 和末尾 `{`，以及第 9 行 `}`。
- **紫色/粉色**：第 3 行 `if (` 与 `)`；第 3 行末尾 `{` 与第 8 行 `}`。
- **蓝色**：第 4 行 `while (` 与 `)`、末尾 `{` 与第 7 行 `}`；第 5 行 `s[]` 的 `[` `]`；第 6 行 `x()` 的 `(` `)`。

另外，第 1 行注释里的 `(` 和第 5 行字符串里的 `{` 是注释/字符串颜色，不算括号高亮。
