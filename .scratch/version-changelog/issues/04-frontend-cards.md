# 04 — 前端版本卡片折叠页 + tab 改名挪组

**要做什么：** 「更新记录」栏目焕新成「版本更新记录」：
tab 改名并挪入「指南」组；页面渲染版本卡片（版本徽章 + 日期 + 一句话主题 +
带语义标签的要点列表），默认只展开最新版本、其余折叠（点标题切换，`aria-expanded`
同步）；空态改为「还没有正式版本——发布新版本时会在这里归纳对用户重要的要点」。
数据来自 03 的新契约；样式复用既有 token。

**被谁阻塞：** 03（拿到新数据形状）。

**状态：** resolved

**审查结论（code-review 双轴）与整改：**
- 标准轴：硬违规 1 条——`.rel-tag.tag-perf` 背景硬编码 rgba 不随主题——已修：
  新增 `--purple-dim` 令牌对（暗 #8b5cf6 / 亮 #8250df 各一套）并改用；模块头
  L1 旧式注释同步（更新记录 → 版本更新记录 + 新工单号）；nav 键列表两处
  数组顺序对齐新 DOM 序（成员校验语义不变）。折叠机制重复为判断项，遵循
  仓库「参数化劣于清晰重复」惯例保留（本卡片折叠是无持久化的轻量版）。
- 规格轴：五条验收逐项落地，无实质缺失；范围蔓延四项均为合理设计延伸
  （「N 条」计数、📦 空态图标、hover 高亮、标签色）——已记录；
  「最新版 = 数组首位」依赖「新版本永远在最前」书写约定（spec 格式契约），
  解析器按文件顺序、前端不排序不为错（契约已约束）。
- 验证：CDP 探针实测（样例版本数据）卡片渲染 2 张 / 最新默认展开 / 次版
  折叠 / 点击翻转 aria-expanded + collapsed 双向 / 六类标签齐 / 真实 API
  空态文案正确 / tab 归属「指南」组 / 零 JS 错误；截图存
  .scratch/version-changelog/shot-04-*.png。

- [x] tab 文案「版本更新记录」、title 面向用户说明、位于「指南」组
- [x] 版本卡片渲染：徽章 / 日期 / 主题 / 标签条目（新增|改进|修复|性能|其他 配色语义化）
- [x] 默认仅最新版展开，其余折叠；点击标题折叠/展开 + aria-expanded + chevron 旋转
- [x] 空态文案（无版本区块时）
- [x] 页头 h2 与说明同步；手动开 app 验证视觉与交互


## Comments

- 2026-09-09 在途盘点（第二轮）：本单未勾项经代码事实逐条核对，判定全部为「已实现（勾选没跟）」——证据见 `.scratch/tracker-audit/2026-09-09-在途盘点.md`（判定总表按批次给出 `文件:行号` / 测试文件名 / grep 否证）。本次只勾选 + 状态归一 resolved，未改任何验收项文字。
