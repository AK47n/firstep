# 08 — 库页体验包前端（排序/刷新/空态/统计提示）

**要做什么：** 依赖工单 07 的 mtime 字段。落地四库可发现性改进：① 模块库/参考库/赛题库排序下拉新增「最近更新」（默认升序、可切降序，与其他维度一致）；② PDF 资料库工具栏加「刷新」按钮（重拉列表并 toast）；③ 母版库空表渲染空态引导（提示从「母版提炼」开始）；④ 四库统计条上可点击的问题计数附带可见「点击可筛选」提示（保留 title 详细说明，激活态沿用既有 .on 样式）。

**被谁阻塞：** 07（mtime 排序数据源）。

**状态：** ready-for-agent

- [ ] 三库排序下拉含「最近更新」项，选后按 mtime 倒序/升序渲染（与 dir 切换按钮联动）
- [ ] mtime 缺失条目（0）排在最后，不报错
- [ ] PDF 库「刷新」按钮：重拉列表、保留当前过滤/排序条件、成功后 toast；空态文案提示可点刷新
- [ ] 母版库空表渲染空态（图标 + 标题 + 引导文案），非空表行为不变
- [ ] 四库统计条可点击计数追加可见「点击可筛选」弱化提示；点击后激活态渲染不影响既有过滤逻辑；再次点击取消仍工作
- [ ] 新增纯函数单测：三库 mtime 排序键、母版空态 HTML 断言（如抽取）

