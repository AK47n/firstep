# 05 — 前端「修订与深化」阶段卡：两条入口 + 流程串联 + SSE 进度

**要做什么：** 生成页「修复中心」之后新增「修订与深化」阶段卡，串起完整用户体验：两条入口（当前会话一键直取上下文；历史目录 = 选目录 → 加载 → 缺题面时补输入框），粘贴新 Q&A → 点「分析」→ 展示逐条影响结论 + 模块增删 diff 卡 + 平台警告变化 → 用户确认 → 勾选可选深化 → 「执行」→ SSE 进度（分析 / 备份 / 重生成 / 深化 / 编译验证）→ 结果展示（diff 记录、验证状态、回滚按钮）。模块集无变化时界面提示"无需重生成"，可直接进深化。

**被谁阻塞：** 02、03、04（后端 API 就绪后串联）

**状态：** ready-for-agent

- [ ] 阶段卡两条入口可用：当前会话直取 / 历史目录选择 + 加载 + 补题面
- [ ] 分析 → 影响结论 + diff 卡 + 警告变化展示；可放弃
- [ ] 确认 → 执行（含深化勾选）→ SSE 进度全程可见
- [ ] 结果：diff 记录 / 验证状态 / 回滚按钮（回滚后状态刷新）
- [ ] 无变化提示：模块集不变 → "无需重生成"，深化可直接进行
- [ ] 错误路径：加载失败 / 分析失败 / 执行失败 / 无工具链降级，均有中文提示
