# 03 — install.bat 首次自动指向随包 library + 设置读取识别磁盘库目录

**要做什么：** 首次安装零手工：install.bat 在配置文件不存在时写入最小引导配置（模块库/母版库 = 随包 library 子目录，api_key 空串），应用保持「未配置」状态但库目录被设置读取接口识别（避免首次保存设置时被默认值覆盖）；已有配置一律不动。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] 配置层新增纯函数：写入最小引导配置（仅文件不存在时；api_key 空串；目录字段非空字符串）
- [x] 配置层新增原始字段解析 helper：配置文件存在且 JSON 可解析时取模块库/母版目录文件值（非空字符串），否则回退默认
- [x] 设置读取接口：未配置 key（config=None）路径下，模块库/母版目录返回 helper 结果；已配置路径行为不变
- [x] install.bat：装依赖后、自检前调用写入函数（传 %~dp0 下 library 子目录；文件已存在跳过；GBK 中文提示风格保持）
- [x] pytest：写入函数（不覆盖既有、空 key、目录值）、helper 回退规则、设置接口端到端（配置文件存在无 key → 返回文件库目录）
- [x] 全量 pytest 无回归
