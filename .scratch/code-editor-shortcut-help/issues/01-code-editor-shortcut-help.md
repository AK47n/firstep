# 工单 01 — 状态栏「快捷键」按钮 + 快捷键帮助弹窗

**要做什么：**
代码页状态栏最右侧新增「快捷键」按钮；点击弹出居中模态「键盘快捷键」，分组（编辑/查找/视图）列出全部常用快捷键与鼠标手势（Ctrl+S 保存 / Tab 缩进 / Enter 自动缩进 / 开括号自动补对 / Backspace 删空对 / 中键关闭标签 / 拖拽排序 / Ctrl+F / Ctrl+H / Enter·Shift+Enter 上下一个 / Esc 清查找 / Ctrl+Shift+[·] 折叠展开 / Ctrl+滚轮 缩放）。键位键帽样式（`.code-kbd`），说明数据单源纯函数（fx/code-shortcuts.js），弹窗关闭：× / 遮罩 / Esc，关闭后焦点还给按钮，Tab 焦点循环（同冲突弹窗先例）。

**被谁阻塞：**
无（状态栏既有结构、遮罩弹层模式均已在册）。

**状态：** resolved（双轴评审通过：无硬违规；整改 3 项——keydown 监听泄漏 / 键位数组化 / 测试断言收窄）

**验收：**
- [x] 状态栏出现「快捷键」按钮（复用状态栏按钮统一样式，focus-visible 生效）
- [x] 点击弹出；标题「键盘快捷键」；三组各条目齐全；`/` 替代键位拆两个键帽
- [x] Esc / × / 遮罩三路关闭；关闭后焦点回按钮
- [x] 内容转义；`.code-*` 前缀 + 既有 token；css-tokens 守卫通过
- [x] tests/js/code-shortcuts.test.mjs（纯函数）+ 冒烟 smoke-09（开/关/内容）通过
- [x] node --test "tests/js/*.mjs" 全量回归通过；中文提交


## Comments

- 2026-09-09 在途盘点（第二轮）：本单未勾项经代码事实逐条核对，判定全部为「已实现（勾选没跟）」——证据见 `.scratch/tracker-audit/2026-09-09-在途盘点.md`（判定总表按批次给出 `文件:行号` / 测试文件名 / grep 否证）。本次只勾选 + 状态归一 resolved，未改任何验收项文字。
