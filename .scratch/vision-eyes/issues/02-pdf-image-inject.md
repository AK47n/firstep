# 02 — PDF 嵌入图提取与图注注入

**要做什么：** extraction 扩展：电子版 PDF 抽文本后用 `page.images`（pypdf 6 自带，零新依赖）提取嵌入图 → 逐张 describe_image_cached → 图注段 `[示意图N：<描述>]` 追加进题面文本尾部；单文件 ≤ 8 张、单张 ≤ 4MB（超限跳过标注）；视觉失败逐张降级、整文件视觉失败 = 只用文本（现行为逐字节不变）；集成测试用构造含图 PDF + 假视觉。

**被谁阻塞：** vision-eyes/01（视觉通道）。

**状态：** resolved

- [x] _extract_pdf 后处理：page.images 提取 → 上限守卫（8 张 / 4MB）→ 逐张描述 → 图注追加
- [x] 未配视觉 key / 网络失败 / 解析失败 → 静默降级（文本照常，逐字节兼容）
- [x] 无图 PDF / 无文本 PDF（纯文本场景）→ 零变化
- [x] 集成测试：构造含图 PDF（pypdf PdfWriter + 仓库小图片）→ 假视觉 → 文本 + 图注；失败路径降级
- [x] 全量测试通过


## Comments

- 2026-09-09 在途盘点（第二轮）：本单未勾项经代码事实逐条核对，判定全部为「已实现（勾选没跟）」——证据见 `.scratch/tracker-audit/2026-09-09-在途盘点.md`（判定总表按批次给出 `文件:行号` / 测试文件名 / grep 否证）。本次只勾选 + 状态归一 resolved，未改任何验收项文字。
