# 03 — 编辑器查找补替换（全部替换 + Ctrl+H）

**要做什么：** 代码栏当前文件内查找旁新增「全部替换」（替换输入 + 按钮 + Ctrl+H 唤起），替换进编辑缓冲（脏点出现，Ctrl+S 落盘），只读标签拒绝替换。

**被谁阻塞：** 无——可立即开始。

**状态：** resolved

- [x] fx 纯件 replaceAllText(src, needle, replacement) → {count, value}（空针/无匹配 → count 0；有匹配 → 全部替换），fx-guard 全域表登记
- [x] 单测：空针/无匹配/单处/多处/替换含特殊字符/替换为空串（tests/js/codeeditor.test.mjs）
- [x] UI：查找行下新增替换行（替换输入 +「全部替换」按钮）；Ctrl+H 与 Ctrl+F 同口径（仅代码 tab、.md 预览先切源码、展开侧栏聚焦替换输入）
- [x] 替换语义：模拟手输路径（textarea 值 + 高亮/行号/脏点/标签条同步），光标置文件尾；只读标签/无匹配 → 提示且不改；不自动写盘
- [x] 冒烟：smoke-03.mjs 8 项全绿（含 md 预览态替换先切源码——Spec 轴评审发现的边缘路径）

## Answer

- 已验证：smoke-03.mjs 8 项全绿；单测 5 项新增；评审整改：替换按钮点击前补 .md 预览切源码（与 Ctrl+F/H 同口径，修复「预览态静默失效并误报成功」）。
