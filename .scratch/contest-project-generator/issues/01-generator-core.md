# 01 — 生成器核心骨架 + fixture 测试基座

**What to build:** 定义并实现工程生成的核心逻辑：接收（目标平台、已选模块的 manifest 集、母版工程路径、main.c 骨架内容），产出完整工程目录——模块文件按平台版本复制、main.c 落位、结构就绪，并把平台工程修改委托给注册表里的插件（本工单先留桩）。同时定稿 manifest JSON 数据模型，并建立 fixture 测试基座（假母版、假模块库、假 LLM 返回），使后续所有工单都能在这个唯一接缝上测试。

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] manifest 数据模型可解析/序列化：功能简介、依赖列表、平台条目（每平台文件列表、验证状态、硬件绑定标记、备注）
- [x] 核心接口接收（平台、已选 manifest 集、母版路径、main.c 内容）并输出完整工程目录：模块文件按平台版本复制，main.c 落位
- [x] 平台修改器通过注册表调用：接口已定义，本工单用桩实现，核心与桩解耦，不绑定任一平台格式
- [x] 文件完整性自检：所选模块文件不齐全时生成失败并给出明确信息，而非产出残缺工程
- [x] fixture 测试：假母版 + 假模块库 + 假 LLM 驱动下 pytest 全绿，断言输出目录结构与文件内容
