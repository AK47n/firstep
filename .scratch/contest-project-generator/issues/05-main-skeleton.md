# 05 — main.c 骨架生成 + 函数自检

**What to build:** 用户拿到生成的工程时，main.c 已由 AI 排好初始化调用序列：只调用所选模块头文件中真实存在的函数，带注释与预留编写区，不确定处注释占位，保证可编译。

**Blocked by:** 01 — 生成器核心骨架 + fixture 测试基座

**Status:** ready-for-agent

- [ ] LLM 基于所选模块的头文件接口生成 main.c 骨架：初始化序列、注释、预留编写区
- [ ] 静态自检：main.c 引用的每个函数都存在于所选模块头文件中；不存在的调用被拦截（改为注释占位或明确报错）
- [ ] 占位处理：AI 不确定的调用以注释形式标注，不凭空造函数
- [ ] fixture 假 LLM 下测试全绿：断言骨架结构与自检行为
