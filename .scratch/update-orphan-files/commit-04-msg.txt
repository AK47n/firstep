验收：离线演练 + 账本收口——盘面与全新安装一致（工单 update-orphan-files/04）

本机离线口径（不发版）：重建真 v1.1.1 沙箱（8.7s）→ 植入 5 条「v1.1.1 发过、本版不再发」的
真实残留 → 用本机修复后的打包器打小发版包（11.1s）→ 产品自带更新器应用（--no-stop
--no-restart --skip-pip，10.6s）→ 构成复算。判据全绿：not_in_official == 0（改动前同口径
1482）、盘上 3071 / 完整包 3071（一致 3068、不同 3 = egg-info、多出 0、缺 0）、植入残留全清、
包内产品文件 2858 条（0 个 revise-backups / 0 个 *.exe）、应用后盘上 1.2.1；8020 释放、
真身数据目录未变、8000 上用户自己的实例全程未被碰。判红 0 / PASS。

演练逮到并修掉两个真问题：
① 删除基准拿小发版清单算，把「完整包会发、但未被 git 跟踪」的产品文件（Project.uvguix.luoji、
   sources/contest/** 下的构建产物与 pdf）写进了删除清单 → 升级时真被删（那一跑
   not_in_official 成立、盘上却少了它们）。修法：新增 full_pack.product_paths（扫树取路径，
   不算哈希）+ pack_update.current_release_files（小发版清单 ∪ 工作树产品文件），删除基准取
   并集；删除清单 7509 → 4599 条。
② 打包脚本按 tag 找基线找错：PS 的 GetFileNameWithoutExtension 只削一层扩展名，把
   firstep-update-v1.1.1.files.txt 切成 v1.1.1.files → 找一个不存在的兄弟名 → 拒绝发版。
   修法：tag 推导与两向定位收进 Python（release_tag_of / find_baseline_parts /
   find_update_files_for）+ 单测 test_release_tag_of_handles_dotted_tags。

账本：backlog 第 8 节三张单按更正口径重写（本轮不发版）；local-environment 第 0 节（挂账 6 笔
+ 两个工具坑：.ps1 的 BOM 会被编辑工具吞掉、探针被强杀会留注入态）、第 1 节（沙箱 = v1.1.1
起点 + 本机修复包）、第 1.5 节（B2 两格已转绿）、第 2 节（8000 现在在跑 = 用户自己的实例）、
第 3 节（main 领先线上资产）。全套测试 4578 passed / 1 skipped。
