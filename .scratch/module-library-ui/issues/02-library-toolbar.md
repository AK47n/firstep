# 02 — 模块库工具栏与统计条（搜索 / 过滤 / 排序 / 统计）

**要做什么：** 表格上方新增工具栏：关键字搜索（匹配 slug / 简介 / 依赖 / 套件 / 备注，
大小写不敏感子串）、平台过滤（全部 / 各平台）、状态过滤（全部 / 已验证 / 未验证 /
硬件绑定）、排序（slug / 平台数 / 依赖数，可逆序）；表格下方或工具栏内嵌统计条：
模块总数、各平台计数、已验证数、硬件绑定数、互斥组数。全部客户端过滤 ——
`GET /api/modules` 数据已全量在手，不新增服务端参数。交互逻辑下沉纯函数。

**被谁阻塞：** 01——表格区域先完成样式改造再挂工具栏逻辑，避免同区域并行冲突。

**状态：** resolved

- [x] 纯函数（过滤 / 排序 / 统计汇总）在 tests/js 有 node:test 单测：匹配规则、
      排序稳定性、统计数字正确性（16 例：关键字/平台/状态/叠加/稳定性/统计/文案/chips/null 条目）
- [x] 搜索框输入即过滤（无需点按钮），清空按钮恢复全量；空结果有友好提示
- [x] 平台与状态过滤为单选 chips（或等效控件），与总搜索条件叠加生效
- [x] 统计条数字与当前库全量（过滤前）一致：总数 / 平台计数 / 已验证 / 硬件绑定 /
      互斥组数（exclusive_group id 去重）
- [x] 过滤 / 排序为纯函数调用，DOM 层只做转发；Esc 或再点一次可清除过滤
- [x] 现有 tests/js 全绿（295）、pytest 全量全绿（2365）、冒烟清单不回归（20 项）

**评审修订（两轴并行）：** ①[HIGH] 排序接线字段错位——`libSortModules` 读 {by,dir}
而调用点直传 libUI{sortBy,sortDir} → 排序全程失效（冒烟首行比对因 adc 双占首位侥幸
PASS）；已改为调用点映射并升级冒烟为全行序列比对；②[LOW] null 平台条目的统计与过滤
语义不一致 → libStats 跳过 null 条目计数 + 回归用例；③内联 font-size 冗余（.muted 已
12px）删除；④/// 注释统一 //；⑤单测多余 esc 依赖清理。
