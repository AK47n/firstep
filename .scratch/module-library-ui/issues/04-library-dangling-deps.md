# 04 — 悬空依赖检测与警示

**要做什么：** 模块库级数据质量体检：检测「模块声明依赖的 slug 在库中不存在」的
悬空依赖，行内 slug 旁显示警示标（title 给出缺失清单），统计条显示红色警示计数。
纯函数判定（比较 dependencies 与库内 slug 集合），零后端。

**被谁阻塞：** 02——警示计数挂在统计条上，行内警示标与统计条同轮落地。

**状态：** ready-for-agent

- [ ] 纯函数 `danglingDependencies(modules)` 在 tests/js 有 node:test 单测：
      正常依赖不报、缺失依赖报出、多缺失合并、空依赖不报、重复声明不重复报
- [ ] 行内 slug 旁警示标：hover / title 显示缺失依赖清单（含被哪个模块引用）
- [ ] 统计条红色计数随库刷新（有悬空 = 显示「悬空依赖 N」，无 = 不显示或灰置）
- [ ] 修好后（重新入库缺失模块）刷新即消失，无需重启
- [ ] 现有 tests/js 全绿、pytest 全量全绿、冒烟清单不回归
