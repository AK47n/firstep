# 06 — 「编辑」弹窗（套件 / 购买链接 + 平台版本文件增删）

**要做什么：** 每行新增「编辑」按钮，弹窗承载平台级编辑：① 硬件身份——套件型号 /
购买链接（`PUT /api/modules/{slug}/platform-identity`，空值 = 保留原值的补填语义）；
② 平台版本文件——列出该平台文件清单并可删除（`DELETE /api/modules/{slug}/platform-files`）、
新增文件（选磁盘文件或粘贴内容，`POST /api/modules/{slug}/platform-files`，复用添加
表单的文件行模式）。所有操作即时生效：成功返回新 manifest → 刷新表格；失败展示原因。

**被谁阻塞：** 05——模态骨架（开合 / 保存中 / 失败反馈）先由 05 落地，本工单复用
同一套模态模式。

**状态：** resolved

- [x] 每行「编辑」入口 → 模态；平台下拉列出该模块的平台版本，切换展示对应条目
- [x] 套件输入框带已有 kit 词表提示（datalist，词表 = 库内所有平台条目 kit 去重），
      购买链接保存前做 URL 格式校验（空 = 保留原值）
- [x] 平台文件清单展示（含「内嵌母版」态），可删除 → `platform-files` DELETE；
      删除前置确认（逐文件 ✕ 删除，接口支持 filenames 列表——Spec 轴判定
      「可多选删除」为可接受口径差异）
- [x] 新增文件：文件行（文件名 + 内容 / 磁盘选取），提交 → `platform-files` POST
- [x] 保存成功 → 表格与统计条即时刷新；失败展示后端错误消息，表单保留
- [x] 只读字段（验证状态 / 备注 / 简介）可展示在本弹窗但不提供编辑（提示走对应入口）
- [x] 现有 tests/js 全绿（305）、pytest 全量全绿（2365）、冒烟清单不回归（40 项）

**评审修订（两轴并行）：** ①[LOW-MEDIUM] 删除某平台最后一个文件后后端移除平台条目，
平台下拉未重算 → rebuildPlatSelect() 重建下拉并落位到剩余平台，状态区不再误导；
②[建议] 保存/推送/删除 await 后补 isConnected 守卫（与 05 对称；数据始终更新，
仅跳过已移除模态的 DOM 渲染，05 同步同构修正）；③[建议] .lib-mod-msg 红色警告兜底
（.ok 覆盖成功态）；④[不修] kit 词表与 loadKitVocabulary 概念重复——数据源/次序
不同、收敛风险大于收益，保留独立实现；⑤冒烟安全考量：不点真实删除（保真实库），
删除成功路径由静态代码保证。
