# 04 — 2025 控制题拆条（E/H 两题入库）

**要做什么：** 从 `library/topics/*/000_2017-2025_全国大学生电子设计竞赛真题汇编.pdf`（34MB，261 页，2025 章节 = p235-260）拆出 **2025E 简易自行瞄准装置（p249）/ 2025H 野生动物巡查系统（p258）** 两条目，复用既有机制、**零 LLM 改写**（流程与工单 03 完全一致）：

1. **定位页**：fitz 文本定位 2025 章节内两题边界——（E 题）/（H 题）起始页为准（p249/p258 锚点，精确边界执行时核实）。
2. **小题 PDF**：fitz 提取各题页 → `.scratch/topics-control-2023-2025/pdf/2025E.pdf` / `2025H.pdf`（几百 KB；不放 34MB 副本）。
3. **确定性拆条**：`split_topics_document(text)`（`topic_library.py:567`）得 TopicDraft；输出 ≠ 1 条用章节切片兜底；**不经 LLM 拆条分支**。
4. **结构补全**：年份标题 / `## 参赛注意事项` 确定性拼接（照 2026H 结构）；不 AI 生成。
5. **视觉核对**：渲染 PNG + read_image 检查文本层完整性（漏图/表则人工补录并记录）。
6. **入库**：`confirm_topics`（或 /api/topics/confirm）：entries = [{year: "2025", number: "E"/"H", problem_text, category: "control"}]，program_dirs = [] → 条目目录 `2025E`/`2025H` + topic.md + 小题 PDF + manifest（original_pdf = 小题 PDF；category=control）。
7. **图注**：入库后 GET /api/topics/2025E 等触发 `enrich_topic_image_notes`（三级降级、幂等、失败静默）——不手写。
8. **断言**（每题）：topic.md 含「一、任务」「四、评分标准」；manifest 字段齐全 + category=control；original_pdf 存在且 < 1MB；`resolve_number` 可解析。

**被谁阻塞：** 01（confirm 需支持 category 落盘）。

**状态：** ready-for-agent

**验收：** 全部 ✓（两题入库；步骤 2-8 清单全过；pytest 全绿；git 提交 = 2 个条目目录 + 拆分脚本，无 34MB 副本；视觉核对记录在工单）。

- [ ] 定位两题精确页边界（题目标记核实）
- [ ] 提取 2 份小题 PDF（< 1MB/份）到 .scratch
- [ ] split_topics_document 拆条（或章节切片兜底）——零 LLM
- [ ] 结构补全（照 2026H）
- [ ] 渲染 PNG + 视觉核对文本层完整性（记录差异与补录）
- [ ] confirm_topics 入库（category=control；programs=[]）
- [ ] 触发 enrich_topic_image_notes 补图注（失败静默记录）
- [ ] 断言 + pytest 全绿 + git 提交核实（无 34MB 副本）

**参考：** 工单 03-split-2023（同流程）；`.scratch/real-run/topic_split_by_year.py`；spec 三-C。
