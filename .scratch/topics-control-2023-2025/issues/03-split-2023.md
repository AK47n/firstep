# 03 — 2023 控制题拆条（E/G/I 三题入库）

**要做什么：** 从 `library/topics/*/000_2017-2025_全国大学生电子设计竞赛真题汇编.pdf`（34MB，261 页，2023 章节 = p171-205）拆出 **2023E 运动目标控制与自动追踪系统（p183）/ 2023G 空地协同智能消防系统（p189）/ 2023I 气垫悬浮车（p197）** 三条目，复用既有机制、**零 LLM 改写**：

1. **定位页**：fitz 文本定位 2023 章节内三题边界——题目标记（E 题）/（G 题）/（I 题）起始页为准（上述页码为锚点，精确边界执行时核实；相邻题分界 = 下一题标题行）。
2. **小题 PDF**：fitz 提取各题页 → `.scratch/topics-control-2023-2025/pdf/2023E.pdf` 等（几百 KB；**不放 34MB 汇编副本**）。
3. **确定性拆条**：提取小题文本 → 调 `split_topics_document(text)`（`topic_library.py:567`，YEAR_RE 年份 + TITLE_RE 题目标记（X 题）/行首 X 题：）得 TopicDraft（year/number/problem_text）；若输出 ≠ 1 条用「章节切片」兜底（标题行起点到文档末尾的原文段落，照 split_topics_document 分块逻辑）；**不经 LLM／webapp /api/topics/split 的 LLM 分支**（TOPIC_SPLIT_LLM_CHAR_CAP 内会走 LLM 拆条——刻意规避）。
4. **结构补全**：拆出草稿缺年份标题 / `## 参赛注意事项` 时，从全文章节文本确定性拼接（照 2026H 结构：`# 2023年全国大学生电子设计竞赛试题` → `## 参赛注意事项` → `# 题名（X 题）` → `## 一、任务` `## 二、要求` `## 三、说明` `## 四、评分标准`）；不 AI 生成。
5. **视觉核对**：fitz 渲染各题页面 PNG → `.scratch/topics-control-2023-2025/pages/`，read_image 检查：文本层是否漏图/表、评分标准表是否齐全（漏则从渲染页人工补录，记录差异）。
6. **入库**：本地脚本调 `confirm_topics`（或走 `POST /api/topics/confirm`，pdf = 小题 PDF）：entries = [{year: "2023", number: "E", problem_text, category: "control"}, ...]，program_dirs = [] → 条目目录 `2023E`/`2023G`/`2023I` + topic.md + 小题 PDF + manifest（original_pdf = 小题 PDF 文件名；category=control）。
7. **图注**：入库后 GET `/api/topics/2023E` 等（webapp L4783 触发 `enrich_topic_image_notes` 三级降级：渲染视觉 [图N 标注：…] → 文字标注 → 内嵌图视觉；幂等；失败静默）——不手写。
8. **断言**（每题）：topic.md 含「一、任务」「四、评分标准」；manifest 五字段齐全 + category=control；original_pdf 存在且 < 1MB；`resolve_number` 可解析。

**被谁阻塞：** 01（confirm 需支持 category 落盘）。

**状态：** resolved

**验收：** 全部 ✓（三题入库；每题的步骤 2-8 清单全过；pytest 全绿（176）；git 提交 = 3 个条目目录 + 拆分脚本，不含 34MB 副本；视觉核对记录见下）。

- [x] 定位三题精确页边界（题目标记核实）——2023 章节 p171-205；**2023E=p183-185、2023G=p189-193、2023I=p197-199**（（X 题）标记起始页 + 页眉「E - 1 / 3」×3 页确认；相邻题分界 = 下一题标题行）
- [x] 提取 3 份小题 PDF（< 1MB/份）到 .scratch——2023E 466KB / 2023G 949KB / 2023I 498KB
- [x] split_topics_document 拆条（或章节切片兜底）——零 LLM：2023E（1699 chars）/2023G（2843 chars）由库函数拆出恰 1 条；**2023I 输出 0 条 → 章节切片兜底**（1577 chars）——根因：库函数 TITLE_RE 字母域 [A-H]，2023 真题 A-K 十一题 I 题超域（库函数有意只认 A-H=往年范围）；兜底逻辑照 split_topics_document 的 _title_marks 行首偏移规则
- [x] 结构补全（年份标题/参赛注意事项/题名/四段——照 2026H）：`# 2023 年全国大学生电子设计竞赛试题` → `## 参赛注意事项`（6 条原文）→ `# 题名（X 题）` → `## 一、 任务` `## 二、 要求` `## 三、 说明` `## 四、 评分标准`（确定性从原文抽拼；去汇编页眉「E - 1 / 3」行；只补不删不改写）
- [x] 渲染 PNG + 视觉核对文本层完整性（记录差异与补录）——11 页渲染核对：E/G 图 1 内标注**在文本层**（提取为散行，原文本层顺序，保留）；**2023I 图 1 标注全缺失**（矢量标注文字不在 get_text 输出：弧半径/圆心坐标/标识线长度/赛道尺寸全部丢失=任务关键参数）→ **人工补录**（自 p197 渲染页抄录，追加在「图1 悬浮车测试赛道」行后，格式 [图1 标注补录（人工补录自 p197 渲染页…）]，照 2026D 图引用先例）；评分标准表三题文本层齐全（E 总分 120/G 总分 120/I 总分 120，与渲染页逐项一致）
- [x] confirm_topics 入库（category=control；programs=[]）——三条目目录 + topic.md + 小题 PDF + manifest（original_pdf=2023E.pdf 等；五字段齐全）
- [x] 触发 enrich_topic_image_notes 补图注（直接调用走完整三级降级；失败静默记录）——**无新增**：①渲染视觉未配置（无 vision key，webapp 亦会 gate 跳过）；②文字标注兜底失败——pypdf visitor_text 对该汇编 PDF 的片段坐标经 cm×tm 还原为 (0,0)（大量片段），_cluster_rows 错乱、「图N」标题行（「图」「1」被拆散到不同行）识别失败→空串。既有先例：2026C（独立 PDF）文字标注成功；2021F/2024H/2026D/F/H 的 [图N 标注：] 为渲染视觉（配置时生成）。题面图内关键参数已靠上述人工补录兜底（I 题）；E/G 标注在文本层。此为机制边界+环境限制，不改库代码（超出工单范围）
- [x] 断言 + pytest 全绿 + git 提交核实（无 34MB 副本）——断言（每题）：topic.md 含「一、任务」「四、评分标准」（正则容忍 E 题原文「一、 任务」空格）、年份标题/参赛注意事项/题名行、manifest 五字段+category=control、original_pdf 存在且 <1MB、resolve_number 可解析、programs=[]——3/3 全过；pytest 176 全绿

**词表维护记录：** tests/test_topic_library.py FOREIGN_TOPIC_MARKERS 删除「激光笔」并更新注释（2023-09-01 人工更新：2023E/G 题面合法大量使用红色/绿色激光笔，黑名单与合法题面冲突——黑名单契约注释「新增赛题入库若真用到需人工更新词表」）。

**参考：** `.scratch/real-run/topic_split_by_year.py`（split_topics_document 真机验证先例）；spec 三-C。
