# 10 — 补齐：模块说明弹窗的键盘无障碍（抄本仓既有弹窗范式）

**要做什么：** 键盘 / 读屏用户打开模块说明弹窗后：焦点**进入**弹窗、Tab **出不去**、
关闭后焦点**回到**触发它的那个「说明」按钮。

**被谁阻塞：** 03（入口是那一单加的）。**无**——可立即开始。

**状态：** resolved

- [x] 真机复现（现成）：深挖脚本 **B 节**，修前实测：打开说明后 `document.activeElement`
      仍是 `BUTTON.mod-info-btn`（焦点没进弹窗）；`Tab` 后焦点到了**弹窗外**的元素；
      `Esc` 关闭后焦点落到 `BODY`。
- [x] **口径：本仓已有范式，本弹窗漏抄**——照抄最近的先例（没有另发明）：

      | 先例 | Tab 焦点陷阱 | 关闭还焦 | 打开即聚焦 |
      |------|--------------|----------|------------|
      | `ui/confirm.js`（:54-63 / :74-75 / :108-111） | ✅ | ✅ | ✅（危险项默认给「取消」） |
      | `ui/codeview.js`（:1376-1385 / :1392 / :1399-1400） | ✅ | ✅ | ✅ |
      | `ui/codeeditor.js`（:191-192 / :203 / :214） | ✅ | ✅ | ✅ |
      | 模块说明弹窗（`openModuleInfo`） | 修前 ❌ → 修后 ✅ | 修前 ❌ → 修后 ✅ | 修前 ❌ → 修后 ✅ |

- [x] 修在 `openModuleInfo`（`ui/generate-recommend.js`）**一处**——三处入口共用它；
      ✕ / 遮罩 / Esc 三条关闭路径统一走同一个 `close()`（`closed` 布尔幂等，doc 上的
      keydown 只解绑一次）；打开即聚焦落在**关闭按钮**（Tab 循环第一站，口径同
      `codeview.js` 的快捷键帮助）。
      顺带消掉一处**既有泄漏**：原实现把「解绑 keydown」挂在 `overlay.addEventListener("remove")`
      上（`remove` 不是 DOM 事件，永远不触发），改成在 `close()` 里显式解绑。
- [x] 【明确不做】`role="dialog"` / `aria-modal="true"`：实测**全仓遮罩都还没有**这套语义。
      本单不为本弹窗加一个孤例（口径一致性优先）——审计脚本 B3b 会持续报这条 WARN，
      **那是给全仓的账**，不属本单。
- [x] 测试守卫（纯函数层测不了焦点 → 真机用例）：`tests/browser/module-intro.spec.mjs`
      新增「说明弹窗键盘无障碍：打开即聚焦、Tab 不外逃、关闭后焦点归位」——8 次 Tab +
      1 次 Shift+Tab 全部要求焦点留在弹窗内、关闭后断言焦点回到**同一个模块**的说明按钮、
      Esc 再按一次不报错（三路径幂等）。
- [x] **反向验证**：把「打开即聚焦」「Tab 陷阱」「关闭还焦」三处分别停用 → 用例当场红
      （实测报 `打开说明后焦点仍在弹窗外（mod-info-btn）—— 未聚焦弹窗`）；恢复后绿，
      grep 确认无反向验证残留。
- [x] 归零回归（修后实测）：`pytest` 全量绿、`node --test "tests/js/*.test.mjs"` 绿、
      `node --test tests/browser/module-intro.spec.mjs` **9/9**、深挖脚本 B 节
      B2/B4/B7 全部转 PASS（仅剩 B3/B3b 两条全仓 role/aria 的 WARN，见上「明确不做」）。
