# 演示脚本

> 本脚本由生成器按评分点 / 功能需求 / 模块清单确定性渲染（不含 AI 生成内容），
> 请按实物接线与现场情况调整演示顺序与细节。

## 演示前准备

- 编译烧录：按 README.md「快速上手：编译 + 烧录」操作
- 硬件接线：按 README.md「引脚接线表」完成
- 模块自检：按 README.md「验证顺序清单」逐项验证

## 模块验证演示
- delay：MSPM0 毫秒延时：delay_ms 基于 CPUCLK_FREQ 换算调用 delay_cycles，任何时钟频率自动适配。
- qmc5883l：QMC5883L 三轴磁力计 / 电子罗盘驱动（纯驱动切片，ADR 0009），API 与 hmc5883l 同名同语义（换芯片只改头文件名）：软 I2C 位操作读取（不占硬件 I2C 外设），qmc5883l_init 启动验 Chip ID（0x0D 读回 0xFF，不符返回 2）+ qmc5883l_read 出三轴原始值（16 位小端有符号，已符号扩展）+ qmc5883l_read_heading 出航向角（0–360°，顺时针，硬铁偏移入参）；适用于电子罗盘/航向角类赛题功能（定向跟随、航向保持、找北、与 UWB/AOA 配合的绝对朝向）。

## 演示注意事项

> 请按实物调整：本脚本由生成器自动产出，实际演示请以现场硬件与评委要求为准。
