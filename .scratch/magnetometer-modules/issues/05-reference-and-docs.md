# 05 — 参考条目与文档：原实现缺陷留痕 + 分工说明

**要做什么：** 让**下一个人（或 AI）查得到**这批的来龙去脉：库里有那两份「有硬伤但没人知道」的 `ml_hmc5883l` 旧实现，本批为何重写、改了哪六处、与库内姿态件怎么分工、HMC 与 QMC 怎么互替。做完后做题时 AI 选型有据可依，维护时能溯源。

**被谁阻塞：** 01、02（代码形态定稿）、03（登记完成）、04（verified 已翻牌，notes 里要写编译记录）。

**状态：** resolved

- [x] 参考文件库新增条目（按 `as32`×`zigbee_uart` 互替件各自立条目的先例）：`hmc5883l` 与 `qmc5883l` 各一条，互写互替关系与「同一路径不应混用寄存器」的警示
- [x] 条目内容含：**手册来源的诚实交代**（库内无 lckfb 磁力计页 → 依据官方数据手册；取不到的项明确标注）、API 语义、默认脚与重叠理由、编译记录、未上板
- [x] 旧实现缺陷留痕：明确指出 `sources/contest/2021F|2026H` 与桌面 `stm32base` 三处逐字节相同的 `ml_hmc5883l.c` 存在六处缺陷，并给出「不要照抄」的具体条目（读地址当寄存器、无符号扩展、yaw 死变量、CRA 取值存疑、init 不验 ID、ACK 全忽略）
- [x] 与库内同类件分工写清：`hmc5883l`/`qmc5883l`（磁力计·航向）vs `ml_mpu6050`（六轴姿态）vs `imu_uart`（串口姿态模块）vs `jy61p`（航向保持用姿态件）——各自适用场景与「不要同时选两个航向源」的建议
- [x] `CONTEXT.md` / 领域词表按需同步（磁力计/电子罗盘术语入表）

**结论（工单 05 已 resolved，2026-09-12）**：参考条目 `磁力计驱动移植笔记-HMC5883L-与-QMC5883L` 落盘（reference.json + 移植笔记.md + 两份 stm32 驱动副本，锚定 kit = 词表内 QMC5883L 模块条目）；笔记含手册来源交代、两件不可混用的寄存器对撞证据、API/默认脚/校准口径、与 jy61p / ml_mpu6050 / imu_uart 的分工表、旧实现六处缺陷清单。CONTEXT.md 引脚绑定行补磁力计双件的总线共享事实。
