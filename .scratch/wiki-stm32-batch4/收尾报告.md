# wiki-stm32-batch4 收尾报告（2026-09-06）

## 完成情况

| 工单 | slug | 形态 | 默认脚 | 矩阵 | verified |
|---|---|---|---|---|---|
| 01 | bmp180 | 软 I2C（共总线 PA6/PA7） | SCL=PA6/SDA=PA7 | 0/0（UV4） | true |
| 02 | ms5611 | 软 I2C（共总线 PA6/PA7，**64 位换算核心件**） | SCL=PA6/SDA=PA7 | 0/0（UV4） | true |
| 03 | dht11 | 单总线位时序 | DATA=PB3 | 0/0（UV4） | true |
| 04 | ds18b20 | 单总线位时序（**0x44 后 750ms 等待**） | DATA=PB1 | 0/0（UV4） | true |
| 05 | vl53l0x | **B 类新 slug（仅 stm32）** | 待定 | — | — |

- 批次 4 验收门槛（01-04 完成即批 4 可收尾）：**达成**。
- vl53l0x：**保持 blocked（待资料）**——`sources/materials/lckfb-地阔星移植手册/网盘下载/vl53l0x/` 目录不存在（用户未下载网盘包）；工单 05 已记录下载清单与放置建议，资料到位后单件收尾，不阻塞本批验收。

## 测试与一致性

- 全量 pytest：**3654 passed**（批次 3 为 3633；本批净增 21 用例 = 4 测试文件 stm32 扩展 + 镜像单测）。
- 一致性快检 sweep_4_modules.py：**4/4 OK**（共总线/SCL OUT_OD 守卫/单总线时间轴/64 位守卫/wordlist 挂接/mspm0 零改动面）。
- UV4 矩阵四件：单选生成 → UV4 真编译 **0 error、0 module warning**（exit 0）；build.log 存 `.scratch/wiki-stm32-batch4/matrix/<slug>/`。
- wordlist 零补录：bmp180/ms5611/dht11/ds18b20 四 slug 已在 `wordlist.json` 的 lib_modules（mspm0 条目同 slug 挂接），diff 零词表改动。
- mspm0 侧零改动：四件 mspm0 文件/配置未动（评审 + sweep 实证）。

## code-review 两轴（fixed point 0bdd1553 = 批次 3 收官）

### Standards 轴（编码标准）
- **0 硬违规**；1 项判断项整改已落实：**send_ack 参数名 `ack` → `is_nack`**（stm32 软 I2C 全族批次 3 定稿口径——名实相反（send_ack(1) 实发 NACK）；bmp180/ms5611 两件已改名 + aht10 式注释，矩阵复跑 0/0）。
- 判断项保留 1 项（不整改）：dht11 时间轴三常量（BIT0_LOW/BIT0_HIGH/BIT1_HIGH）无代码消费方——spec/工单明确要求 .h 单源 54/27/74 宏（协议事实 + 测试钉值），保留。
- 备注（后续批量可整改，非本批义务）：四件 description 仍写「mspm0 纯驱动」——与 sht30/aht10 双平台现状同款（简介判据①轻微漂移），建议后续双平台批次统一批量更新。

### Spec 轴（规格忠实）
- **零硬违反**：4 工单全部 checkbox 对应产物在 diff 内；manifest 四件 verified=true/kit 非空/notes 含手册路径+原页 source_url+网盘+缺陷清单+未上板；白名单 PA6/PA7 +2×2、PB3 +1、PB1 +1；64 位镜像单测（int64 负温向量 ±3.6e6——32 位形态偏差 ~18kPa）与海拔表镜像（±0.5m）齐。
- 1 条措辞一致性改进已落实：ds18b20 总线释放机制统一为「OUT+SET(1)」（工单初稿「gpio_init IU」为措辞偏差，实现与 mspm0 同款达标）——工单 04 与 .h 缺陷清单⑤已更正。
- 范围蔓延：无（CHANGELOG 边界回填属自动提交工具行为；test_pins 12 宏 = spec 摘要「8 宏」+ 工单 03/04 各 2 宏，微调必要）。

## 附带产物

- `.scratch/wiki-stm32-batch4/matrix/run_{bmp180,ms5611,dht11,ds18b20}_matrix.py`（4 个矩阵脚本，与批次 2/3 同配方）。
- `.scratch/wiki-stm32-batch4/sweep_4_modules.py`（批次 4 一致性快检）。
- CONTEXT.md 平台行补录「stm32 线批次 4」块（气压/单总线组 + 64 位口径 + vl53l0x 待资料状态）。

## 遗留（后续）

1. **vl53l0x（工单 05, blocked 待资料）**：下载清单 = 网盘索引.md `vl53l0x-laser-distance-sensor` 行 2 条链接（新式/旧式）；必需内容 = VL53L0X 驱动源码 + 寄存器表/初始化序列 + 接线图（XSHUT/GPIO 用途）；放 `sources/materials/lckfb-地阔星移植手册/网盘下载/vl53l0x/` 后通知实施。
2. 双平台模块 description「mspm0 纯驱动」措辞批量更新（建议并入后续批次收尾）。
3. 上板真机验证（四件全部未上板——notes 已注明）。
