# 07 — 代码折叠

**要做什么：** .c/.h 按花括号配对、.md 按标题层级折叠代码区；行号列箭头点击折叠/展开，折叠区显示占位行（`… N 行`），编辑经偏移映射写回模型，跳行落在折叠区内自动展开；.md 预览态标题可折叠。

**被谁阻塞：** 无强依赖（建议排在 06 后减少渲染器冲突；08 依赖本单）

**状态：** ready-for-agent

- [ ] 折叠区计算（fx 纯件）：.c/.h = 花括号配对（同行闭合 `{}` 不生成；跳过字符串 / 字符 / 单行注释 / 块注释内假括号；嵌套正确）；.md = 标题到同层或更高层标题前（顶层标题到文件尾）
- [ ] 行号列左侧（或行号列与代码之间）折叠箭头：可折叠行显示 ▸（折叠）/ ▾（展开）；点击切换
- [ ] 折叠态渲染：可见行 = 模型行去折叠区 + 每条折叠区一条占位行（`… N 行`，点击展开）；行号列显示模型真实行号（跳号）；textarea 内容 = 可见行拼接
- [ ] 偏移映射：编辑经「可见偏移 ↔ 模型偏移」静态映射写回模型；模型为全量基线（保存 / 脏判定 / 冲突 / 磁盘感知 / 大纲均基于模型）
- [ ] 跨折叠区编辑防护：选区跨越折叠区 → 自动展开后再处理；占位行直接编辑 → 展开
- [ ] 跳行（大纲 / 搜索 / 查找命中）落在折叠区内 → 自动展开该折叠区再定位；`clearAllFolds`（若有）切目录 / 关文件生效
- [ ] Ctrl+Shift+[ / ] 折叠/展开光标所在区；CDP 冒烟实测 Chrome 不冲突则保留，冲突则去掉并只留箭头 + 占位行交互
- [ ] .md 预览态：标题旁折叠钮（details/summary 或点击收起至下一标题）；预览态交互不影响编辑态
- [ ] 折叠状态会话内有效、不持久化；切目录 / 重命名后重算或清空
- [ ] tests/js：折叠区（嵌套 / 同行闭合 / 字符串注释假括号 / md 层级）、可见行映射（占位行、真实行号）、偏移映射（插入 / 删除 / 跨折叠展开 / 跳行命中折叠区）单测
- [ ] CDP 冒烟：Ctrl+Shift+[ 折叠 → 占位行出现、行号跳号；点击占位行展开；跳行到折叠区自动展开
- [ ] 既有功能回归：保存（折叠态编辑后 Ctrl+S 落盘内容 = 模型）、冲突模态、脏点、查找高亮与选中词高亮（若已实现）在折叠态只渲染可见行

**补充：** 最大单。行号 gutter / 高亮层 / textarea / 跳行 / 当前行索引全部改为「可见行」寻址（模型行号单独显示）；占位行不参与编辑内容，仅展示。若实现中发现索引迁移面过大，允许把「跳行自动展开」与「折叠态查找/选中词渲染」作为本单内后续 commit 分批落地，但本单必须完整交付。

(End of file - total 22 lines)
</content>