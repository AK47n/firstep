# 06 — 真机复检三档 + 文档收口（本特性的完成判据）

**要做什么：** 用**三档真实程度递增**的复检回答一个问题：
「一个网络不稳的新用户，从点下载到工具重启完成，这次会不会白等？」
并把结果如实记账——包括**没验到的部分**。

**被谁阻塞：** 03（两条链路接上）、05（界面话术定稿）。
→ 与 04 无强依赖，但 04 已在 03 之后，故实际顺序是 03 → 04 → 05 → 06。

**状态：** ready-for-agent

- [ ] **档① 本地可控**：通跑 01 的五个用例（稳定 / 连断 / 卡死 / 忽略 Range / 慢后恢复），
      证据 `verify-06-local.txt`（含每次的续传偏移、重试次数、最终 SHA256）。
- [ ] **档② 线上真资产**：对真实线上资产复核 `Range` 支持（`206` + `Content-Range`），
      并**跑一次小卷真实下载**；证据 `verify-06-online.txt`。
- [ ] **档③ 真实完整包端到端**：走 8020 沙箱口径（重定向 `USERPROFILE`、`FIRSTEP_LAUNCHER_PORT=8020`、
      真身只读），真实完整包「检查 → 下载 → **中途断开一次确认从偏移接上** → 替换 → 重启」；
      拔线/断流用本地可控代理或系统级手段，**不碰真身文件**；
      完成判据 = 工具在 8020 起得来 + 版本号正确 + 真身 `~\.contest_generator` mtime 未变。
- [ ] **反证**：至少一处「故意让断点逻辑失效 → 档① 转红」的可重跑证据。
- [ ] 记账：`docs/agents/local-environment.md` 补本特性产物（模拟服务器脚本 / 探针 / 端口 /
      半成品边车落点，以及「半成品会留在 updates 下」这条**用户可见的新事实**）。
- [ ] `CHANGELOG` 由提交信息自动补录（中文提交信息）；
      若本轮产生用户可见的行为变化，写进发布要点草稿（不发版，只留稿）。

## 备注 · 诚实边界

- 档③ 若因**真实带宽/时间**只能跑部分（例如只验到「下载完成并替换」、重启由既有路径覆盖），
  如实写清「哪一步没在这个环境下验过」，不写成「全链路已验证」。
- 本单不改产品代码：**只验、只记**。若验出缺陷 → 另开工单，不塞进本单。
