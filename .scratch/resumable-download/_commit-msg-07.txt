test(下载): 补完档③ 真实完整包端到端（替换→重启→版本号 1.1.0→1.1.1）+ 修 resumed_from 语义（工单 resumable-download/07）

工单 06 档③ 唯一没验到的一格补齐了：真实完整包「检查 → 下载 783 MB → 真替换 →
真重启 → 版本号正确」在用户那条路上成立。证据 verify-07-tier3-e2e.txt：

  替换前 /api/health version=1.1.0 → 真下载 783 MB（done / error='')
  → tools/update-app.py 真替换（8777 个条目安全校验通过、停服、备份、落位）
  → 真重启 → 替换后 /api/health version=1.1.1，复检 reason=up-to-date
  隔离边界：真身数据目录 mtime / updates 条目 / 工作树（排除 VCS 与缓存）三条全「未变」

替换环节为什么在 06 那次没走完——零下载诊断定位到**不是产品缺陷**：
那支验收脚本搭一次性工具根时只复制了 src/，没复制 tools/，而替换链入口就是
tool_root/tools/update-app.py，于是 apply_full_package 的拉起前预检拒了它
（probe-07-stage-preflight.py：不带 tools/ → ok=False；补上 → ok=True）。
产品侧的预检工作正常，拦下的恰恰是一个假的工具根。

产品代码只改一处（+ 文档同步）：
- resumed_from 语义按 spec 修正——口径是「本次**真正用 Range 接上**的起始偏移」，
  原实现报的是「进函数时盘上的字节」：服务器忽略 Range 回 200 时明明从 0 重下，
  却报「续传了」。改由 _AttemptOutcome.resumed_at 在判完「有没有真接上」后回填；
  判据 test_resumed_from_reports_whether_range_actually_connected（反向验证：
  去掉修正 → assert 102400 == 0 转红）。
- 边车口径文档同步（CONTEXT.md / 两个任务模块 docstring / write_partial_marker）：
  「只在取消 / 失败时写」是工单 03 的原口径，工单 06 真机实测硬杀进程两条路都不走
  （白下 210 MB）后已改成「开跑就写」——四处文档记的还是旧行为，属评审判出的硬性违规。

评审五条断言的独立复现（probe-07-review-claims.py，真 HTTP）：成立 2 / 误报 2 /
对照组 1。「本地比远端大不重下」「完整卷留孤儿边车」两条经复现是**误报**。

显式决策点（未修，见工单验收记录第五节）：服务器 416 未按 spec「删掉从 0 重下」
处理——异常直接冒泡、半成品留盘，同一坏半成品会反复卡住该卷。修法约 10 行。

回归：4429 passed / 1 skipped；probe-01-resume.py 与 probe-03-corridor.py 复跑总判 PASS。
