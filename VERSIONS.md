# 版本更新记录

面向用户的版本要点：只看「用户听得懂」的改进——新增了什么能力、修了什么坑，
不写工单编号、内部重构与性能黑话（那些留在 CHANGELOG.md 的自动草稿里）。

**发版流程**（每次发布新版本时执行）：把 CHANGELOG.md 最近积累的自动草稿归纳成
3~6 条要点，写在本文件**最顶部**（新版本永远在最前），然后按需发出 GitHub Release。

**格式约定**：`## v<版本> (<YYYY-MM-DD>)` 开新版本区块（ASCII 括号、严格日期）；
组内条目 `- <标签>：<一句话>`，标签 ∈ 新增 / 改进 / 修复 / 性能（其他不带标签）；
可选的 `- 主题：<一句话概括本期>` 放在组内第一条，渲染为卡片主题。

## v1.2.0 (2026-09-13，2026-09-14 重发)
- 主题：下载断了能接着下 + 第一次装完就知道双击哪个 + 解压不再报「路径太长」
- 新增：下载断线后**从断点接着下**——以前一次网络抖动就从头再来，完整包这种几百 MB 的尤其伤；现在按已下好的字节接着传
- 新增：中途取消、下载失败、甚至**强制结束工具（任务管理器 / 崩溃 / 断电）**，已下载的部分都会留着；下次点重试从那里接着下（校验失败才丢掉重来）
- 修复：**被截断的下载不再当成「下完了」**——以前进度跑到 100% 却报校验失败、又从 0 重来，现在当场识别为「没下完」并接着下
- 修复：断线**自动重试**（等待 2 → 4 → 8 … 秒，最多 60 秒一次，**不会因为你没管它就放弃**；随时可取消）
- 修复：**解压完整包报「路径太长」（错误代码 `0x80010135`）**——Windows 自带解压器把「解压位置 + 包内路径」加起来不能超 259 字符，而包里有几个厂商例程目录套得特别深，用户名一长、或者解压时多套了一层文件夹就会撞上；**点「跳过」还会静默丢掉那个文件**，你会拿到一个看着解压成功、其实缺文件的包。现在包内最长路径从 223 字符压到 194，并加了一道发版闸门（超 200 字符不放行），常见的解压姿势都不会再撞
- 改进：下载进度说人话——剩余时间写成「约 12 分钟」，网络慢时明说「网络较慢」，自动重试时写明「正在自动重试（第 N 次），从 X% 接着下」；失败分成两类话术，校验类会直接告诉你「重新下载也不会有变化」
- 改进：第一次装完就知道双击哪个——安装脚本会在桌面放一个 firstep 快捷方式，并在结尾说明「以后双击桌面这个，不用再运行本脚本」；完整包解压后第一个文件是说明书 `00-START-HERE.txt`，照三步走即可

## v1.1.1 (2026-09-13)
- 主题：修复「一键全量下载」与全量后的资料库更新
- 修复：点「下载完整 firstep」时，下载会正常完成但**替换没有执行**（工具不重启、文件没更新），页面却显示成功——现在替换真正执行，完成后自动重启
- 修复：全量下载完成后，「资料库更新」仍提示「版本未知、请下载完整包」——现在能正确读到已落位的资料库基线，之后只提示变化的部分（增量）
- 修复：更新时去停另一个端口上的实例；现在只停本工具自己监听的那个端口
- 修复：源码方式启动（`start-app.bat`，即默认启动方式）下，工具根目录判定错误带来的上述连带问题

## v1.1.0 (2026-09-13)
- 主题：三段式更新——从此不用再重下 6 GB 完整包
- 新增：一键下载完整 firstep（约 1 GB）——「设置 → 完整包下载」点一下，自动下载、替换并重启；修损坏、换机器、本地没有资料库基线时都用它，不用手动下分卷、不用装 7-Zip；中途取消或断网后重试只补没下完的卷
- 新增：应用内一键更新——「设置 → 软件更新」检查更新 → 一键更新，自动完成停服、备份、替换、重启；DeepSeek key、任务状态、对话记录与电赛资料库原样保留
- 新增：资料库增量更新——「设置 → 资料库更新」按批次（芯片手册 / K230 视觉 / 真题…）勾选下载，只传变化的部分；真实进度与速度、卷级断点续传、下载完自动解压落位备份
- 新增：硬件模块扩充——红外对射、Zigbee 无线链路、磁力计（HMC5883L / QMC5883L）等入库，双平台驱动 + 编译矩阵验证
- 改进：模块「说明」入口——推荐区模块名旁可直接看简介与用法；参考库文件按类型分流打开（视频 / 音频 / 图片新标签内联预览）；引脚冲突诊断给出「至少去掉几个」的明确结论
- 修复：启动器失败原因可机读（更新中 / 上次更新未完成 / 端口被占用分别提示）；自动编译与推荐链路的若干误判收口

## v1.0.0 (2026-08-30)
- 主题：首个正式版本——贴上赛题原文，生成「打开就能编译、直接开写」的完整工程
- 新增：三步主线——AI 读懂赛题、按题推荐模块、引脚配置出骨架，一次生成完整工程
- 新增：双平台支持——STM32F103C8T6（Keil5）与 MSPM0G3507（CCS）
- 新增：自动编译 + AI 修复——编译报错自动定位，修到通过为止
- 新增：模块推荐收敛——AI 按题面逐句对照功能需求，命中可勾选、可多实例
- 新增：历年赛题库 + 资料三馆——真题入库，参考例程 / PDF 资料 / 模块随手管理
- 新增：新手指引 12 步向导 + 交接提示词——第一次用也能从头跑到烧录交付

<!-- 格式示例（正式条目写在示例上方）：
## v1.1.0 (2026-09-04)
- 主题：一句话概括本期
- 新增：用户能感知的新能力
- 改进：既有能力的打磨
- 修复：用户遇到的坑
-->
